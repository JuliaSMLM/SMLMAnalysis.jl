var documenterSearchIndex = {"docs":
[{"category":"section","location":"steps/bagol/#Bayesian-Grouping-(BaGoL)","page":"Bayesian Grouping","text":"A single fluorophore blinks many times during an acquisition, so it appears not as one point but as a scatter of localizations around its true position. Bayesian Grouping of Localizations (BaGoL) groups those repeated blinks back into the individual emitters that produced them, yielding emitter positions more precise than any single localization — and, as a byproduct, a count of how many emitters are actually present. The step is selected by a BaGoLConfig and backed by SMLMBaGoL, which runs a reversible-jump MCMC (RJMCMC) sampler over the number and positions of emitters.\n\nanalyze(smld, BaGoLConfig(μ = 10.0, shape = 2.0))   # → (grouped_smld, StepInfo)","title":"Bayesian Grouping (BaGoL)"},{"category":"section","location":"steps/bagol/#When-to-use-/-prerequisites","page":"Bayesian Grouping","text":"Use BaGoL for counting and for maximum positional precision — typically as an alternative or supplement to a final Rendering: group first, then render the grouped emitters. It is normally the last analysis step.\n\nBaGoL makes two assumptions, both of which dictate preprocessing:\n\nEach localization is a real observation of a single emitter. Run BaGoL on cleaned localizations — apply a Quality Filter and an Intensity Filter first to drop spurious detections and multi-emitter fits (where one localization stands for two or more emitters), which BaGoL cannot model.\nThe reported precision σ is correct. BaGoL groups by each localization's fitted σ_x, σ_y, so it expects Frame Connection (and ideally the uncertainty calibration configured inside FrameConnectConfig) to have run, and it defaults to se_adjust = :auto to repair any residual σ underestimate (see below). Underestimated σ causes BaGoL to over-split one emitter into several.","title":"When to use / prerequisites"},{"category":"section","location":"steps/bagol/#Inputs,-returns-and-artifacts","page":"Bayesian Grouping","text":"Input: the current smld of 2D fitted localizations (Emitter2DFit), each carrying its uncertainty σ_x, σ_y. Positions and uncertainties are in microns.\nReturns: (grouped_smld, StepInfo). grouped_smld.emitters are the grouped emitters (Emitter2DFit, each with a posterior position uncertainty), not the input localizations. The full SMLMBaGoL.BaGoLDiagnostics is on StepInfo.info.diagnostics.\nArtifacts (when outdir is set, at STANDARD verbosity, in NN_bagol/): config.toml and info.toml; a metrics report and posterior diagnostic figures (upstream write_report / plot_report); a render set written by upstream render_report — render_mapn, render_sr, render_circles, and render_partitions (at zoom = 50); and, when se_adjust = :auto, a finder diagnostic plot (plot_se_adjust). The grouped SMLD is checkpointed to smld_bagol.jld2 at Checkpoint.EXPENSIVE.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/bagol/#Concept","page":"Bayesian Grouping","text":"BaGoL treats the localizations as measurements from a mixture of emitters whose number and positions are both unknown. Its RJMCMC moves split, merge, create, and remove emitters, so the chain explores models with different emitter counts rather than fixing one. Instead of committing to a single grouping it builds a full posterior over both the emitter count K and the positions; for downstream use the posterior is summarized by the MAP-N estimate — the most probable count with a representative grouping at that count — pooling each emitter's localizations to a super-resolved position. For the priors, the collapsed sampler, and the MAP-N estimators see the SMLMBaGoL documentation.","title":"Concept"},{"category":"section","location":"steps/bagol/#Configuration","page":"Bayesian Grouping","text":"BaGoLConfig is re-exported from SMLMBaGoL (the \"upstream owns the config\" idiom); its fields map 1:1 onto run_bagol kwargs. The fields you set most often:\n\nfield typical / default meaning\nμ 10.0 NegBin mean localizations (blinks) per emitter (typed \\mu+Tab)\nshape 2.0 count-distribution shape (1 ≈ dSTORM/exponential, >1 ≈ peaked/DNA-PAINT)\nlearn_distribution true learn count params: true (both), false (fix both), :mu, or :shape\nn_iterations 4000 total MCMC iterations\nburn_in 2000 iterations discarded before accumulating\nse_adjust :auto :auto estimates excess uncertainty τ and adds it in quadrature (σ²+τ²); 0.0 = none; a number = manual τ (µm)\npartition_sigma 3.0 precision-weighted DBSCAN partition threshold in σ units (Inf = no partitioning)\n\nSee the SMLMBaGoL documentation for the complete field list (model choices, split/merge tuning, partitioning) and defaults.\n\n# Group cleaned, frame-connected localizations into emitters\n(grouped, info) = analyze(smld, BaGoLConfig(μ = 10.0, shape = 2.0,\n                                            n_iterations = 4000, se_adjust = :auto))\n\ngrouped.emitters            # the grouped emitters (super-resolved positions)\ninfo.info.n_emitters        # MAP-N emitter count\ninfo.info.diagnostics       # full BaGoLDiagnostics (posterior_k, acceptance_rates, …)","title":"Configuration"},{"category":"section","location":"steps/bagol/#Output-and-interpretation","page":"Bayesian Grouping","text":"The step's StepInfo.summary reports the headline numbers:\n\nfield meaning\nn_locs_in input localization count\nn_emitters MAP-N number of grouped emitters\ncompression n_locs_in / n_emitters — localizations pooled per emitter\nfinal_μ, final_shape learned count-distribution parameters\nn_partitions number of spatial partitions the data was split into\ntau_um applied excess uncertainty τ̂ (µm); the se_adjust correction actually used\nse_adjust applied (τx, τy) provenance\n\nSanity checks (drill into info.info.diagnostics for the full picture): a plausible compression is a handful to tens of localizations per emitter; a surprisingly large final_μ signals under-splitting (too few emitters, each absorbing too many localizations). The diagnostics' posterior_k should be concentrated on a count rather than smeared across many K, and the split / merge / birth / death acceptance_rates should be nonzero — all-zero means the chain is stuck at one K. A large tau_um means the reported σ was badly underestimated upstream.","title":"Output & interpretation"},{"category":"section","location":"steps/bagol/#Notes-and-caveats","page":"Bayesian Grouping","text":"Clean first. Spurious and multi-emitter fits violate BaGoL's single-emitter assumption; filter them out before grouping or they corrupt the count.\nCalibrated σ matters. Underestimated precision over-splits emitters. Prefer calibrated, frame-connected input and keep se_adjust = :auto so the finder repairs residual error (it self-guards against double-correcting an already σ-corrected SMLD).\nIt is expensive. BaGoL runs a full MCMC chain; this is the costliest step and is checkpointed by default. Large datasets are partitioned automatically and run in parallel — start Julia with -t auto.\nThe output is emitters, not localizations. Downstream steps (e.g. a render) operate on the grouped emitters, so a BaGoL render looks sparser and sharper than the raw-localization render.","title":"Notes & caveats"},{"category":"section","location":"steps/bagol/#References","page":"Bayesian Grouping","text":"M. Fazel, et al. \"High-Precision Estimation of Emitter Positions using Bayesian Grouping of Localizations.\" Nature Communications 13, 7152 (2022). doi:10.1038/s41467-022-34894-2\n\nSee the SMLMBaGoL documentation for the RJMCMC sampler, priors, MAP-N estimators, and uncertainty correction in full.","title":"References"},{"category":"section","location":"references/#references-page","page":"References","text":"The primary literature for the methods SMLMAnalysis orchestrates, grouped by step. Each step page repeats its own reference and links to the backing package's documentation for the algorithm details.","title":"References"},{"category":"section","location":"references/#Detection-and-fitting","page":"References","text":"Detection (SMLMBoxer). F. Huang, T. M. P. Hartwich, F. E. Rivera-Molina, et al. \"Video-rate nanoscopy using sCMOS camera-specific single-molecule localization algorithms.\" Nature Methods 10, 653–658 (2013). doi:10.1038/nmeth.2488\nMLE fitting (GaussMLE). C. S. Smith, N. Joseph, B. Rieger, K. A. Lidke. \"Fast, single-molecule localization that achieves theoretically minimum uncertainty.\" Nature Methods 7, 373–375 (2010). doi:10.1038/nmeth.1449\nLocalization precision / CRLB. The reported σ are the exact CRLB from the MLE Fisher information — Smith et al. 2010 (above), extended to per-pixel sCMOS noise by Huang et al. 2013 (above) — not an analytical approximation.\nsCMOS noise model. Huang et al. 2013 (above).\nMulti-emitter fitting & goodness-of-fit (pvalue). F. Huang, S. L. Schwartz, J. M. Byars, K. A. Lidke. \"Simultaneous multiple-emitter fitting for single molecule super-resolution imaging.\" Biomedical Optics Express 2, 1377–1393 (2011). doi:10.1364/BOE.2.001377","title":"Detection & fitting"},{"category":"section","location":"references/#Frame-connection","page":"References","text":"D. J. Schodt, K. A. Lidke. \"Spatiotemporal Clustering of Repeated Super-Resolution Localizations via Linear Assignment Problem.\" Frontiers in Bioinformatics 1, 724325 (2021). doi:10.3389/fbinf.2021.724325","title":"Frame connection"},{"category":"section","location":"references/#Drift-correction","page":"References","text":"J. Cnossen, T. J. Cui, C. Joo, C. Smith. \"Drift correction in localization microscopy using entropy minimization.\" Optics Express 29, 27961–27974 (2021). doi:10.1364/OE.426620\nM. J. Wester, D. J. Schodt, H. Mazloom-Farsibaf, M. Fazel, S. Pallikkuth, K. A. Lidke. \"Robust, fiducial-free drift correction for super-resolution imaging.\" Scientific Reports 11, 23672 (2021). doi:10.1038/s41598-021-02850-7","title":"Drift correction"},{"category":"section","location":"references/#Bayesian-grouping","page":"References","text":"M. Fazel, et al. \"High-Precision Estimation of Emitter Positions using Bayesian Grouping of Localizations.\" Nature Communications 13, 7152 (2022). doi:10.1038/s41467-022-34894-2","title":"Bayesian grouping"},{"category":"section","location":"references/#Clustering","page":"References","text":"DBSCAN. M. Ester, H.-P. Kriegel, J. Sander, X. Xu. \"A Density-Based Algorithm for Discovering Clusters in Large Spatial Databases with Noise.\" Proc. 2nd Int. Conf. on Knowledge Discovery and Data Mining (KDD-96), 226–231 (1996).\nHDBSCAN. R. J. G. B. Campello, D. Moulavi, J. Sander. \"Density-Based Clustering Based on Hierarchical Density Estimates.\" PAKDD 2013, LNCS 7819, 160–172. doi:10.1007/978-3-642-37456-2_14\nVoronoi / SR-Tesseler. F. Levet, et al. \"SR-Tesseler: a method to segment and quantify localization-based super-resolution microscopy data.\" Nature Methods 12, 1065–1071 (2015). doi:10.1038/nmeth.3579\nClustering tendency (Hopkins statistic). B. Hopkins, J. G. Skellam. \"A new method for determining the type of distribution of plant individuals.\" Annals of Botany 18, 213–227 (1954). doi:10.1093/oxfordjournals.aob.a083391\n\nSee the SMLMClustering documentation for the full set of backends and their individual references.","title":"Clustering"},{"category":"section","location":"references/#Cross-correlation-(multi-channel)","page":"References","text":"Pair correlation, theory. B. D. Ripley. \"Modelling spatial patterns.\" Journal of the Royal Statistical Society B 39, 172–212 (1977).\nPair correlation in SMLM. P. Sengupta, T. Jovanovic-Talisman, D. Skoko, et al. \"Probing protein heterogeneity in the plasma membrane using PALM and pair correlation analysis.\" Nature Methods 8, 969–975 (2011). doi:10.1038/nmeth.1704\nOver-counting correction. S. L. Veatch, B. B. Machta, S. A. Shelby, et al. \"Correlation functions quantify super-resolution images and estimate apparent clustering due to over-counting.\" PLoS ONE 7(2), e31457 (2012). doi:10.1371/journal.pone.0031457","title":"Cross-correlation (multi-channel)"},{"category":"section","location":"references/#Methods-native-to-SMLMAnalysis","page":"References","text":"The Quality Filter, Intensity Filter, and Density Filter steps are implemented in SMLMAnalysis. Their methods (Poisson upper-tail multi-emitter rejection against an estimated excitation field; neighbor-count density filtering with automatic threshold selection) are described in full on their step pages.","title":"Methods native to SMLMAnalysis"},{"category":"section","location":"references/#How-to-cite","page":"References","text":"If you use SMLMAnalysis in your research, please cite the package together with the primary references for the specific methods your pipeline used (the steps above). A package citation entry (CITATION.bib) will accompany the registered release.","title":"How to cite"},{"category":"section","location":"workflows/install/#Installation-and-Setup","page":"Installation & Setup","text":"","title":"Installation & Setup"},{"category":"section","location":"workflows/install/#Requirements","page":"Installation & Setup","text":"Julia 1.12 or newer.\nA CUDA-capable GPU is recommended. The fitting step uses GaussMLE, which is GPU-accelerated via CUDA; see the GaussMLE documentation for GPU/CPU options.\nStart Julia with multiple threads — several steps (drift correction, frame connection) are threaded:\njulia -t auto --project=.","title":"Requirements"},{"category":"section","location":"workflows/install/#Installing","page":"Installation & Setup","text":"Once SMLMAnalysis is registered in the Julia General registry, installation will be the usual one-liner:\n\nusing Pkg\nPkg.add(\"SMLMAnalysis\")\n\nnote: Registration status\nSMLMAnalysis is being prepared for registration. Until it and its JuliaSMLM dependencies are registered, install from source as below. (This page will switch to Pkg.add once the registration is live.)","title":"Installing"},{"category":"section","location":"workflows/install/#From-source-(current)","page":"Installation & Setup","text":"Clone the repository and instantiate its environment:\n\ngit clone https://github.com/JuliaSMLM/SMLMAnalysis.jl\ncd SMLMAnalysis.jl\njulia -t auto --project=. -e 'using Pkg; Pkg.instantiate()'\n\nThen:\n\nusing SMLMAnalysis\n\nusing SMLMAnalysis re-exports the key ecosystem types and verbs (cameras, emitter types, the step configs, cluster, render, run_bagol, frameconnect, …), so for most work no further imports are needed.","title":"From source (current)"},{"category":"section","location":"workflows/install/#Verifying-the-install","page":"Installation & Setup","text":"A quick end-to-end check on simulated data:\n\nusing SMLMAnalysis\n\n# Simulate a small dataset and its image stack (SMLMSim, re-exported)\n# then run a minimal pipeline:\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130)),\n        RenderConfig(zoom = 10),\n    ],\n)\n(result, info) = analyze(images, config)\n@show length(result.smld.emitters)\n\nSee Getting Started for a full simulated walkthrough, and Running a Pipeline for the config-driven and step-by-step styles.","title":"Verifying the install"},{"category":"section","location":"workflows/install/#Building-the-documentation","page":"Installation & Setup","text":"The documentation is built from the main checkout of the repository (its docs/ environment anchors its path-sourced JuliaSMLM dependencies relative to that checkout):\n\njulia --project=docs -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate()'\njulia --project=docs docs/make.jl","title":"Building the documentation"},{"category":"section","location":"steps/cross_align/#Cross-Alignment","page":"Cross-Alignment","text":"In a multi-color experiment each channel is acquired separately, so a residual shift between channels — from chromatic offset, stage settling, or registration error — leaves the colors misregistered in the overlay. The cross-alignment step estimates that shift and applies it, registering every channel to a common reference frame so structures that should coincide actually overlap. It is a multi-channel step: it operates on a Vector{BasicSMLD} (one SMLD per channel), is selected by a CrossAlignConfig, and is a native SMLMAnalysis step built on SMLMDriftCorrection's align_smld.\n\nanalyze(smlds, CrossAlignConfig(method = :entropy))   # → (aligned_smlds, StepInfo)","title":"Cross-Alignment"},{"category":"section","location":"steps/cross_align/#When-to-use-/-prerequisites","page":"Cross-Alignment","text":"Use in a multi-channel pipeline, after each channel has been independently detected, fitted, filtered, and (ideally) drift-corrected. It is normally listed in a MultiTargetConfig.steps and dispatched on the resulting Vector{BasicSMLD} — see Multi-Channel.\nAlignment is data-driven: it registers channels by the structures they share, so it needs spatially correlated content across channels (overlapping or co-localized features). It corrects a global translation, not chromatic distortion or rotation.\nA common pattern is to bracket this step between two Composite Render steps to see the overlay before and after alignment.","title":"When to use / prerequisites"},{"category":"section","location":"steps/cross_align/#Inputs,-returns-and-artifacts","page":"Cross-Alignment","text":"Input: the channels as a Vector{BasicSMLD}.\nReturns: (aligned_smlds, StepInfo). The returned vector holds the same channels with the per-channel shift applied to their coordinates; the step is state-modifying for the downstream multi-target steps. The per-channel shifts and the upstream alignment info live on StepInfo.info (a CrossAlignInfo).\nArtifacts (when outdir is set), written under NN_crossalign/: config.toml, info.toml (the upstream AlignInfo fields), and — at STANDARD verbosity or above — stats.md with the method, channel count, max shift, and a per-channel table of X/Y/magnitude shifts in nm.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/cross_align/#Concept","page":"Cross-Alignment","text":"Aligning channels and correcting drift are the same registration problem with a shorter trajectory: here the \"trajectory\" is a single rigid translation per channel rather than a time-varying path. With method = :entropy the step seeds the offset with a histogram cross-correlation and then refines it by minimizing the entropy of the combined localization cloud — the shift that makes shared structures pile up most tightly (Cnossen et al. 2021), the same fiducial-free, redundancy-based idea used for Drift Correction (Wester et al. 2021). With method = :fft it stops at the cross-correlation peak (no entropy refinement), which is faster but less precise. For the algorithm in full see the SMLMDriftCorrection documentation.","title":"Concept"},{"category":"section","location":"steps/cross_align/#Configuration","page":"Cross-Alignment","text":"CrossAlignConfig is an SMLMAnalysis multi-target config whose fields map onto the upstream AlignConfig:\n\nfield typical/default meaning\nmethod :entropy :entropy (cross-correlation seed + entropy refinement) or :fft (cross-correlation only)\nmaxn 100 maximum neighbors used in the entropy calculation\nhistbinsize 0.05 histogram bin size (µm) for the cross-correlation seed\n\n# Two-channel overlay: render, align, render again\nmt = MultiTargetConfig(\n    labels = [:ch1, :ch2],\n    steps = [\n        CompositeRenderConfig(zoom = 20.0, strategy = GaussianRender()),\n        CrossAlignConfig(method = :entropy, histbinsize = 0.05),\n        CompositeRenderConfig(zoom = 20.0, strategy = GaussianRender()),  # post-alignment\n    ],\n    outdir = \"output/cell1/\",\n)\n\n# Or call the step directly on a channel vector\n(aligned, info) = analyze(smlds, CrossAlignConfig(method = :entropy))\nshifts = info.info.shifts          # Vector{Vector{Float64}}, one (x, y) per channel, in µm","title":"Configuration"},{"category":"section","location":"steps/cross_align/#Output-and-interpretation","page":"Cross-Alignment","text":"The step's StepInfo.summary reports the headline numbers:\n\nfield meaning\nmax_shift_nm largest per-channel shift magnitude applied (nm)\nn_channels number of channels aligned\nmethod the alignment method used (:entropy / :fft)\n\nStepInfo.info (a CrossAlignInfo) carries the full detail: shifts (the applied per-channel (x, y) offsets, in µm), max_shift_nm, the upstream align_info, and elapsed_s.\n\nSanity checks: shifts of a few tens to a couple hundred nm are typical for chromatic/registration offset, and the post-alignment composite render should show the colors snapping into register. A shift much larger than the expected inter-channel offset usually means the channels lack enough shared structure to register against.","title":"Output & interpretation"},{"category":"section","location":"steps/cross_align/#Notes-and-caveats","page":"Cross-Alignment","text":"Translation only. Cross-alignment removes a global X/Y shift per channel; it does not correct rotation, scaling, or field-dependent chromatic distortion. For those, apply an upstream geometric transform first.\nShared structure is required. Channels with no co-localized or overlapping features give an ill-defined offset; the cross-correlation seed will lock onto noise.\n:fft vs :entropy. Use :fft for a fast first pass; use :entropy (the default) when you need the tighter, sub-bin offset.\nUnits. shifts are in µm (matching the SMLD coordinates); the summary and stats.md report magnitudes in nm.","title":"Notes & caveats"},{"category":"section","location":"steps/cross_align/#References","page":"Cross-Alignment","text":"J. Cnossen, T. J. Cui, C. Joo, C. Smith. \"Drift correction in localization microscopy using entropy minimization.\" Optics Express 29, 27961 (2021). doi:10.1364/OE.426620\nM. J. Wester, et al. \"Robust, fiducial-free drift correction for super-resolution imaging.\" Scientific Reports 11, 23672 (2021). doi:10.1038/s41598-021-02850-7\n\nSee the SMLMDriftCorrection documentation for the alignment algorithm in full and all configuration options.","title":"References"},{"category":"section","location":"steps/crosscorr/#Cross-Correlation","page":"Cross-Correlation","text":"Two proteins imaged in separate channels may be distributed independently, may co-cluster, or may exclude one another. The cross-correlation step quantifies this by computing the pair cross-correlation function g(r) between two channels: the density of channel-B localizations at distance r from a channel-A localization, normalized by what complete spatial randomness would give. It is selected by a CrossCorrConfig and is a native SMLMAnalysis step built on the KD-tree range queries of NearestNeighbors.jl.\n\nanalyze(smlds, CrossCorrConfig(r_max = 0.5))   # → (smlds, StepInfo)\n\nThis is a multi-channel step: it operates on a Vector{BasicSMLD} (see Multi-Channel). It is read-only — it computes a curve and writes it out, but the SMLDs pass through unmodified.","title":"Cross-Correlation"},{"category":"section","location":"steps/crosscorr/#When-to-use-/-prerequisites","page":"Cross-Correlation","text":"Run on two channels of localizations once both are reconstructed (after detection/fitting and the usual quality filter on each channel).\nThe two channels must live in a common coordinate frame. If they came from different optical paths, register them first with Cross-Alignment; otherwise a real co-localization can be masked by a residual channel-to-channel offset.\nBoth channels need enough localizations to populate the radial bins — sparse data gives a noisy g(r), especially at small r.","title":"When to use / prerequisites"},{"category":"section","location":"steps/crosscorr/#Inputs,-returns-and-artifacts","page":"Cross-Correlation","text":"Input: the channel vector smlds. The two channels are picked by the 1-based channels index tuple; they must be distinct and in range (the step errors otherwise). The FOV area used for normalization is taken from channel A's camera, which assumes both channels share a camera.\nReturns: (smlds, StepInfo) — smlds is returned unchanged. The computed curve lives on StepInfo.info, a CrossCorrInfo carrying r, g, n_a, n_b, area, and the channel labels.\nArtifacts (when outdir is set): crosscorr_gr.csv (the r,g table, always written), and at STANDARD verbosity crosscorr_gr.png (the curve with the CSR=1 line and the peak annotated) and stats.md (counts, FOV area, peak g(r) and its radius). The config is also dumped for provenance. Nothing is checkpointed — the SMLDs are unchanged.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/crosscorr/#Concept","page":"Cross-Correlation","text":"For each localization in channel A, the step finds every channel-B localization within r_max via a KD-tree inrange query, bins the pair distances into shells of width dr, and normalizes each shell by the count expected under complete spatial randomness (CSR):\n\ng(r) = fractextobserved BA pairs in shell  r\n            n_A rho_B (pi(r_textouter^2 - r_textinner^2))\n\nwhere rho_B = n_B  textarea is the mean channel-B density. The denominator is the number of pairs a uniform, independent channel B would contribute to that annulus. Reading the result:\n\ng(r)  1 — co-clustering: more B near A than chance, at separation r.\ng(r) approx 1 — the two channels are independent (the CSR baseline).\ng(r)  1 — exclusion / anti-correlation: the species avoid each other.\n\nBecause this is a cross-correlation between distinct channels, there is no self-pair spike at r to 0 from repeated blinks of one molecule (zero-distance pairs are skipped); a small-r rise is genuine co-localization blurred by localization precision.\n\nEdge correction. A localization near the FOV edge sees a clipped annulus, so naive counts undercount pairs at large r. With edge_correction = true each pair is weighted by the inverse of the fraction of its circle of radius r that lies inside the rectangular FOV — Ripley's isotropic correction (Ripley\n\n— up-weighting near-boundary points to compensate.","title":"Concept"},{"category":"section","location":"steps/crosscorr/#Configuration","page":"Cross-Correlation","text":"field typical / default meaning\nr_max 1.0 maximum separation in μm over which g(r) is computed\ndr 0.01 radial bin width in μm; sets the curve's resolution\nedge_correction true apply Ripley's isotropic edge correction for the rectangular FOV\nchannels (1, 2) 1-based indices (A, B) of the two channels to correlate; must differ and be in range\n\n# g(r) between channels 1 and 2 out to 500 nm, 10 nm bins\n(smlds, info) = analyze(smlds,\n    CrossCorrConfig(r_max = 0.5, dr = 0.01, channels = (1, 2)))\n\ngr = info.info          # CrossCorrInfo\ngr.r                    # bin centers (µm)\ngr.g                    # g(r) values","title":"Configuration"},{"category":"section","location":"steps/crosscorr/#Output-and-interpretation","page":"Cross-Correlation","text":"The step's StepInfo.summary reports the headline numbers:\n\nfield meaning\nn_a, n_b localization counts in the two channels\nn_bins number of radial bins (≈ r_max / dr)\npeak_g the maximum of g(r) — the strongest correlation observed\nchannel_a, channel_b the channel labels used (from the pipeline's labels, else Ch1, Ch2)\n\nSanity checks: a peak_g near 1 with a flat curve means the channels are spatially independent; a peak_g well above 1 at small r indicates co-clustering, and the radius at which g(r) decays back to ~1 estimates the co-cluster length scale. Confirm the curve does not diverge as r approaches r_max — runaway tails usually mean the edge correction is off or the FOV/area is mis-set. Very small r bins are intrinsically noisy (few pairs); shrink dr only when both channels are dense.","title":"Output & interpretation"},{"category":"section","location":"steps/crosscorr/#Notes-and-caveats","page":"Cross-Correlation","text":"Register channels first. g(r) measures separation, so an uncorrected inter-channel offset shifts and smears the peak. Run Cross-Alignment upstream.\nIt does not relabel emitters. This is an analysis-only step; downstream steps see the same SMLDs. Use it alongside Composite Render to see the overlap you are quantifying.\nShared camera assumed. The normalization area comes from channel A's camera; mismatched cameras or cropped channels will bias rho_B and hence the baseline.\nAsymmetry. Pairs are counted from A to B; for well-sampled channels g_AB(r) and g_BA(r) agree, but with very different counts the finite-sampling noise differs — check both if in doubt by swapping channels.","title":"Notes & caveats"},{"category":"section","location":"steps/crosscorr/#References","page":"Cross-Correlation","text":"B. D. Ripley. \"Modelling spatial patterns.\" Journal of the Royal Statistical Society B 39, 172–212 (1977).\nS. Sengupta, T. Jovanovic-Talisman, D. Skoko, M. Renz, S. L. Veatch, J. Lippincott-Schwartz. \"Probing protein heterogeneity in the plasma membrane using PALM and pair correlation analysis.\" Nature Methods 8, 969–975 (2011). doi:10.1038/nmeth.1704\n\nThe KD-tree range queries are provided by NearestNeighbors.jl.","title":"References"},{"category":"section","location":"workflows/extending/#Extending-the-Pipeline","page":"Extending the Pipeline","text":"Because the pipeline is routed entirely by Julia's multiple dispatch, you can add a new step without modifying SMLMAnalysis at all. Define a config type and an analyze(smld, cfg) method — in your own package, in a script, or even at the REPL — and your step composes into AnalysisConfig.steps like any built-in one. There is no step registry to register with and no orchestrator to edit; the fold simply calls analyze(state, cfg) and dispatch finds your method.\n\nThis is the same mechanism the built-in steps use. The only requirements are:\n\nYour config subtypes SMLMData.AbstractSMLMConfig (so it is a valid element of the steps vector).\nYou add a method analyze(smld::BasicSMLD, cfg::YourConfig; kwargs...) that returns a (result, StepInfo) tuple.\nThe method accepts (and may ignore) the orchestrator's keyword arguments via a trailing kwargs....","title":"Extending the Pipeline"},{"category":"section","location":"workflows/extending/#Minimal-external-step","page":"Extending the Pipeline","text":"The smallest useful step is a few lines. Put this in your own module or script — nothing here lives inside SMLMAnalysis:\n\nusing SMLMAnalysis\nusing SMLMAnalysis: analyze, StepInfo, Verbosity   # extend `analyze`, build a StepInfo\nusing SMLMData\n\n# 1. A config type — parameters only, no logic.\nstruct SpatialFilterConfig <: SMLMData.AbstractSMLMConfig\n    x_range::Tuple{Float64,Float64}   # microns\n    y_range::Tuple{Float64,Float64}\nend\n\n# 2. An analyze() method. Dispatch on (BasicSMLD, SpatialFilterConfig) routes\n#    the pipeline here automatically.\nfunction SMLMAnalysis.analyze(smld::BasicSMLD, cfg::SpatialFilterConfig;\n                              step_number::Int = 0, verbose::Int = Verbosity.STANDARD,\n                              kwargs...)\n    keep = [cfg.x_range[1] <= e.x <= cfg.x_range[2] &&\n            cfg.y_range[1] <= e.y <= cfg.y_range[2] for e in smld.emitters]\n    out = BasicSMLD(smld.emitters[keep], smld.camera,\n                    smld.n_frames, smld.n_datasets, smld.metadata)\n\n    summary = Dict{Symbol,Any}(:n_before => length(smld.emitters),\n                               :n_after  => length(out.emitters))\n    (out, StepInfo(step_number, cfg, 0.0, summary))\nend\n\nThat is enough to use it both standalone and in a pipeline:\n\n# Standalone\n(cropped, info) = analyze(smld, SpatialFilterConfig((1.0, 5.0), (1.0, 5.0)))\n\n# As a pipeline step — it slots in next to the built-ins\nconfig = AnalysisConfig(camera = cam, steps = [\n    DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130)),\n    SpatialFilterConfig((1.0, 5.0), (1.0, 5.0)),   # ← your step\n    RenderConfig(zoom = 20),\n])\n(result, info) = analyze(image_stacks, config)\n\nIf you list SpatialFilterConfig before any DetectFitConfig, you get a clear MethodError (no analyze(::Vector{…}, ::SpatialFilterConfig)), not a silent wrong result — see error clarity.","title":"Minimal external step"},{"category":"section","location":"workflows/extending/#The-recommended-structure","page":"Extending the Pipeline","text":"For anything beyond a few lines, follow the same two-layer shape the built-in steps use: an internal work function that does the computation and returns (result, <YourInfo>), and a thin analyze() wrapper that times it and builds the StepInfo. This keeps the work reusable and testable independently of the pipeline.","title":"The recommended structure"},{"category":"section","location":"workflows/extending/#1.-Config-and-info-types","page":"Extending the Pipeline","text":"@kwdef struct SpatialFilterConfig <: SMLMData.AbstractSMLMConfig\n    x_range::Tuple{Float64,Float64}\n    y_range::Tuple{Float64,Float64}\nend\n\nstruct SpatialFilterInfo <: SMLMData.AbstractSMLMInfo\n    n_before::Int\n    n_after::Int\nend\n\nSubtyping AbstractSMLMInfo lets your info struct be stored on StepInfo.info and surfaced in AnalysisInfo alongside the upstream info structs.","title":"1. Config and info types"},{"category":"section","location":"workflows/extending/#2.-Internal-work-function","page":"Extending the Pipeline","text":"function spatialfilter_step(smld::BasicSMLD, cfg::SpatialFilterConfig;\n                            outdir = nothing, step_number::Int = 0,\n                            verbose::Int = Verbosity.STANDARD)\n    v = verbose\n    dir = SMLMAnalysis.step_outdir(outdir, step_number, cfg)\n    v >= Verbosity.PROGRESS && @info \"[$step_number] $(SMLMAnalysis.step_name(cfg))\"\n\n    n_before = length(smld.emitters)\n    keep = [cfg.x_range[1] <= e.x <= cfg.x_range[2] &&\n            cfg.y_range[1] <= e.y <= cfg.y_range[2] for e in smld.emitters]\n    out = BasicSMLD(smld.emitters[keep], smld.camera,\n                    smld.n_frames, smld.n_datasets, smld.metadata)\n\n    if dir !== nothing\n        mkpath(dir)\n        SMLMAnalysis._save_config!(dir, cfg)     # writes config.toml for provenance\n    end\n    v >= Verbosity.PROGRESS && @info \"  → $(length(out.emitters)) / $n_before\"\n    (out, SpatialFilterInfo(n_before, length(out.emitters)))\nend","title":"2. Internal work function"},{"category":"section","location":"workflows/extending/#3.-Summary-dispatch","page":"Extending the Pipeline","text":"_step_summary turns your info struct into the Dict shown in the pipeline's summary tables and stats.md:\n\nSMLMAnalysis._step_summary(info::SpatialFilterInfo) = Dict{Symbol,Any}(\n    :n_before   => info.n_before,\n    :n_after    => info.n_after,\n    :acceptance => round(info.n_after / max(1, info.n_before), digits = 3),\n)","title":"3. Summary dispatch"},{"category":"section","location":"workflows/extending/#4.-analyze()-wrapper","page":"Extending the Pipeline","text":"function SMLMAnalysis.analyze(smld::BasicSMLD, cfg::SpatialFilterConfig;\n                              outdir = nothing, step_number::Int = 0,\n                              verbose::Int = Verbosity.STANDARD, kwargs...)\n    t = @elapsed (out, sf_info) = spatialfilter_step(smld, cfg;\n        outdir = outdir, step_number = step_number, verbose = verbose)\n    (out, StepInfo(step_number, cfg, t, SMLMAnalysis._step_summary(sf_info); info = sf_info))\nend\n\nThe trailing kwargs... is required: the orchestrator passes context keywords (outdir, step_number, verbose, checkpoint, and occasionally others) to every step, and yours must accept the ones it does not use.","title":"4. analyze() wrapper"},{"category":"section","location":"workflows/extending/#Wrapping-an-upstream-package","page":"Extending the Pipeline","text":"A step that wraps an existing (result, info)-returning function is even thinner — the work function is the upstream call. This is exactly how the Clustering step wraps SMLMClustering.cluster, Drift Correction wraps SMLMDriftCorrection.driftcorrect, and Bayesian Grouping wraps SMLMBaGoL.run_bagol. Follow the \"upstream owns the config\" idiom: alias the upstream config (const YourConfig = ThatPkg.Config) and dispatch your analyze() method on it directly rather than re-declaring its fields.","title":"Wrapping an upstream package"},{"category":"section","location":"workflows/extending/#Contributing-a-step-upstream","page":"Extending the Pipeline","text":"If your step is generally useful and you want it in SMLMAnalysis itself, the in-repo recipe is the same five pieces, plus: put the work function in src/steps/yourstep.jl, include and export it from src/SMLMAnalysis.jl, add a docstring and an API Reference entry, and add a step page under Pipeline Steps. See CONTRIBUTING for the full checklist.","title":"Contributing a step upstream"},{"category":"section","location":"workflows/running/#Running-a-Pipeline","page":"Running a Pipeline","text":"This page is a practical guide to running an analysis: the two ways to drive the pipeline, the analyze() entry forms, how to read what comes back, and the two dials — verbosity and checkpointing — that control how much is written to disk. For the why behind the design, see The Pipeline Model; for the catalog of available steps, see Pipeline Steps.","title":"Running a Pipeline"},{"category":"section","location":"workflows/running/#Two-ways-to-drive-the-pipeline","page":"Running a Pipeline","text":"analyze() is the only verb. You run a whole pipeline at once (config-driven) or one step at a time (step-by-step); both call the same dispatch, so a step behaves identically either way.","title":"Two ways to drive the pipeline"},{"category":"section","location":"workflows/running/#Config-driven-(run-it-all-at-once)","page":"Running a Pipeline","text":"Build an AnalysisConfig holding the camera, an ordered steps vector, and output settings, then hand it to analyze():\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130),\n                        fitter = GaussMLEConfig(psf_model = GaussianXYNBS())),\n        FilterConfig(photons = (500.0, Inf)),\n        FrameConnectConfig(max_frame_gap = 5),\n        DriftConfig(degree = 2),\n        RenderConfig(zoom = 20, colormap = :inferno),\n    ],\n    outdir = \"output/\",\n)\n(result, info) = analyze(image_stacks, config)\n\nThis is the form to reach for when the recipe is settled — it captures the entire pipeline in one provenance-tracked object.","title":"Config-driven (run it all at once)"},{"category":"section","location":"workflows/running/#Step-by-step-(inspect-between-steps)","page":"Running a Pipeline","text":"Call analyze() repeatedly, threading the working smld from one call into the next. Each call returns (smld, step_info::StepInfo), so you can inspect counts, timing, and intermediate figures before deciding the next step's parameters. The first call takes the image data and a DetectFitConfig (which carries its own camera, since there is no AnalysisConfig to inject one); every later call takes the smld:\n\n(smld, info) = analyze(image_stacks,\n    DetectFitConfig(camera = cam, boxer = BoxerConfig(boxsize = 9)))\n@show length(smld.emitters)             # decide the photon cut from this\n\n(smld, info) = analyze(smld, FilterConfig(photons = (500.0, Inf)))\n(smld, info) = analyze(smld, DriftConfig(degree = 2))\n(smld, info) = analyze(smld, RenderConfig(zoom = 20))\n\nBecause detection/fitting is the expensive step, a common pattern is to run it once, save the smld (see I/O & Resume), then iterate cheaply on filter and render parameters.","title":"Step-by-step (inspect between steps)"},{"category":"section","location":"workflows/running/#analyze()-entry-forms","page":"Running a Pipeline","text":"Call Use when\nanalyze(data, config::AnalysisConfig) The primary form; data is one image stack or a Vector of stacks (one per dataset).\nanalyze(data, step1, step2, …; camera, outdir, …) Varargs convenience — builds the AnalysisConfig for you from positional step configs plus keywords.\nanalyze(config::AnalysisConfig) File-based: no data argument. The DetectFitConfig carries path=/paths= and images are loaded from disk.\n\nThe varargs form is handy for short, throwaway runs:\n\n(result, info) = analyze(image_stacks,\n    DetectFitConfig(boxer = BoxerConfig(boxsize = 9)),\n    FilterConfig(photons = (500.0, Inf)),\n    DriftConfig(degree = 2);\n    camera = cam, outdir = \"output/\")\n\nThe file-based form keeps large acquisitions off the heap — the data argument is omitted entirely:\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [DetectFitConfig(path = \"data.h5\", h5_format = :mic,\n                             boxer = BoxerConfig(boxsize = 9)),\n             FilterConfig(photons = (500.0, Inf)),\n             RenderConfig(zoom = 20)],\n    outdir = \"output/\",\n)\n(result, info) = analyze(config)","title":"analyze() entry forms"},{"category":"section","location":"workflows/running/#Reading-the-result","page":"Running a Pipeline","text":"A full pipeline run returns an AnalysisResult and an AnalysisInfo:\n\nresult.smld             # final BasicSMLD after all steps\nresult.smld_connected   # tracks from FrameConnectConfig (nothing if not run)\nresult.drift_model      # fitted drift polynomial from DriftConfig (nothing if not run)\n\ninfo.elapsed_s          # total wall time, seconds\nstepinfo(info, :driftcorrect).info  # upstream info struct, looked up by step name\ninfo.step_infos         # Vector{StepInfo}: per-step timing, config, summary\n\nThe emitters in result.smld carry positions and uncertainties in microns; see Data Model & Provenance for the full type layout and what each step contributes to the (result, info) tuple. In the step-by-step style the second tuple element of each call is a single StepInfo rather than the aggregated AnalysisInfo.","title":"Reading the result"},{"category":"section","location":"workflows/running/#Output-directory","page":"Running a Pipeline","text":"Setting outdir turns on disk output. Each step writes into its own numbered subdirectory, outdir/NN_stepname/, alongside a top-level config.toml (the full AnalysisConfig) and summary.md (the run timing/result table):\n\noutput/\n├── config.toml          # AnalysisConfig: camera, ROI, verbosity, step manifest\n├── summary.md           # per-step timing and result table\n├── log.txt              # teed @info output\n├── 01_detectfit/        # config.toml, info.toml, stats.md, figures\n├── 02_filter/\n├── 04_driftcorrect/\n└── 08_render/\n\nEach step subdir holds config.toml (that step's config), info.toml (scalar fields of the upstream info struct), stats.md, and any figures gated by the verbosity level. With outdir = nothing (the default) nothing is written and the pipeline runs in-memory only. For saving and reloading the smld itself across sessions, see I/O & Resume.","title":"Output directory"},{"category":"section","location":"workflows/running/#Verbosity-levels","page":"Running a Pipeline","text":"The verbose field (default Verbosity.STANDARD) controls how much diagnostic output each step produces. Levels are cumulative — each adds to the ones below it:\n\nLevel Constant Adds\n0 Verbosity.SILENT Errors only\n1 Verbosity.PROGRESS Step names, counts, timing\n2 Verbosity.STANDARD + stats.md and basic figures (fit quality, overlays, drift plots)\n3 Verbosity.DETAILED + diagnostic plots, per-filter breakdowns, localizations-per-frame\n4 Verbosity.DEBUG + MP4 animations, frame-by-frame, heavy visualization\n\nconfig = AnalysisConfig(camera = cam, steps = [...], verbose = Verbosity.DETAILED)\n\n# or per call, step-by-step\n(smld, info) = analyze(smld, RenderConfig(zoom = 20); verbose = Verbosity.DEBUG)","title":"Verbosity levels"},{"category":"section","location":"workflows/running/#Checkpoint-levels","page":"Running a Pipeline","text":"The checkpoint field (default Checkpoint.EXPENSIVE) controls which steps persist their output smld to disk as a JLD2 checkpoint, so you can resume downstream work without re-running upstream steps:\n\nLevel Constant Writes\n0 Checkpoint.NONE No SMLD checkpoints\n1 Checkpoint.END Only the final SMLD-producing step's output\n2 Checkpoint.EXPENSIVE Expensive steps (DetectFit, FrameConnect, Drift, BaGoL); cheap filters skipped (default)\n3 Checkpoint.ALL Every SMLD-producing step\n\nThe EXPENSIVE default guarantees that no costly step ever runs leaving only image/diagnostic output — its smld is always on disk for the next iteration. Checkpoints require outdir to be set. See I/O & Resume for loading them back.\n\nconfig = AnalysisConfig(camera = cam, steps = [...], outdir = \"output/\",\n                        checkpoint = Checkpoint.ALL)","title":"Checkpoint levels"},{"category":"section","location":"concepts/ecosystem/#The-JuliaSMLM-Ecosystem","page":"The JuliaSMLM Ecosystem","text":"SMLMAnalysis sits at the top of JuliaSMLM, a set of focused packages that each own one part of the SMLM analysis problem. This page is the map: what each package does, which step it backs, and when you might call it directly instead of through the pipeline.\n\nAll packages share the core data types from SMLMData (BasicSMLD, Emitter2DFit, cameras), so values pass between them without conversion.","title":"The JuliaSMLM Ecosystem"},{"category":"section","location":"concepts/ecosystem/#Dependency-map","page":"The JuliaSMLM Ecosystem","text":"SMLMData ............ core types (Emitter, Camera, BasicSMLD, ROIBatch) — no deps\n   │\n   ├── SMLMBoxer ............ ROI detection from raw frames\n   ├── GaussMLE ............. GPU-accelerated MLE PSF fitting\n   ├── SMLMFrameConnection .. link blinks across frames (+ uncertainty calibration)\n   ├── SMLMDriftCorrection .. fiducial-free drift correction\n   ├── SMLMRender ........... super-resolution image rendering\n   ├── SMLMBaGoL ............ Bayesian grouping of localizations (RJMCMC)\n   ├── SMLMClustering ....... DBSCAN / Hierarchical / Voronoi / Hopkins\n   ├── SMLMSim .............. simulation + image generation\n   └── MicroscopePSFs ....... PSF models (Gaussian, Airy, …)\n                              │\n                       SMLMAnalysis (integrates all of the above)","title":"Dependency map"},{"category":"section","location":"concepts/ecosystem/#What-each-package-does","page":"The JuliaSMLM Ecosystem","text":"Package Backs the step Role Primary reference\nSMLMBoxer Detection Finds candidate molecules and cuts ROIs from raw frames Huang et al. 2013\nGaussMLE Fitting Maximum-likelihood Gaussian/astigmatic PSF fitting with CRLB uncertainties (GPU) Smith et al. 2010\nSMLMFrameConnection Frame Connection Links repeated blinks of one fluorophore into single, higher-precision localizations; optional uncertainty calibration Schodt & Lidke 2021\nSMLMDriftCorrection Drift Correction Fiducial-free drift estimation by entropy minimization Cnossen et al. 2021; Wester et al. 2021\nSMLMRender Rendering Turns localizations into super-resolution images (histogram / Gaussian / circle / ellipse) —\nSMLMBaGoL Bayesian Grouping Groups localizations into true emitters via RJMCMC, beyond raw precision Fazel et al. 2022\nSMLMClustering Clustering Cluster labeling (DBSCAN/Hierarchical/Voronoi) and spatial statistics (Hopkins, Voronoi density) Ester et al. 1996; Levet et al. 2015\nSMLMSim — Simulates SMLM data and generates synthetic image stacks (used throughout the docs) —\nMicroscopePSFs — PSF models used by fitting and simulation —\n\nFull citations are on the References page; each step page repeats its own primary reference and links to the upstream package's documentation for the algorithm details.","title":"What each package does"},{"category":"section","location":"concepts/ecosystem/#Steps-SMLMAnalysis-owns","page":"The JuliaSMLM Ecosystem","text":"Not every step comes from an upstream package. SMLMAnalysis implements several of its own — the parts of a real pipeline that do not belong to any single lower-level package:\n\nQuality Filter (FilterConfig) — threshold filtering on photons, precision, p-value, PSF width, and z.\nIntensity Filter (IntensityFilterConfig) — Poisson upper-tail rejection of multi-emitter events against an estimated excitation field.\nDensity Filter (DensityFilterConfig) — removal of isolated localizations by neighbor count, with automatic threshold selection.\nMulti-channel steps — Composite Render, Cross-Alignment, and Cross-Correlation operate across colors and are built here on top of SMLMRender / SMLMDriftCorrection / NearestNeighbors.","title":"Steps SMLMAnalysis owns"},{"category":"section","location":"concepts/ecosystem/#When-to-use-a-package-directly","page":"The JuliaSMLM Ecosystem","text":"The pipeline is the right tool when you want a reproducible, multi-step analysis with provenance and on-disk diagnostics. Reach past it, straight to an upstream package, when:\n\nYou need a capability the pipeline doesn't expose — an upstream function or option SMLMAnalysis doesn't surface. Because all packages share SMLMData types, you can pull result.smld out of a pipeline and pass it to any upstream function.\nYou are developing or debugging one algorithm in isolation and want its package's full API and docs.\nYou are doing post-hoc exploration on an already-analyzed SMLD (e.g. trying several clustering parameters) and don't need it recorded as a pipeline step.\n\nSince using SMLMAnalysis re-exports the key upstream types and verbs (cluster, render, run_bagol, frameconnect, the config types, the emitter types), you can usually do both styles from the same session without extra imports.","title":"When to use a package directly"},{"category":"section","location":"concepts/data_model/#Data-Model-and-Provenance","page":"Data Model & Provenance","text":"Everything that flows between steps is built from a handful of types defined in SMLMData and shared across the whole ecosystem. Knowing them makes the (result, info) tuples and the on-disk outputs easy to read.","title":"Data Model & Provenance"},{"category":"section","location":"concepts/data_model/#Localizations:-emitters","page":"Data Model & Provenance","text":"A single fitted blinking event is an emitter. The common 2D type is Emitter2DFit, which carries a position, a photon count, a background, and the fitted uncertainties of each:\n\nField Meaning\nx, y position, in microns\nphotons, bg integrated photons and per-pixel background\nσ_x, σ_y localization precision (CRLB estimate), in microns\nσ_photons, σ_bg uncertainties of photons / background\nframe frame index within its dataset\ndataset which dataset (acquisition block) the emitter belongs to\ntrack_id track id assigned by Frame Connection (0 if unconnected)\nid cluster id assigned by Clustering (0 = noise)\n\n3D data uses Emitter3DFit (adds z, σ_z); astigmatic and σ-fitting PSF models add PSF-width fields. All positions and uncertainties are in microns throughout the pipeline.\n\nThe fitted σ_x/σ_y are not incidental — they are the currency several steps spend: Frame Connection weights links by them, the calibration adjusts them, Bayesian Grouping groups by them, and Density Filter measures neighbor distances in their units.","title":"Localizations: emitters"},{"category":"section","location":"concepts/data_model/#The-container:-BasicSMLD","page":"Data Model & Provenance","text":"A whole localization set is a SMLMData.BasicSMLD:\n\nstruct BasicSMLD\n    emitters       # Vector of Emitter2DFit / Emitter3DFit / …\n    camera         # the AbstractCamera (pixel geometry, gain, …)\n    n_frames       # frames per dataset\n    n_datasets     # number of datasets\n    metadata       # Dict{String,Any} of provenance\nend\n\nThis single value is the state threaded through the pipeline: each step takes a BasicSMLD and returns a BasicSMLD. Note that n_frames is per dataset, not the global total — see Multi-Dataset for why frame numbering is per-dataset.","title":"The container: BasicSMLD"},{"category":"section","location":"concepts/data_model/#The-camera-and-coordinate-system","page":"Data Model & Provenance","text":"Every BasicSMLD carries a camera — an AbstractCamera that defines the pixel grid in physical space. The camera is the bridge between pixel indices (what the detector records) and microns (what every downstream step works in): it stores the pixel edge positions in microns, with the origin (0, 0) at the top-left corner and pixel (1, 1) centered at (pixel_size/2, pixel_size/2). This is why every position and uncertainty in the pipeline is in microns — the camera fixes the scale once, and detection and fitting express their results in it.\n\nYou set the camera once, on the AnalysisConfig (or per channel in a multi-target run). The pipeline injects it into DetectFitConfig, carries it on the resulting SMLD, and reuses it wherever a pixel↔physical conversion is needed — detection, MLE fitting, and rendering (a render at zoom = 20 produces an image 20× the camera's pixel resolution). The camera also rides along through I/O, so a reloaded SMLD still knows its geometry.\n\nTwo camera types are re-exported from SMLMData:\n\nIdealCamera(nx, ny, pixel_size) — a uniform pixel grid with Poisson-only noise. pixel_size is in microns: IdealCamera(256, 128, 0.1) is a 256×128 sensor with 100 nm pixels. The right choice for EMCCD-like data and simulations.\nSCMOSCamera(nx, ny, pixel_size, readnoise; offset, gain, qe) — adds the per-pixel sCMOS noise model (read noise, offset, gain, quantum efficiency), each a scalar or a full per-pixel map. The MLE fitter uses these to weight each pixel correctly. build_camera_from_mic_h5 constructs one straight from a MIC calibration file.\n\nPixel size is the master scale: it fixes how pixel coordinates map to microns, and with them the meaning of every σ, every neighbor radius, and every render dimension. Set it to match your objective and sensor.","title":"The camera & coordinate system"},{"category":"section","location":"concepts/data_model/#What-analyze()-returns","page":"Data Model & Provenance","text":"","title":"What analyze() returns"},{"category":"section","location":"concepts/data_model/#AnalysisResult","page":"Data Model & Provenance","text":"The whole-pipeline result holds the final localizations plus the special state captured along the way:\n\nresult.smld             # final BasicSMLD after all steps\nresult.smld_connected   # frame-connected tracks (or nothing)\nresult.drift_model      # fitted drift trajectory (or nothing)","title":"AnalysisResult"},{"category":"section","location":"concepts/data_model/#Provenance:-StepInfo-and-AnalysisInfo","page":"Data Model & Provenance","text":"Provenance is first-class, never hidden in globals. Each step produces a StepInfo:\n\nstruct StepInfo\n    number       # position in the pipeline\n    name         # step name, e.g. \"filter\", \"driftcorrect\"\n    config       # the exact config used\n    timestamp\n    elapsed_s\n    summary      # Dict of headline stats (counts, rates, …)\n    info         # the upstream package's own typed info struct (or nothing)\nend\n\nThe whole run aggregates these into an AnalysisInfo:\n\n(result, info) = analyze(data, config)\ninfo.elapsed_s                       # total wall-clock time\ninfo.step_infos                      # Vector{StepInfo} — full ordered history\nstepinfo(info, :driftcorrect).info   # the DriftInfo from the drift step\nstepinfo(info, :detectfit).info      # the DetectFitInfo, etc.\n\nstepinfo(info, name) searches info.step_infos for the first step with that name (a Symbol or String) and returns its StepInfo; reach the upstream package's own info struct (e.g. FrameConnectInfo, DriftInfo, BaGoLInfo) via .info. When a step name repeats (e.g. several render steps), stepinfos(info, name) returns them all in pipeline order.\n\nnote: Migration from `info.steps`\nThe info.steps::Dict{Symbol,Any} field was removed (from both AnalysisInfo and MultiTargetInfo). Replace info.steps[:x] with stepinfo(info, :x).info. The old Dict returned the last occurrence of a duplicated step name and hid repeats behind suffixed keys like :compositerender_3; the accessors return the first match and expose every repeat via stepinfos(info, :compositerender). MultiTargetInfo.channels is unchanged.","title":"Provenance: StepInfo and AnalysisInfo"},{"category":"section","location":"concepts/data_model/#On-disk-outputs","page":"Data Model & Provenance","text":"When AnalysisConfig.outdir is set, each step writes a numbered subdirectory — outdir/02_filter/, outdir/05_driftcorrect/, … — containing:\n\nconfig.toml — the step's configuration (reproducibility);\ninfo.toml — the scalar fields of the upstream info struct;\nstats.md — a human-readable summary;\ndiagnostic figures (gated by verbosity);\noptionally a checkpointed SMLD (*.jld2), gated by the checkpoint level.\n\nThe pipeline also writes a top-level outdir/config.toml capturing the camera, ROI, verbosity, and the full ordered step list. For persisting and reloading analysis state across sessions, see I/O & Resume.","title":"On-disk outputs"},{"category":"section","location":"concepts/#Concepts:-Overview","page":"Overview","text":"SMLMAnalysis is an integration layer: it does little numerical work itself, and instead composes the JuliaSMLM ecosystem's specialized packages into one reproducible pipeline. These pages explain the few ideas you need to use it well and to reason about what a pipeline is doing. Read them top to bottom, or jump to a topic:\n\nThe Pipeline Model — how analyze() dispatch turns a list of step configs into an analysis, the (result, info) tuple pattern, and state threading. Start here.\nThe JuliaSMLM Ecosystem — what each upstream package does, which steps SMLMAnalysis owns itself, and when to reach for a package directly.\nData Model & Provenance — the BasicSMLD localization container and emitter types that flow between steps, the camera and micron coordinate system, and the StepInfo / AnalysisInfo records that capture what happened.\nThe Multi-Target Model — how the single-channel pipeline generalizes to multi-color experiments: a Vector{BasicSMLD} state and cross-channel steps.\n\nFor day-to-day tasks (installing, running, multi-dataset and multi-channel acquisitions, I/O, adding a step) see the Workflows section; for the per-step reference see Pipeline Steps.","title":"Concepts: Overview"},{"category":"section","location":"concepts/#The-mental-model-in-one-paragraph","page":"Overview","text":"You hand analyze() some data and a description of the analysis. The description is an AnalysisConfig: a camera, an output directory, and an ordered steps vector of typed configs. The pipeline applies each step in turn, passing the localization set from one to the next, and returns the final localizations (AnalysisResult) together with a complete provenance record (AnalysisInfo). Each step is backed by one ecosystem package (or by SMLMAnalysis itself), selected purely by the config's type.","title":"The mental model in one paragraph"},{"category":"section","location":"concepts/#Glossary","page":"Overview","text":"Localization — a single fitted blinking event: a position with a fitted uncertainty (and photons, background, …). Stored as an Emitter2DFit / Emitter3DFit. Positions are in microns throughout.\nSMLD — Single-Molecule Localization Data: the SMLMData.BasicSMLD container holding a vector of emitters plus the camera, frame count, and dataset count. This is the value that flows between steps.\nCamera — an AbstractCamera (IdealCamera or SCMOSCamera) that defines the pixel grid in microns and so the coordinate system every step works in. Set once on the AnalysisConfig; see The camera & coordinate system.\nEmitter — after Bayesian grouping the word also means a true fluorophore reconstructed from many localizations; in raw data \"emitter\" and \"localization\" coincide.\nStep — one transformation in the pipeline, named by its config type (DetectFitConfig, FilterConfig, …). Each step has an analyze(state, cfg) method.\nConfig — a typed struct describing a step's parameters (<: SMLMData.AbstractSMLMConfig). Configs carry parameters, not logic.\nInfo — the second element of every analyze() return tuple: a StepInfo per step (wrapping the upstream package's own info struct), aggregated into an AnalysisInfo for the whole run.\nDataset — an independent acquisition block within one SMLD (e.g. a cell, an ROI, or a registered segment). Frames are numbered per dataset. See Multi-Dataset.\nChannel / target — one color in a multi-color experiment. Several SMLDs are analyzed together by MultiTargetConfig. See The Multi-Target Model for the concept and Multi-Channel for the workflow.\nTuple pattern — the JuliaSMLM convention that analysis functions return (result, info) rather than mutating arguments or hiding state in globals.\nCRLB — the Cramér–Rao lower bound, the theoretical minimum localization uncertainty; the per-localization σ reported by the fitter is a CRLB estimate, and several steps use it as a weight.","title":"Glossary"},{"category":"section","location":"steps/composite_render/#Composite-Render","page":"Composite Render","text":"A composite render overlays several single-color reconstructions into one multi-color image: each channel is rendered to its own intensity map, tinted a chosen color, and the tinted maps are summed into a single RGB picture. The step is selected by a CompositeRenderConfig and is a native SMLMAnalysis step built on SMLMRender — it simply calls SMLMRender.render with a per-channel color assignment.\n\nUnlike the single-color Rendering step, this is a multi-channel step: it operates on a Vector{BasicSMLD} (one SMLD per color/target), not a single SMLD, and is dispatched accordingly.\n\nanalyze(smlds, CompositeRenderConfig(zoom = 20))   # smlds::Vector{BasicSMLD}\n#   → (smlds, StepInfo)        # SMLDs pass through unmodified; image goes to disk","title":"Composite Render"},{"category":"section","location":"steps/composite_render/#When-to-use-/-prerequisites","page":"Composite Render","text":"This is a Multi-Channel step. It needs two or more channels — one BasicSMLD per color — so it only makes sense in a multi-target workflow, not a single-color pipeline.\nIt is almost always driven by MultiTargetConfig: each channel runs its own single-color pipeline first, then composite render (and any Cross-Alignment / Cross-Correlation) runs over the resulting vector of SMLDs. Run it after the per-channel localizations are finished, and after any cross-alignment when you want the registered overlay.\nIt produces an image only; it does not modify the localizations.","title":"When to use / prerequisites"},{"category":"section","location":"steps/composite_render/#Inputs,-returns-and-artifacts","page":"Composite Render","text":"Input: a Vector{BasicSMLD}, one entry per channel, in the same order as the channel colors.\nReturns: (smlds, StepInfo). The SMLDs are returned unchanged (pass-through). The render metadata is on StepInfo.info, a CompositeRenderInfo.\nArtifacts (when outdir is set), written to outdir/NN_compositerender/:\n<strategy>_<zoom>x.png — the RGB composite image (e.g. gaussianrender_20x.png).\nconfig.toml, info.toml — the step config and the upstream RenderInfo.\nstats.md (at STANDARD verbosity) — strategy, zoom, per-channel localization counts, output size, pixel size, and timing.\nWhen driven by MultiTargetConfig the step directory lives under outdir/composite/, alongside a top-level README.md documenting the color scheme and channel labels.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/composite_render/#Concept","page":"Composite Render","text":"Each channel is rendered independently into a grayscale intensity image (using the chosen Rendering strategy), then mapped onto an RGB color and the channels are added together. The result is the familiar two- or three-color overlay where regions of co-localization appear as the blend of the constituent colors (e.g. cyan + magenta → white). The actual rendering, normalization, and color mixing are done by SMLMRender.render(smlds; colors=...); see the SMLMRender documentation for the rendering strategies and how intensities are mapped to color.\n\nTwo display choices interact with the render strategy and are resolved automatically when left at their defaults:\n\nnormalize_each — whether each channel is normalized to its own dynamic range before mixing. Auto: false for HistogramRender (saturate mode), true for the others (clip + normalize).\nclip_percentile — intensity clipping for the normalize/clip path. For HistogramRender the default 0.99 is treated as \"no clip\" (saturate).","title":"Concept"},{"category":"section","location":"steps/composite_render/#Configuration","page":"Composite Render","text":"CompositeRenderConfig mirrors the rendering knobs of SMLMRender.render. The fields that matter most:\n\nfield typical / default meaning\nstrategy GaussianRender() rendering strategy: GaussianRender, HistogramRender, CircleRender, …\nzoom 20.0 output magnification (super-resolution pixels per camera pixel)\ncolors nothing per-channel colors as Vector{Symbol}; nothing = inherit from MultiTargetConfig.colors\nclip_percentile 0.99 intensity clip for normalize mode; nothing = saturate\nnormalize_each nothing per-channel normalization; nothing = auto (false for histogram, true otherwise)\nscalebar true draw a scale bar on the composite\nscalebar_length nothing scale-bar length in µm; nothing = auto\nscalebar_position :br corner: :br, :bl, :tr, :tl\nscalebar_color :white scale-bar color\n\n# Two-color overlay, colors inherited from the MultiTargetConfig\nmt = MultiTargetConfig(\n    labels = [:IgG, :C1q],          # colors default to cyan / magenta\n    steps  = [CompositeRenderConfig(zoom = 20.0, strategy = GaussianRender())],\n    outdir = \"output/cell1/\",\n)\n(result, info) = analyze(channels, mt)\n\n# Or call the step directly on a vector of SMLDs\n(smlds, sinfo) = analyze([smld_red, smld_green],\n    CompositeRenderConfig(zoom = 20.0, colors = [:red, :green]);\n    outdir = \"output/\")\n\nIf you set colors on the config it overrides the MultiTargetConfig defaults; otherwise the channel colors flow down from the parent config.","title":"Configuration"},{"category":"section","location":"steps/composite_render/#Output-and-interpretation","page":"Composite Render","text":"StepInfo.summary reports:\n\nfield meaning\nstrategy rendering strategy used (Symbol)\nzoom magnification factor\nn_channels number of channels overlaid\noutput_size composite image size in pixels\n\nSanity checks: n_channels should equal the number of SMLDs you passed (and the number of colors); output_size should scale with zoom. Inspect the PNG — the channels should land on top of each other where the targets co-localize. If the overlay looks misregistered, run a Cross-Alignment step before the composite render.","title":"Output & interpretation"},{"category":"section","location":"steps/composite_render/#Notes-and-caveats","page":"Composite Render","text":"Pass-through. The SMLDs are returned unchanged; the only product is the image. Re-render at a different zoom/strategy freely without re-running the pipeline.\nColors must match channels. When driven by MultiTargetConfig, the number of colors must equal the number of channels (the driver enforces this).\nHistogram vs. Gaussian. HistogramRender defaults to saturate mode (no per-channel normalization, no clip); the smooth strategies normalize and clip. Override normalize_each / clip_percentile only if the auto behavior is not what you want.","title":"Notes & caveats"},{"category":"section","location":"steps/composite_render/#References","page":"Composite Render","text":"No external citation — this is standard per-channel RGB compositing. For the rendering strategies, intensity normalization, and color mapping, see the SMLMRender documentation, and the Multi-Channel workflow page for the surrounding multi-target pipeline.","title":"References"},{"category":"section","location":"workflows/multichannel/#Multi-Channel-Analysis","page":"Multi-Channel","text":"A multi-color experiment images two or more targets in separate channels — for example an IgG variant and complement C1q, each on its own dye. This page shows how to analyze every channel with its own pipeline and then run cross-channel steps (composite renders, alignment, cross-correlation) that operate on all channels together. The orchestrator is MultiTargetConfig; if you are new to the single-channel flow, read The Pipeline Model and Running a Pipeline first.","title":"Multi-Channel Analysis"},{"category":"section","location":"workflows/multichannel/#The-MultiTargetConfig","page":"Multi-Channel","text":"MultiTargetConfig is the single object that describes a multi-channel run — the channels, their colors, and the cross-channel steps to run once each channel is analyzed. It is the orchestrator you construct and hand to analyze:\n\nIts fields in practical terms:\n\nField Type Meaning\nlabels Vector{Symbol} Channel names, e.g. [:IgG, :C1q]. Must be unique and match the number of channels.\ncolors Vector{Symbol} One color per channel. Defaults to cyan/magenta for 2 channels, CMY for 3 (up to 6; pass explicit colors beyond that).\nsteps Vector{AbstractMultiTargetStep} Ordered cross-channel steps (composite renders, alignment, cross-correlation).\noutdir String Root output directory.\nverbose Int Verbosity level (default Verbosity.STANDARD).\n\nmt = MultiTargetConfig(\n    labels = [:IgG, :C1q],\n    colors = [:cyan, :magenta],\n    steps = [\n        CompositeRenderConfig(zoom = 20.0, strategy = GaussianRender()),\n        CrossAlignConfig(method = :entropy),\n        CompositeRenderConfig(zoom = 20.0, strategy = GaussianRender()),  # post-alignment\n        CrossCorrConfig(r_max = 0.5, dr = 0.005),\n    ],\n    outdir = \"output/cell1/\",\n)\n\nThe steps run in order, threading a Vector{BasicSMLD} through. Most steps pass the SMLDs through unchanged; Cross-Alignment replaces them with aligned copies, so a second composite render placed after it shows the corrected overlay.","title":"The MultiTargetConfig"},{"category":"section","location":"workflows/multichannel/#The-shape-of-a-multi-channel-run","page":"Multi-Channel","text":"A multi-channel analysis has two phases, both described by the one MultiTargetConfig above and run by a single analyze call:\n\nPer-channel pipelines. Each channel is analyzed independently with its own AnalysisConfig — exactly the pipeline you would run on a single color.\nCross-channel steps. Once every channel has produced a BasicSMLD, the ordered multi-target steps run across the resulting Vector{BasicSMLD}.","title":"The shape of a multi-channel run"},{"category":"section","location":"workflows/multichannel/#Running-it","page":"Multi-Channel","text":"Call analyze with a vector of (data, AnalysisConfig) tuples — one per channel, in the same order as labels — together with the MultiTargetConfig:\n\n(result, info) = analyze([\n    (image_stacks_647, config_647),\n    (image_stacks_568, config_568),\n], mt)\n\nEach data is whatever the single-channel analyze accepts (an image stack, a vector of stacks, or a file path). Each channel's AnalysisConfig carries its own camera, steps, ROI, and verbosity; the orchestrator reuses all of those but redirects the channel's output under outdir/<label>/, so you do not set a per-channel outdir yourself.","title":"Running it"},{"category":"section","location":"workflows/multichannel/#Working-with-the-result","page":"Multi-Channel","text":"analyze returns a (MultiTargetResult, MultiTargetInfo) tuple, following the JuliaSMLM convention (see Data Model & Provenance).\n\nMultiTargetResult fields and indexing:\n\nresult.smlds        # Vector{BasicSMLD}, one per channel (aligned if CrossAlign ran)\nresult.channels     # Dict{Symbol, AnalysisResult}\nresult.step_infos   # Vector{StepInfo} for the cross-channel steps\nresult.outdir       # root output directory\nresult[:IgG]        # AnalysisResult for one channel (indexing == result.channels[:IgG])\nresult[:IgG].smld   # that channel's final SMLD\nkeys(result)        # the channel labels, in order\n\nMultiTargetInfo carries the metadata:\n\ninfo.elapsed_s      # total wall time, seconds\ninfo.channels[:IgG] # AnalysisInfo for one channel's pipeline\ninfo.step_infos     # Vector{StepInfo} for the cross-channel steps\nstepinfo(info, :crossalign).info  # look up a cross-channel step by name (searches step_infos, not channels)","title":"Working with the result"},{"category":"section","location":"workflows/multichannel/#Cross-channel-steps","page":"Multi-Channel","text":"Each multi-target step is a <: AbstractMultiTargetStep config dispatched on the channel Vector{BasicSMLD}. They are documented on their own pages — add them to MultiTargetConfig.steps in the order you want them to run:\n\nComposite Render — overlay the channels into a single multi-color image, each tinted by its colors entry (per-step colors override the MultiTargetConfig default). Accepts the same strategies as Rendering (GaussianRender, HistogramRender, CircleRender). Pass-through.\nCross-Alignment — remove residual channel-to-channel shift (chromatic offset, registration error) by entropy or FFT cross-correlation. State-modifying: it returns aligned SMLDs, so steps after it see the corrected positions.\nCross-Correlation — quantify co-localization between a pair of channels via the pair cross-correlation g(r). Pass-through.","title":"Cross-channel steps"},{"category":"section","location":"workflows/multichannel/#Output-layout","page":"Multi-Channel","text":"The run mirrors the two-phase structure on disk: a subdirectory per channel, a composite/ directory for the cross-channel steps, and per-channel SMLD files plus a serialized config at the root.\n\noutput/cell1/\n├── IgG/                    # per-channel pipeline output (01_detectfit/, 02_filter/, ...)\n├── C1q/\n├── composite/              # cross-channel step outputs, numbered in step order\n│   ├── 01_compositerender/\n│   ├── 02_crossalign/\n│   ├── 03_compositerender/\n│   ├── 04_crosscorr/\n│   └── README.md           # color scheme, per-channel counts, step summary\n├── smld_IgG.h5             # per-channel saved SMLDs (with drift model)\n├── smld_C1q.h5\n└── multi_target_config.toml","title":"Output layout"},{"category":"type","location":"workflows/multichannel/#SMLMAnalysis.MultiTargetConfig","page":"Multi-Channel","text":"MultiTargetConfig <: AbstractSMLMConfig\n\nConfiguration for multi-target (multi-color) SMLM analysis.\n\nEach channel runs an independent analyze(data, AnalysisConfig) pipeline, then cross-channel steps (composite rendering, alignment) are executed via dispatch on AbstractMultiTargetStep configs.\n\nFields\n\nlabels::Vector{Symbol}: Channel labels (e.g., [:IgG, :C1q])\ncolors::Vector{Symbol}: Colors per channel (default: cyan/magenta for 2, CMY for 3)\nsteps::Vector{AbstractMultiTargetStep}: Ordered multi-target steps (renders, alignment, etc.)\noutdir::String: Output directory\nverbose::Int: Verbosity level\n\nExample\n\nmt = MultiTargetConfig(\n    labels = [:IgG, :C1q],\n    steps = [\n        CompositeRenderConfig(zoom=20.0, strategy=GaussianRender()),\n        CrossAlignConfig(method=:entropy),\n        CompositeRenderConfig(zoom=20.0, strategy=GaussianRender()),\n    ],\n    outdir = \"output/cell1/\",\n)\n\n\n\n\n\n","title":"SMLMAnalysis.MultiTargetConfig"},{"category":"section","location":"steps/clustering/#clustering-step","page":"Clustering","text":"A super-resolution point cloud is rarely uniform: receptors aggregate, complexes assemble, and the background scatters at random. The clustering step asks two distinct questions of that cloud — which points belong to the same structure? and is there any structure at all? — and is backed by SMLMClustering. It exposes two families of config, selected entirely by type:\n\nCluster-labeling (DBSCANConfig, HDBSCANConfig, VoronoiConfig, HierarchicalConfig) — assigns a cluster id to every localization and returns a labeled SMLD.\nSpatial statistics (HopkinsConfig, VoronoiDensityConfig) — computes a read-only summary and returns the SMLD unchanged.\n\nanalyze(smld, DBSCANConfig(eps_nm = 50.0, min_points = 5))  # → (labeled_smld, StepInfo)\nanalyze(smld, HopkinsConfig())                              # → (smld, StepInfo)","title":"Clustering"},{"category":"section","location":"steps/clustering/#When-to-use-/-prerequisites","page":"Clustering","text":"Run on a BasicSMLD of localizations, normally late in the pipeline — after detection/fitting, a quality filter, and usually frame connection and drift correction, so clusters reflect real structure rather than blinking or drift.\nUse a labeling backend when you want to segment and count structures, then render or export them colored by id. Use a statistics backend (HopkinsConfig) to check clustering tendency before committing to a labeling pass, or to attach a per-emitter density feature for your own thresholding.\nMost length parameters are given in nm for convenience even though BasicSMLD positions are in µm; each backend documents its own units upstream.","title":"When to use / prerequisites"},{"category":"section","location":"steps/clustering/#Inputs,-returns-and-artifacts","page":"Clustering","text":"Input: the current smld.\nReturns: (smld_out, StepInfo).\nLabeling configs: smld_out is a deep-copied, labeled SMLD — the input is not mutated. Each emitter.id carries the cluster id: 0 = noise / unclustered, 1..K = clusters. StepInfo.info is a ClusterInfo.\nStatistics configs: smld_out is the same SMLD, untouched. The result scalar and any per-emitter/per-dataset vectors live on StepInfo.info::ClusterStatisticsInfo (info.statistic, info.extras).\nArtifacts (when outdir is set, written into {NN}_{backend}/ — e.g. 03_dbscan/, 04_hopkins/): at STANDARD verbosity, config.toml and info.toml. Labeling backends additionally checkpoint the labeled SMLD to smld_clustered.jld2, but only at Checkpoint.ALL (clustering is not treated as an \"expensive\" step, so this is not written by the default Checkpoint.EXPENSIVE).\n\nThe step directory name is the backend name itself (dbscan, hdbscan, voronoi, hierarchical, hopkins, voronoidensity), derived from the config type — so a pipeline that clusters then renders the labels reads cleanly on disk.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/clustering/#Concept","page":"Clustering","text":"The two analyze() methods are thin wrappers over SMLMClustering's two verbs. Labeling dispatches to SMLMClustering.cluster, which discovers K clusters and writes an integer label onto each emitter.id; because the label lives on id, a subsequent render (or any downstream step) sees the clustered SMLD with no extra plumbing. Statistics dispatches to SMLMClustering.cluster_statistics, which is read-only — HopkinsConfig returns a clustering-tendency scalar H, Voronoi density returns per-emitter densities.\n\nThe backends span density methods (DBSCAN, HDBSCAN), tessellation density (Voronoi / SR-Tesseler), and agglomerative linkage (Hierarchical). The algorithm math, the full backend catalog, and per-backend tuning live in the SMLMClustering documentation — this page is the pipeline-integration view, not a re-documentation of it.","title":"Concept"},{"category":"section","location":"steps/clustering/#Configuration","page":"Clustering","text":"All cluster configs are owned by SMLMClustering (the \"upstream owns the config\" idiom); SMLMAnalysis re-exports the common ones as const aliases. Dispatch is on the abstract types AbstractClusterConfig / AbstractStatisticsConfig, so any SMLMClustering backend of those families composes as a step — including ones SMLMAnalysis does not alias (reach them as SMLMClustering.XConfig).\n\nBackend selector and the field you tune first:\n\nConfig Family Key field(s)\nDBSCANConfig density ε-neighborhood eps_nm (neighborhood radius), min_points\nHDBSCANConfig hierarchical density min_points (core-distance k), min_cluster_size\nVoronoiConfig tessellation density (SR-Tesseler) density_factor, min_points\nHierarchicalConfig agglomerative linkage cut_threshold or n_clusters, linkage\nHopkinsConfig statistic (tendency H) n_samples, random_repeats, seed\nVoronoiDensityConfig statistic (per-emitter density) — (uses use_3d / per_dataset only)\n\nFields shared by the labeling backends: min_points (minimum cluster size; for HDBSCAN this is instead the core-distance neighbor count), per_dataset = true (cluster each dataset independently — ids are then local to a dataset), use_3d (2D vs 3D; Voronoi is 2D only), and remove_unclustered = false (when true, noise points are dropped from the returned SMLD rather than labeled 0).\n\n# Density-based clustering, then render colored by cluster id\n(labeled, info) = analyze(smld, DBSCANConfig(eps_nm = 50.0, min_points = 5))\ninfo.info.n_clusters                       # number of clusters found\n(labeled, _) = analyze(labeled, RenderConfig(zoom = 20))  # pass-through; labels flow through emitter.id\n(img, _) = render(labeled, RenderConfig(zoom = 20))       # image array, colored by cluster id\n\n# Read-only: is the data clustered at all?\n(_, h) = analyze(smld, HopkinsConfig())\nh.info.statistic                           # Hopkins H (≈0.5 random, →1 clustered)\n\nSee the SMLMClustering documentation for the complete field list, defaults, and the additional backends.","title":"Configuration"},{"category":"section","location":"steps/clustering/#Output-and-interpretation","page":"Clustering","text":"StepInfo.summary reports the headline numbers. For labeling backends (ClusterInfo):\n\nfield meaning\nalgorithm backend used (:dbscan, :voronoi, …)\nn_locs_in localizations entering the step\nn_clustered localizations assigned to a cluster (id ≥ 1)\nn_noise localizations left as noise (id = 0)\nn_clusters number of clusters K discovered\n\nFor statistics backends (ClusterStatisticsInfo): algorithm, n_locs_in, statistic_name, and the scalar statistic.\n\nSanity checks: n_clustered + n_noise should equal n_locs_in (unless remove_unclustered = true). A Hopkins H near 0.5 means the cloud is consistent with spatial randomness — no clusters to find — while H approaching 1 indicates genuine aggregation. If DBSCAN returns one giant cluster, eps_nm is too large; if everything is noise, it is too small (or min_points too high).","title":"Output & interpretation"},{"category":"section","location":"steps/clustering/#Notes-and-caveats","page":"Clustering","text":"Labels are not stable across runs. 1..K are discovered per run and only meaningful within it; with per_dataset = true the pair (dataset, id) uniquely identifies a cluster. A later labeling step reusing emitter.id overwrites an earlier one.\nAll clustering backends are re-exported. DBSCANConfig, HDBSCANConfig, HierarchicalConfig, and VoronoiConfig each have a const alias in SMLMAnalysis, so they are available unqualified after using SMLMAnalysis. A backend without an alias would need to be constructed qualified as SMLMClustering.<Name>(...).\nStatistics steps change nothing downstream. They are observational; place them anywhere without affecting the threaded SMLD.","title":"Notes & caveats"},{"category":"section","location":"steps/clustering/#References","page":"Clustering","text":"M. Ester, H.-P. Kriegel, J. Sander, X. Xu. \"A Density-Based Algorithm for Discovering Clusters in Large Spatial Databases with Noise.\" Proc. 2nd Int. Conf. on Knowledge Discovery and Data Mining (KDD-96), 226–231 (1996).\nF. Levet, E. Hosy, A. Kechkar, C. Butler, A. Beghin, D. Choquet, J.-B. Sibarita. \"SR-Tesseler: a method to segment and quantify localization-based super-resolution microscopy data.\" Nature Methods 12, 1065–1071 (2015). doi:10.1038/nmeth.3579\n\nSee the SMLMClustering documentation for the algorithms, the full backend catalog, and all configuration options.","title":"References"},{"category":"section","location":"api/#API-Reference","page":"Overview","text":"SMLMAnalysis re-exports a large API from across the JuliaSMLM ecosystem. To keep each page readable, the reference is split into four parts:\n\nOverview (this page) — the module, the analyze verb, and the core pipeline types.\nStep Configs & Info — per-step configuration and info structs.\nMulti-Target & I/O — multi-channel types, file import/export, and utilities.\nInternals — non-exported helpers.","title":"API Reference"},{"category":"section","location":"api/#Core-Functions","page":"Overview","text":"","title":"Core Functions"},{"category":"section","location":"api/#Types","page":"Overview","text":"","title":"Types"},{"category":"section","location":"api/#Verbosity-Levels","page":"Overview","text":"Verbosity is a module with integer constants controlling output detail:\n\nLevel Constant Output\n0 Verbosity.SILENT Errors only\n1 Verbosity.PROGRESS Step names, counts, timing\n2 Verbosity.STANDARD + stats.md, basic figures\n3 Verbosity.DETAILED + diagnostic plots, per-filter breakdowns\n4 Verbosity.DEBUG + MP4 animations, frame-by-frame analysis","title":"Verbosity Levels"},{"category":"module","location":"api/#SMLMAnalysis.SMLMAnalysis","page":"Overview","text":"SMLMAnalysis\n\nHigh-level integration package for the JuliaSMLM ecosystem.\n\nProvides a unified analyze() API for SMLM analysis. The config type determines the operation via multiple dispatch:\n\nQuick Start\n\nusing SMLMAnalysis\n\n# Full pipeline with AnalysisConfig\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(\n            boxer=BoxerConfig(boxsize=9, psf_sigma=0.130),\n            fitter=GaussMLEConfig(psf_model=GaussianXYNBS(), iterations=20)),\n        FilterConfig(photons=(500.0, Inf)),\n        FrameConnectConfig(max_frame_gap=5),\n        DriftConfig(degree=2, dataset_mode=:registered),\n        RenderConfig(zoom=20, colormap=:inferno),\n    ],\n    outdir = \"output/\",\n)\n(result, info) = analyze(image_stacks, config)\n\n# Individual steps via analyze() dispatch\n(smld, info) = analyze(image_stacks, DetectFitConfig(\n    camera=cam, boxer=BoxerConfig(boxsize=9, psf_sigma=0.130)))\n(smld, info) = analyze(smld, FilterConfig(photons=(500.0, Inf)))\n(smld, info) = analyze(smld, FrameConnectConfig(max_frame_gap=5))\n(smld, info) = analyze(smld, DriftConfig(degree=2))\n# RenderConfig is a pass-through step: it returns (smld, StepInfo) and writes the\n# image to outdir. For the image in-memory, call SMLMRender.render(smld, cfg) directly.\n(smld, info) = analyze(smld, RenderConfig(zoom=20, colormap=:inferno))\n\nRe-exported Types\n\nKey types from ecosystem packages are re-exported for convenience:\n\nSMLMData: AbstractCamera, IdealCamera, SCMOSCamera, BasicSMLD, Emitter types\nGaussMLE: GaussMLEConfig, PSF models, ROIBatch\nSMLMFrameConnection: FrameConnectConfig\nSMLMDriftCorrection: DriftConfig\nSMLMRender: render strategies\n\n\n\n\n\n","title":"SMLMAnalysis.SMLMAnalysis"},{"category":"function","location":"api/#SMLMAnalysis.analyze","page":"Overview","text":"analyze(data, cfg::DetectFitConfig; kwargs...) -> (smld, StepInfo)\n\nRun combined detection and fitting. Camera must be set in cfg.camera.\n\n\n\n\n\nFile-based dispatch for pipeline use: analyze(nothing, DetectFitConfig(path=...)). Routes to file-based analyze(cfg::DetectFitConfig) when no data is provided.\n\n\n\n\n\nanalyze(cfg::DetectFitConfig; kwargs...) -> (smld, StepInfo)\n\nFile-based detection and fitting. Requires cfg.path or cfg.paths and cfg.camera.\n\n\n\n\n\nanalyze(smld, cfg::FilterConfig; kwargs...) -> (filtered_smld, StepInfo)\n\nFilter localizations by quality criteria.\n\n\n\n\n\nanalyze(smld, cfg::SMLMFrameConnection.FrameConnectConfig; kwargs...) -> (combined_smld, StepInfo)\n\nRun frame connection on localizations.\n\n\n\n\n\nanalyze(smld, cfg::SMLMDriftCorrection.DriftConfig; kwargs...) -> (corrected_smld, StepInfo)\n\nRun drift correction on localizations.\n\n\n\n\n\nanalyze(smld, cfg::DensityFilterConfig; kwargs...) -> (filtered_smld, StepInfo)\n\nFilter localizations by neighbor density.\n\n\n\n\n\nanalyze(smld, cfg::IntensityFilterConfig; kwargs...) -> (filtered_smld, StepInfo)\n\nFilter localizations by intensity-based multi-emitter rejection.\n\n\n\n\n\nanalyze(smld, cfg::RenderConfig; kwargs...) -> (smld, StepInfo)\n\nRender localizations to a super-resolution image. The image is saved to disk (via renderstep); the smld passes through so subsequent pipeline steps can operate on it. Use `rendersteporSMLMRender.render` directly to get the image.\n\n\n\n\n\nanalyze(smlds::Vector{BasicSMLD}, cfg::CompositeRenderConfig; kwargs...) -> (smlds, StepInfo)\n\nMulti-target dispatch: composite render. SMLDs pass through.\n\n\n\n\n\nanalyze(smlds::Vector{BasicSMLD}, cfg::CrossAlignConfig; kwargs...) -> (aligned_smlds, StepInfo)\n\nMulti-target dispatch: cross-channel alignment. Modifies SMLDs.\n\n\n\n\n\nanalyze(smlds::Vector{BasicSMLD}, cfg::CrossCorrConfig; kwargs...) -> (smlds, StepInfo)\n\nMulti-target dispatch: pair cross-correlation. SMLDs pass through.\n\n\n\n\n\nanalyze(smld, cfg::BaGoLConfig; kwargs...) -> (bagol_smld, StepInfo)\n\nGroup localizations into emitters via Bayesian inference (BaGoL).\n\n\n\n\n\nanalyze(smld, cfg::AbstractClusterConfig; kwargs...) -> (smld_out, StepInfo)\n\nLabel each localization with a cluster id via SMLMClustering. The backend is selected by the concrete type of cfg (DBSCANConfig, HDBSCANConfig, VoronoiConfig, HierarchicalConfig). The input SMLD is not modified — a deep-copied, labeled SMLD is returned and threaded onward (emitter.id: 0 = noise, 1..K = cluster). See the SMLMClustering documentation for the algorithm details and per-backend configuration.\n\n\n\n\n\nanalyze(smld, cfg::AbstractStatisticsConfig; kwargs...) -> (smld, StepInfo)\n\nCompute a read-only spatial statistic via SMLMClustering — e.g. Hopkins clustering tendency (HopkinsConfig) or Voronoi density (VoronoiDensityConfig). The SMLD is returned unchanged; the result scalar and any per-emitter/per-dataset vectors live in the step's ClusterStatisticsInfo (info.statistic, info.extras).\n\n\n\n\n\nanalyze(smld, cfg::AbstractEdgeClassifyConfig; kwargs...) -> (smld_out, StepInfo)\n\nClassify each localization as :interior / :membrane / :outside against the cell mask(s) carved by SMLMClustering. The method is selected by the concrete type of cfg (KdeValleyConfig — the dSTORM-tuned default — or OuterPolygonConfig). The emitters are not modified and are shared into the returned SMLD; only the cell-mask geometry is mirrored into metadata (edge_cells, edge_outer_polygon) for downstream steps. The authoritative per-emitter class and the full geometry live in the step's EdgeClassifyInfo (info.class / interior_mask(info)).\n\n\n\n\n\nanalyze(data, config::AnalysisConfig) -> (AnalysisResult, AnalysisInfo)\n\nRun SMLM analysis pipeline defined by config.\n\nReturns a tuple of (AnalysisResult, AnalysisInfo) following the JuliaSMLM tuple-pattern.\n\nArguments\n\ndata: Image data - one of:\nVector{AbstractArray{<:Real,3}}: Multiple datasets (primary path)\nAbstractArray{<:Real,3}: Single dataset\nconfig: AnalysisConfig with camera, steps, and output settings\n\nExample\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(boxer=BoxerConfig(boxsize=9)),\n        FilterConfig(photons=(500.0, Inf)),\n        DriftConfig(degree=2),\n        RenderConfig(zoom=20, colormap=:inferno),\n    ],\n    outdir = \"output/\",\n)\n(result, info) = analyze(image_stacks, config)\nresult.smld                          # Final SMLD\nstepinfo(info, :detectfit).info      # DetectFit step info\nstepinfo(info, :driftcorrect).info   # Drift step info\n\n\n\n\n\nanalyze(data, steps::AbstractSMLMConfig...; camera, kwargs...) -> (AnalysisResult, AnalysisInfo)\n\nConvenience varargs form. Builds AnalysisConfig from positional step configs and keyword arguments.\n\nExample\n\n(result, info) = analyze(image_stacks,\n    DetectFitConfig(boxer=BoxerConfig(boxsize=9)),\n    FilterConfig(photons=(500.0, Inf)),\n    DriftConfig(degree=2);\n    camera=cam, outdir=\"output/\")\n\n\n\n\n\nanalyze(config::AnalysisConfig) -> (AnalysisResult, AnalysisInfo)\n\nRun analysis from file paths specified in DetectFitConfig. No image data argument needed - data is loaded from files.\n\nExample\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(path=\"data.h5\"),\n        FilterConfig(photons=(500.0, Inf)),\n    ],\n    outdir = \"output/\",\n)\n(result, info) = analyze(config)\n\n\n\n\n\nanalyze(channels::Vector{<:Tuple}, config::MultiTargetConfig) -> (MultiTargetResult, MultiTargetInfo)\n\nRun independent analysis pipelines for each channel, then execute multi-target steps (composite rendering, cross-channel alignment, etc.) via dispatch.\n\nEach element of channels is a (data, AnalysisConfig) tuple where data is an image stack (or Vector of stacks) or file path. The config.labels must match the number of channels.\n\nArguments\n\nchannels: Vector of (data, AnalysisConfig) tuples, one per target/color\nconfig: MultiTargetConfig with labels, colors, steps, and output settings\n\nReturns\n\n(MultiTargetResult, MultiTargetInfo) tuple following the JuliaSMLM convention.\n\nExample\n\nmt = MultiTargetConfig(\n    labels = [:IgG, :C1q],\n    steps = [\n        CompositeRenderConfig(zoom=20.0, strategy=GaussianRender()),\n        CrossAlignConfig(method=:entropy),\n        CompositeRenderConfig(zoom=20.0, strategy=GaussianRender()),\n    ],\n    outdir = \"output/cell1/\",\n)\n\n(result, info) = analyze([\n    (image_stacks_647, config_647),\n    (image_stacks_568, config_568),\n], mt)\n\nresult.smlds              # Vector{BasicSMLD}\nresult[:IgG].smld         # Per-channel access\ninfo.channels[:IgG]       # Per-channel AnalysisInfo\n\n\n\n\n\n","title":"SMLMAnalysis.analyze"},{"category":"function","location":"api/#SMLMAnalysis.stepinfo","page":"Overview","text":"stepinfo(info, name) -> StepInfo\n\nThe FIRST pipeline step whose name matches name (a Symbol or String, e.g. :frameconnect), searching info.step_infos. Throws KeyError if none matches. Access the upstream typed info via .info, timing via .elapsed_s, config via .config.\n\nFor MultiTargetInfo this searches only the cross-channel step_infos, NOT the per-channel histories in info.channels. When a step name repeats (e.g. several render steps) this returns the first; use stepinfos to get them all. (This differs from the removed steps Dict, which returned the LAST occurrence.)\n\n\n\n\n\n","title":"SMLMAnalysis.stepinfo"},{"category":"function","location":"api/#SMLMAnalysis.stepinfos","page":"Overview","text":"stepinfos(info, name) -> Vector{StepInfo}\n\nAll steps whose name matches name (a Symbol or String), in pipeline order (empty if none). Use when a step type repeats, e.g. stepinfos(info, :compositerender) to recover the several composite renders that the removed steps Dict hid behind suffixed keys. Same MultiTargetInfo caveat as stepinfo: it searches step_infos, not channels.\n\n\n\n\n\n","title":"SMLMAnalysis.stepinfos"},{"category":"type","location":"api/#SMLMAnalysis.AnalysisConfig","page":"Overview","text":"AnalysisConfig <: AbstractSMLMConfig\n\nComplete description of an SMLM analysis pipeline.\n\nThe steps vector contains upstream package configs (BoxerConfig, GaussMLEConfig, etc.) and SMLMAnalysis-specific configs (FilterConfig, DensityFilterConfig). The pipeline executes steps in order.\n\nFields\n\ncamera::AbstractCamera: Camera model (required, no default)\nsteps::Vector{SMLMData.AbstractSMLMConfig}: Ordered pipeline steps\nroi::Union{NamedTuple, Nothing}: Optional ROI as (x=100:300, y=50:200) to crop images/camera\noutdir::Union{String, Nothing}: Output directory for results\nverbose::Int: Verbosity level (default: STANDARD)\n\nExample\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(\n            boxer=BoxerConfig(boxsize=9, psf_sigma=0.130),\n            fitter=GaussMLEConfig(psf_model=GaussianXYNBS(), iterations=20)),\n        FilterConfig(photons=(500.0, Inf)),\n        DriftConfig(degree=2, dataset_mode=:registered),\n        RenderConfig(zoom=20, colormap=:inferno),\n    ],\n    outdir = \"output/\",\n)\n(result, info) = analyze(image_stacks, config)\n\n\n\n\n\n","title":"SMLMAnalysis.AnalysisConfig"},{"category":"type","location":"api/#SMLMAnalysis.AnalysisResult","page":"Overview","text":"AnalysisResult\n\nImmutable result from analyze(). Replaces the old mutable Analysis struct.\n\nFields\n\nsmld::BasicSMLD: Final SMLD after all steps\nsmld_connected::Union{BasicSMLD, Nothing}: Connected SMLD (if frameconnect was run)\ndrift_model::Any: Drift model (if driftcorrect was run)\n\nAccess\n\n(result, info) = analyze(image_stacks, config)\nresult.smld               # Final SMLD\nresult.drift_model        # Drift model for plotting\nresult.smld_connected     # Connected SMLD for track analysis\nstepinfo(info, :driftcorrect).info # Step info from upstream packages\n\n\n\n\n\n","title":"SMLMAnalysis.AnalysisResult"},{"category":"type","location":"api/#SMLMAnalysis.AnalysisInfo","page":"Overview","text":"AnalysisInfo <: AbstractSMLMInfo\n\nAggregated metadata from all analysis steps, following the tuple-pattern.\n\nFields\n\nelapsed_s::Float64: Total elapsed time in seconds\nstep_infos::Vector{StepInfo}: Full step history with timing and config\n\nLook up a step by name with the stepinfo / stepinfos accessors.\n\n\n\n\n\n","title":"SMLMAnalysis.AnalysisInfo"},{"category":"type","location":"api/#SMLMAnalysis.StepInfo","page":"Overview","text":"StepInfo <: AbstractSMLMInfo\n\nRecord of a completed pipeline step, stored in AnalysisInfo.\n\nFields\n\nnumber::Int: Step number in the pipeline\nname::String: Step name (derived from config type, e.g. \"filter\")\nconfig::AbstractSMLMConfig: The config used for this step\ntimestamp::DateTime: When the step completed\nelapsed_s::Float64: Elapsed time in seconds\nsummary::Dict{Symbol, Any}: Summary statistics (counts, acceptance rates, etc.)\ninfo::Union{AbstractSMLMInfo, Nothing}: Typed upstream info struct (DriftInfo, FrameConnectInfo, etc.)\n\n\n\n\n\n","title":"SMLMAnalysis.StepInfo"},{"category":"type","location":"api/#SMLMAnalysis.DataSource","page":"Overview","text":"DataSource\n\nLazy loading wrapper for SMLM image data. Supports:\n\nSingle 3D array (one dataset)\nVector of 3D arrays (multiple datasets, boundaries encoded in data structure)\nFile path for deferred loading\n\nConstructors\n\nDataSource(images)                          # From single 3D array (1 dataset)\nDataSource(image_stacks)                    # From Vector{Array} (N datasets)\nDataSource(path; frame_range=nothing)       # From file path (lazy)\n\n\n\n\n\n","title":"SMLMAnalysis.DataSource"},{"category":"module","location":"api/#SMLMAnalysis.Checkpoint","page":"Overview","text":"Checkpoint\n\nCheckpoint level controls per-step SMLD persistence to disk for downstream iteration without re-running upstream pipeline steps. Mirrors the Verbosity pattern: a single integer level passed via checkpoint= kwarg or AnalysisConfig.checkpoint. Each step's analyze() dispatch decides what the level means for itself by inlining if checkpoint >= Checkpoint.X checks (symmetric with how verbose >= Verbosity.STANDARD works).\n\nLevels\n\nNONE = 0: no SMLD checkpoints written\nEND = 1: orchestrator writes only the final SMLD-producing step's output\nEXPENSIVE = 2 (default): expensive steps (DetectFit, FrameConnect, Drift, BaGoL) write their output SMLD; cheap filters do not\nALL = 3: every SMLD-producing step writes its output\n\nDefault\n\nThe default is EXPENSIVE. This guarantees that no expensive step ever runs producing only image/diagnostic outputs — the SMLD is always on disk for downstream iteration.\n\n\n\n\n\n","title":"SMLMAnalysis.Checkpoint"},{"category":"section","location":"concepts/pipeline/#The-Pipeline-Model","page":"The Pipeline Model","text":"SMLMAnalysis has one organizing idea: an analysis is an ordered list of typed step configs, and Julia's method dispatch routes each one to its implementation. Understanding that idea explains the whole package — why steps compose freely, why analyze() is the only verb you call, and why adding a step requires no change to the orchestrator.\n\n(Image: The analyze() pipeline: a single call threads the data through an ordered list of typed step configs, each returning (smld, info); optional steps drop in by adding their config.)","title":"The Pipeline Model"},{"category":"section","location":"concepts/pipeline/#A-pipeline-is-a-vector-of-step-configs","page":"The Pipeline Model","text":"A localization pipeline is a sequence of transformations on a localization set. In SMLMAnalysis each transformation is a step, and each step is described by a typed configuration struct that subtypes SMLMData.AbstractSMLMConfig:\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130),\n                        fitter = GaussMLEConfig(psf_model = GaussianXYNBS())),\n        FilterConfig(photons = (500.0, Inf)),\n        FrameConnectConfig(max_frame_gap = 5),\n        DriftConfig(degree = 2),\n        RenderConfig(zoom = 20),\n    ],\n    outdir = \"output/\",\n)\n\nAnalysisConfig.steps is just a Vector{AbstractSMLMConfig}. The config carries what you want done and the parameters for each step; it carries no logic.","title":"A pipeline is a vector of step configs"},{"category":"section","location":"concepts/pipeline/#Dispatch-is-the-router","page":"The Pipeline Model","text":"The single verb is analyze. Calling analyze(state, cfg) dispatches on the types of both arguments — the current data state and the step cfg — to the right method. There is no step registry, no string lookup, and no if/elseif chain in the pipeline loop. The orchestrator is a plain fold:\n\nstate = image_stacks\nfor (i, cfg) in enumerate(steps)\n    cfg = _prepare_step(cfg, camera)         # e.g. inject the camera into DetectFitConfig\n    (state, step_info) = analyze(state, cfg; outdir, step_number = i, verbose)\n    push!(step_infos, step_info)\nend\n\nThree consequences follow directly:\n\nComposability. The loop does not care what the steps are — only that analyze() has a method for (typeof(state), typeof(cfg)). So steps can be reordered, repeated, or omitted freely.\nExtensibility. Adding a step needs no change to the orchestrator. Define a config type and an analyze() method and the step works — even from another package or your own script. See Extending the Pipeline.\nError clarity. A wrong order is a MethodError, not a silent wrong answer. FilterConfig before any DetectFitConfig gives no method matching analyze(::Vector{…}, ::FilterConfig) — because filtering expects a BasicSMLD, and only detection produces one from raw images.","title":"Dispatch is the router"},{"category":"section","location":"concepts/pipeline/#The-tuple-pattern:-(result,-info)","page":"The Pipeline Model","text":"Every analyze() call returns a 2-tuple following the JuliaSMLM convention:\n\n(result, info) = analyze(data, config)   # whole pipeline\n(smld,   info) = analyze(smld, cfg)       # one step\n\nThe result becomes the input state for the next step. For most steps it is a BasicSMLD; for the whole pipeline it is an AnalysisResult.\nThe info is the provenance: per-step it is a StepInfo wrapping the upstream package's own info struct; for the whole pipeline it is an AnalysisInfo aggregating them all. Nothing is hidden in globals — if a step computed it, it is on the returned info.\n\nThis is why you can run a pipeline in one call or step by step and get exactly the same per-step information either way.","title":"The tuple pattern: (result, info)"},{"category":"section","location":"concepts/pipeline/#State-threading","page":"The Pipeline Model","text":"The orchestrator threads one working smld through the steps: each step receives the current localizations and returns the updated set. Two pieces of state are captured from specific steps and surfaced on the final result:\n\nresult.smld_connected — the frame-connected tracks, from FrameConnectConfig.\nresult.drift_model — the fitted drift trajectory, from DriftConfig.\n\nThe first step is special only in that it manufactures the initial smld:\n\nDetectFitConfig (required first)\n        │ produces the initial smld from raw images\n        ▼\n  ┌────────────────────────────────────┐\n  │ FilterConfig          (0+ times)   │\n  │ IntensityFilterConfig (0+ times)   │   any order,\n  │ FrameConnectConfig    (0–1)        │   any combination —\n  │ DriftConfig           (0–1)        │   dispatch doesn't\n  │ DensityFilterConfig   (0+ times)   │   care, as long as\n  │ BaGoLConfig           (0–1)        │   each step's input\n  │ cluster configs       (0+ times)   │   type is satisfied\n  │ RenderConfig          (0+ times)   │\n  └────────────────────────────────────┘\n\nThe only hard constraint is that a DetectFitConfig comes first, because it is the only step that turns raw image stacks into a BasicSMLD; every other step consumes and returns an existing smld.","title":"State threading"},{"category":"section","location":"concepts/pipeline/#Repeatable-and-optional-steps","page":"The Pipeline Model","text":"Because dispatch — not a registry — drives the loop, multiplicity is free:\n\nFilterConfig, RenderConfig, DensityFilterConfig, IntensityFilterConfig, and the cluster configs can appear any number of times (a coarse filter early and a tighter one after connection; several renders at different zooms; cluster then render the labels).\nFrameConnectConfig and DriftConfig are typically used once, but this is convention, not an enforced rule.\n\nUncertainty calibration is not a separate step — it is configured inside FrameConnectConfig via its calibration = field, runs as part of frame connection, and its results appear on FrameConnectInfo.calibration.","title":"Repeatable and optional steps"},{"category":"section","location":"concepts/pipeline/#Two-layers-per-step","page":"The Pipeline Model","text":"Each step is implemented in two layers, which is worth knowing when reading the source or adding your own:\n\nAn internal work function (not exported) that does the computation and returns (result, <StepInfo>) — either implemented in SMLMAnalysis (e.g. filter_step) or delegated to an upstream package (e.g. SMLMDriftCorrection.driftcorrect).\nA thin analyze() method that times the call, wraps the result in a StepInfo, and returns the tuple. Its signature ends in kwargs... so the orchestrator can pass context keywords a step may ignore.","title":"Two layers per step"},{"category":"section","location":"concepts/pipeline/#Where-configs-come-from","page":"The Pipeline Model","text":"Some step configs are defined in SMLMAnalysis; others are re-exported from the upstream package that owns them (the \"upstream owns the config\" idiom — a const alias, dispatched on directly).\n\nConfig Defined in Notes\nDetectFitConfig SMLMAnalysis wraps SMLMBoxer + GaussMLE\nFilterConfig SMLMAnalysis native quality filtering\nIntensityFilterConfig SMLMAnalysis native Poisson multi-emitter rejection\nDensityFilterConfig SMLMAnalysis native neighbor filtering\nFrameConnectConfig / CalibrationConfig SMLMFrameConnection re-exported (CalibrationConfig is a sub-config, not a step)\nDriftConfig SMLMDriftCorrection re-exported\nRenderConfig SMLMRender re-exported\nBaGoLConfig SMLMBaGoL re-exported; step wraps run_bagol\nDBSCANConfig, VoronoiConfig, HopkinsConfig, … SMLMClustering re-exported; steps wrap cluster / cluster_statistics\n\nSee The JuliaSMLM Ecosystem for what each upstream package does, and Data Model & Provenance for the types that flow between steps.","title":"Where configs come from"},{"category":"section","location":"steps/frameconnect/#Frame-Connection","page":"Frame Connection","text":"A single fluorophore typically blinks on for several consecutive frames before going dark, so one emitter generates a short burst of repeated localizations. Frame connection recognizes those repeats and links them into a single, higher-precision localization — the average of a track is more precise than any one blink. It is selected by a FrameConnectConfig and backed by SMLMFrameConnection, which solves a spatiotemporal Linear Assignment Problem (LAP) over proximity and estimated blinking kinetics.\n\nanalyze(smld, FrameConnectConfig(max_frame_gap = 5))   # → (combined_smld, StepInfo)","title":"Frame Connection"},{"category":"section","location":"steps/frameconnect/#When-to-use-/-prerequisites","page":"Frame Connection","text":"Run on a BasicSMLD of Emitter2DFit localizations, after detection/fitting and usually after a quality filter. It is normally run before drift correction.\nInputs must carry positive position uncertainties (σ_x, σ_y > 0) — these weight the LAP cost and the MLE combination. Frames are 1-based.\nMost useful when the same dye fluoresces over multiple frames (dSTORM, DNA-PAINT). It also lets you drop single-frame events as noise via track_length.","title":"When to use / prerequisites"},{"category":"section","location":"steps/frameconnect/#Inputs,-returns-and-artifacts","page":"Frame Connection","text":"Input: the current smld.\nReturns: (combined_smld, StepInfo). combined_smld holds the recombined, higher-precision localizations (one per track). The full upstream FrameConnectInfo is on StepInfo.info; its .connected field is the uncombined input with a track_id assigned to each localization, which the pipeline also surfaces on AnalysisResult.smld_connected for track-level analysis.\nArtifacts (when outdir is set): the config and FrameConnectInfo are written; at STANDARD verbosity track_histogram.png (localizations-per-track distribution with mean/median and an estimated k_off) and stats.md are added. The combined SMLD is checkpointed to smld_combined.jld2 at Checkpoint.EXPENSIVE. When calibration is enabled, uncertainty_calibration.png, shift_histogram.png, and drift_jitter.png are also written.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/frameconnect/#Concept","page":"Frame Connection","text":"After preclustering spatially/temporally adjacent detections, the algorithm estimates the fluorophore's on/off/bleach rates and emitter density from the data itself, then assigns localizations to emitters by solving a LAP whose costs trade off spatial distance against blinking probability. Connected localizations are then combined by an MLE weighted mean. See the SMLMFrameConnection documentation for the full algorithm; the method is from Schodt & Lidke (2021).","title":"Concept"},{"category":"section","location":"steps/frameconnect/#Uncertainty-calibration-(optional,-inside-this-step)","page":"Frame Connection","text":"Calibration is not a separate pipeline step — it is configured inside FrameConnectConfig via its calibration= field (a CalibrationConfig). When set, frame-to-frame jitter within tracks is analyzed to fit observed_var = A + B·CRLB_var, recovering an additive motion variance (σ_motion² = A) and a CRLB scale factor (k² = B). Corrected uncertainties Σ = σ_motion²·I + k²·Σ_CRLB are applied before combination, so tracks are weighted with calibrated errors in a single pass. The diagnostics land on FrameConnectInfo.calibration (a CalibrationResult).","title":"Uncertainty calibration (optional, inside this step)"},{"category":"section","location":"steps/frameconnect/#Configuration","page":"Frame Connection","text":"FrameConnectConfig is re-exported from SMLMFrameConnection (the \"upstream owns the config\" idiom). The fields you set most often:\n\nfield typical/default meaning\nmax_frame_gap 5 (dSTORM 10–20) max dark-frame gap bridged within a track; raise for long dark states\nmax_sigma_dist 5.0 distance threshold as a multiple of localization error; higher links over larger distances\ntrack_length nothing inclusive (min, max) locs-per-track filter; (2.0, Inf) drops single-frame blinks\nmax_neighbors 2 nearest neighbors inspected during preclustering\nn_density_neighbors 2 preclusters used for the local density estimate\ncalibration nothing a CalibrationConfig to enable uncertainty calibration (see above)\n\nCalibrationConfig fields: clamp_k_to_one (default true, since CRLB is a lower bound), filter_high_chi2 (default false), and chi2_filter_threshold (default 6.0). See the SMLMFrameConnection documentation for the complete list.\n\n# Basic frame connection, dropping single-frame blinks\n(combined, info) = analyze(smld,\n    FrameConnectConfig(max_frame_gap = 5, track_length = (2.0, Inf)))\n\n# With uncertainty calibration\n(combined, info) = analyze(smld,\n    FrameConnectConfig(max_frame_gap = 5,\n                       calibration = CalibrationConfig(filter_high_chi2 = true)))\n\nconnected = info.info.connected   # uncombined locs with track_id","title":"Configuration"},{"category":"section","location":"steps/frameconnect/#Output-and-interpretation","page":"Frame Connection","text":"The step's StepInfo.summary reports the headline numbers:\n\nfield meaning\nn_before input localizations (FrameConnectInfo.n_input)\nn_after output tracks / combined localizations (n_combined)\nn_filtered tracks dropped by the track_length filter\ncompression n_before / n_after — mean localizations per track\nk_scale, sigma_motion_nm, mean_chi2 added only when calibration was applied\n\nSanity checks: compression should be ≳ 1 (a few-to-many for blinky dyes); a value of ~1 means almost nothing connected — max_frame_gap/max_sigma_dist may be too tight, or the data is genuinely sparse. When calibration runs, mean_chi2 should be near 2.0 (two axes) if uncertainties are well-calibrated, and k_scale ≥ 1 reflects CRLB being a lower bound.","title":"Output & interpretation"},{"category":"section","location":"steps/frameconnect/#Notes-and-caveats","page":"Frame Connection","text":"Calibration lives here, not as its own step. Enable it through calibration=CalibrationConfig(...); its results appear on FrameConnectInfo.calibration, never as a standalone StepInfo.\nCombined vs. connected. The returned smld is the combined set (one loc per track). If you need per-blink localizations with their track assignment, use info.info.connected / AnalysisResult.smld_connected.\nTune max_frame_gap to the dye. dSTORM dyes with long dark states want a larger gap (10–20); too large over-links distinct emitters, too small fragments one emitter into many tracks.\n2D only. SMLMFrameConnection operates on Emitter2DFit data.","title":"Notes & caveats"},{"category":"section","location":"steps/frameconnect/#References","page":"Frame Connection","text":"D. J. Schodt and K. A. Lidke. \"Spatiotemporal Clustering of Repeated Super-Resolution Localizations via Linear Assignment Problem.\" Frontiers in Bioinformatics 1, 724325 (2021). doi:10.3389/fbinf.2021.724325\n\nSee the SMLMFrameConnection documentation for the algorithm in full and all configuration options.","title":"References"},{"category":"section","location":"steps/densityfilter/#Density-Filter","page":"Density Filter","text":"Isolated localizations — single fits with no nearby support — are usually noise: spurious detections, mis-fits, or unlabeled background. The density filter drops them by counting each localization's neighbors and rejecting those with too few. It is selected by a DensityFilterConfig and is a native SMLMAnalysis step (built on a KD-tree from NearestNeighbors.jl); no upstream package backs it.\n\nanalyze(smld, DensityFilterConfig())   # → (filtered_smld, StepInfo)","title":"Density Filter"},{"category":"section","location":"steps/densityfilter/#When-to-use-/-prerequisites","page":"Density Filter","text":"Run on a BasicSMLD of localizations, typically late in the pipeline — after drift correction and frame connection. Those steps sharpen and consolidate localizations, so the neighbor counts reflect true structure rather than blur or repeated blinks.\nMost useful when real structures are genuinely denser than the noise floor, so the neighbor-count distribution separates into an isolated population and a clustered population. Sparse or uniformly distributed data has no such valley to exploit — set an explicit cutoff instead (see below).","title":"When to use / prerequisites"},{"category":"section","location":"steps/densityfilter/#Inputs,-returns-and-artifacts","page":"Density Filter","text":"Input: the current smld.\nReturns: (filtered_smld, StepInfo). The per-step DensityFilterInfo is on StepInfo.info.\nArtifacts (when outdir is set): stats.md (input/output/rejected counts and the chosen threshold) and neighbor_histogram.png (the neighbor-count distribution with the threshold marked) at STANDARD verbosity, plus the saved config and info. A filtered SMLD is checkpointed to smld_density.jld2 at Checkpoint.ALL.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/densityfilter/#Concept","page":"Density Filter","text":"For every localization the step counts how many other localizations lie within n_sigma combined localization uncertainties. The neighbor test is per-pair and precision-aware: localizations i and j are neighbors when their center-to-center distance satisfies\n\nd_ij  textttn_sigma cdot sqrtsigma_i^2 + sigma_j^2\n\nwhere each sigma = sqrtsigma_x^2 + sigma_y^2 is the radial CRLB uncertainty (µm). Because the radius scales with each pair's own precision, dense high-precision regions (small sigma) require closer neighbors to count, while loosely localized points get a proportionally larger search radius. A 2-D KD-tree over the (x y) coordinates (µm) does a coarse range query (n_sigma · 2 · max σ), then the exact per-pair test refines each candidate.\n\nThe keep/reject threshold is a minimum neighbor count. With min_neighbors = :auto the step picks it by valley detection on the neighbor-count histogram: it smooths the histogram, finds the rightmost significant peak (the clustered population), and places the threshold at the local minimum (valley) between the origin and that peak. If the distribution is not clearly bimodal it falls back to a conservative heuristic (and warns) rather than guess a valley.","title":"Concept"},{"category":"section","location":"steps/densityfilter/#Configuration","page":"Density Filter","text":"field typical/default meaning\nn_sigma 2.0 neighbor radius in combined-uncertainty units; j counts as a neighbor of i when d_ij  textttn_sigmasqrtsigma_i^2+sigma_j^2\nmin_neighbors :auto minimum neighbor count to keep a localization; :auto chooses it by valley detection on the histogram, or pass an Int for an explicit cutoff\n\nSee the API Reference for the complete field list.\n\n# Automatic threshold (valley detection)\n(filtered, info) = analyze(smld, DensityFilterConfig())\n\n# Explicit cutoff: keep only localizations with ≥ 2 neighbors within 3σ\n(filtered, info) = analyze(smld, DensityFilterConfig(n_sigma = 3.0, min_neighbors = 2))\n\ninfo.info.threshold   # the neighbor-count threshold actually applied","title":"Configuration"},{"category":"section","location":"steps/densityfilter/#Output-and-interpretation","page":"Density Filter","text":"StepInfo.summary reports the headline numbers:\n\nfield meaning\nn_before localizations entering the step\nn_after localizations kept\nn_rejected n_before - n_after (the isolated localizations dropped)\nthreshold the minimum-neighbor threshold applied (chosen or supplied)\n\nDensityFilterInfo carries the same n_before, n_after, threshold plus elapsed_s.\n\nSanity checks: open neighbor_histogram.png and confirm the red threshold line sits in the valley between the low-count (isolated) and high-count (clustered) populations. A reasonable filter rejects a modest tail of isolated points, not the bulk of the data. Heed the warnings: :auto emits one when the distribution peaks at very low neighbor counts (\"most emitters appear isolated\") or is unimodal — in those cases it returns a conservative default (3) or keeps almost everything (1), which usually means density filtering is the wrong tool for that dataset or that an explicit min_neighbors should be set.","title":"Output & interpretation"},{"category":"section","location":"steps/densityfilter/#Notes-and-caveats","page":"Density Filter","text":"Run it late, and order matters. Neighbor counts depend on the entire localization set, so running before drift correction or frame connection inflates apparent isolation. The step is repeatable — re-run with a different n_sigma/min_neighbors to tune.\n:auto assumes bimodality. If the isolated and clustered populations do not separate cleanly, prefer an explicit integer min_neighbors.\nIt is a pure spatial filter. Coordinates and uncertainties are unchanged; only emitters are removed.\nThe coarse KD-tree radius uses the global maximum sigma; the exact per-pair uncertainty test is what determines neighbor membership.","title":"Notes & caveats"},{"category":"section","location":"steps/densityfilter/#References","page":"Density Filter","text":"This is a native SMLMAnalysis step with no external method citation; the automatic threshold is the histogram valley-detection heuristic described above. See the API Reference for DensityFilterConfig and DensityFilterInfo.","title":"References"},{"category":"section","location":"api/steps/#Step-Configs-and-Info","page":"Step Configs & Info","text":"Each pipeline step is driven by a config struct and returns a typed info struct. See The Pipeline Model for how analyze dispatches on these types, and Pipeline Steps: Overview for the per-step reference pages.","title":"Step Configs & Info"},{"category":"section","location":"api/steps/#Step-Configs","page":"Step Configs & Info","text":"Native to SMLMAnalysis:\n\nRe-exported from upstream packages (the owning package documents the algorithm; SMLMAnalysis dispatches analyze on these types — see The Pipeline Model):\n\nnote: CalibrationConfig is a sub-config\nCalibrationConfig configures uncertainty calibration inside FrameConnectConfig (via its calibration= field) — it is not a separate pipeline step. CalibrationResult holds its output.","title":"Step Configs"},{"category":"section","location":"api/steps/#Clustering-(re-exported)","page":"Step Configs & Info","text":"The clustering verbs cluster / cluster_statistics and their config types (DBSCANConfig, HDBSCANConfig, HierarchicalConfig, VoronoiConfig, HopkinsConfig, VoronoiDensityConfig) are re-exported from SMLMClustering and dispatched as pipeline steps — see Clustering. Their full API and algorithm reference lives in the SMLMClustering manual; SMLMAnalysis adds only the analyze() dispatch (it does not re-document the upstream API here).","title":"Clustering (re-exported)"},{"category":"section","location":"api/steps/#Step-Info-Types","page":"Step Configs & Info","text":"Each step's analyze() returns a StepInfo whose .info field holds the step's typed info struct.\n\nnote: Upstream info types\nSome steps return their upstream packages' info structs: SMLMFrameConnection.FrameConnectInfo, SMLMDriftCorrection.DriftInfo, SMLMRender.RenderInfo, and the clustering ClusterInfo / ClusterStatisticsInfo — documented in those packages. The connected tracks and drift model are also surfaced on AnalysisResult as smld_connected and drift_model.","title":"Step Info Types"},{"category":"type","location":"api/steps/#SMLMAnalysis.DetectFitConfig","page":"Step Configs & Info","text":"DetectFitConfig <: AbstractSMLMConfig\n\nCombined detection and fitting step. Detects ROIs via SMLMBoxer and fits localizations via GaussMLE in a single step, with per-dataset processing.\n\nEmbedded Configs\n\nboxer::BoxerConfig: Detection parameters (boxsize, minphotons, psfsigma, etc.)\nfitter::GaussMLEConfig: Fitting parameters (psf_model, iterations, constraints, etc.)\n\nData Source Keywords (for file-based workflows)\n\npath: Single H5 file path\npaths: Vector of H5 file paths (one per dataset)\ndataset_frames: Explicit frame ranges per dataset\nh5_format: :auto, :smart, or :mic\n\nDataset Selection\n\ndatasets: Optional AbstractVector{Int} (e.g. 1:19 or [1,2,3,5]) selecting a subset of the resolved source slots. Applies uniformly across all source modes: MIC auto-blocks, multi-path paths, explicit dataset_frames, and in-memory Vector{Array} input. Selected slots are reindexed to contiguous 1:length(datasets) in the output SMLD (original source indices preserved in DetectFitInfo.selected_source_indices). Default nothing uses all resolved slots.\n\n\n\n\n\n","title":"SMLMAnalysis.DetectFitConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.FilterConfig","page":"Step Configs & Info","text":"FilterConfig <: AbstractSMLMConfig\n\nQuality-based filtering of localizations. All criteria use (min, max) tuples.\n\nKeywords\n\nphotons: Photon count range, e.g. (500.0, Inf)\nprecision: Lateral localization precision range in microns, max(σ_x, σ_y), e.g. (0.0, 0.007)\npvalue: Goodness-of-fit p-value range, e.g. (1e-3, 1.0)\npsf_sigma: PSF width filter. :auto uses mode ± 10%, or explicit (min, max) in microns\nz: Axial position range in microns (3D fits only), e.g. (-0.4, 0.4). Useful to drop localizations railed to the z-grid edge. No-op on 2D SMLDs.\nsigma_z: Axial precision range in microns (3D fits only), e.g. (0.0, 0.030). The axial analog of precision. No-op on 2D SMLDs.\n\nAll filters default to nothing (disabled).\n\n\n\n\n\n","title":"SMLMAnalysis.FilterConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.IntensityFilterConfig","page":"Step Configs & Info","text":"IntensityFilterConfig <: AbstractSMLMConfig\n\nIntensity-based multi-emitter rejection. Estimates the spatially-varying expected single-emitter photon rate and rejects localizations whose photon count is statistically inconsistent with single-emitter emission (Poisson upper-tail test).\n\nThe rate estimate uses the rate_percentile of the per-bin photon distribution (default: 95th percentile). This captures the upper bound of single-emitter emission at each spatial position, accounting for both excitation field variation and stochastic on-time variance. The Gaussian field fit then smooths across bins.\n\nKeywords\n\ncutoff::Float64: P-value cutoff; emitters with p < cutoff are rejected (default: 0.01)\nfield_mode::Symbol: Excitation field model — :uniform (single global rate) or :gaussian (spatially-varying Gaussian beam fit) (default: :gaussian)\nn_bins::Int: Spatial grid bins per axis for field estimation (default: 10)\nmin_bin_count::Int: Minimum emitters per bin for rate estimation (default: 30)\nrate_percentile::Float64: Percentile of per-bin photon distribution used as the expected single-emitter rate (default: 0.95). Higher = more permissive.\n\np₂ (double-emitter fraction, info-only)\n\np₂ is the estimated fraction of localizations that are actually two overlapping emitters. It is reported in IntensityFilterInfo and does not affect which localizations are filtered.\n\nestimate_p2::Bool: Whether to estimate p₂ (default: true).\np2_method::Symbol: Estimator for p₂ (default: :mixture).\n:mixture — threshold-free two-component mixture fit of the field-normalized photon distribution: h(x) = (1-p)·f_single(x) + p·f_double(x) with f_double = f_single ⊛ f_single. f_single is a Gamma model fit jointly with the mixing weight p; p is returned directly as the double fraction. Uses the whole distribution (no tail threshold), so it is unbiased and does not floor near 1 - rate_percentile.\n:tail — legacy tail-ratio estimate Σh(>τ) / Σf_double(>τ) at p2_tail_threshold. Biased high (floored near 1 - rate_percentile) because it attributes the entire single-emitter tail to doubles. Kept for comparison/back-compat only.\np2_tail_threshold::Float64: Tail threshold τ (in units of λ) for the :tail method and for the tail-mass diagnostics reported in IntensityFilterInfo (default: 1.0).\np2_n_bins::Int: Number of histogram bins used for p₂ estimation (default: 200).\n\n\n\n\n\n","title":"SMLMAnalysis.IntensityFilterConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.DensityFilterConfig","page":"Step Configs & Info","text":"DensityFilterConfig <: AbstractSMLMConfig\n\nDensity-based filtering that removes isolated localizations lacking nearby neighbors.\n\nKeywords\n\nn_sigma: Search radius in localization uncertainty units (default: 2.0)\nmin_neighbors: Minimum neighbor count. :auto uses valley detection between isolated and clustered populations (default: :auto)\n\n\n\n\n\n","title":"SMLMAnalysis.DensityFilterConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.FrameConnectConfig","page":"Step Configs & Info","text":"FrameConnectConfig\n\nConfiguration parameters for frame connection algorithm.\n\nFields\n\nn_density_neighbors::Int=2: Number of nearest preclusters used for local density                            estimates (see estimatedensities)\nmax_sigma_dist::Float64=5.0: Multiplier of localization errors that defines a                           pre-clustering distance threshold (see precluster)\nmax_frame_gap::Int=5: Maximum frame gap between temporally adjacent localizations                       in a precluster (see precluster)\nmax_neighbors::Int=2: Maximum number of nearest-neighbors inspected for precluster                  membership (see precluster)\ntrack_length::Union{Tuple{Float64,Float64}, Nothing}=nothing: Inclusive (min, max)                  range on the number of localizations a track must contain to be emitted                  as a combined localization (lo <= nperID <= hi). Counts localizations                  sharing a track_id, not temporal frame-span. nothing (default) disables                  filtering. Use (2.0, Inf) to drop single-frame blinks, or (2.0, 50.0)                  to also drop over-long tracks (e.g. fiducials, sticky docking strands in                  dense DNA-PAINT). Mirrors the (min, max) filter idiom used elsewhere in                  the ecosystem. See combinelocalizations.\ncalibration::Union{CalibrationConfig, Nothing}=nothing: Optional uncertainty calibration\n\nExample\n\n# Without calibration (default)\n(combined, info) = frameconnect(smld)\n\n# With calibration\nconfig = FrameConnectConfig(calibration=CalibrationConfig())\n(combined, info) = frameconnect(smld, config)\n\n# With calibration + chi2 filtering\nconfig = FrameConnectConfig(\n    calibration=CalibrationConfig(filter_high_chi2=true, chi2_filter_threshold=4.0)\n)\n(combined, info) = frameconnect(smld, config)\n\n\n\n\n\n","title":"SMLMAnalysis.FrameConnectConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.CalibrationConfig","page":"Step Configs & Info","text":"CalibrationConfig\n\nConfiguration for optional uncertainty calibration within frame connection.\n\nCalibration analyzes frame-to-frame jitter within connected tracks to estimate a motion variance (σ_motion²) and CRLB scale factor (k²), then applies corrected uncertainties before track combination: Σ_corrected = σ_motion² I + k² Σ_CRLB.\n\nFields\n\nclamp_k_to_one::Bool=true: Clamp k_scale to minimum of 1.0 (CRLB is a lower bound)\nfilter_high_chi2::Bool=false: Filter tracks with high chi² pairs before fitting\nchi2_filter_threshold::Float64=6.0: Chi² threshold for track filtering (per pair)\n\n\n\n\n\n","title":"SMLMAnalysis.CalibrationConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.DriftConfig","page":"Step Configs & Info","text":"DriftConfig <: AbstractSMLMConfig\n\nConfiguration for drift correction, holding all algorithm parameters. Constructed with keyword arguments; all fields have sensible defaults.\n\nFields\n\nField Default Description\nquality :singlepass Quality tier: :fft, :singlepass, or :iterative\ndegree 2 Legendre polynomial degree for intra-dataset drift\ndataset_mode :registered Multi-dataset handling: :registered or :continuous\nchunk_frames 0 Split datasets into chunks of N frames (0 = no chunking)\nn_chunks 0 Alternative: number of chunks per dataset (0 = use chunk_frames)\nmaxn 100 Maximum neighbors for entropy calculation\nmax_iterations 10 Maximum iterations for :iterative mode\nconvergence_tol 0.001 Convergence tolerance in μm for :iterative mode\nwarm_start nothing Previous info.model for warm starting optimization\nverbose 0 Verbosity level: 0=quiet, 1=info, 2=debug\nauto_roi false Use dense ROI subset for faster estimation\nσ_loc 0.010 Typical localization precision (μm) for ROI sizing\nσ_target 0.001 Target drift precision (μm) for ROI sizing\nroi_safety_factor 4.0 Safety multiplier for required localizations\n\nExample\n\njulia> config = DriftConfig(quality=:iterative, degree=3);\n\njulia> config.quality\n:iterative\n\njulia> config.degree\n3\n\njulia> config.convergence_tol\n0.001\n\n\n\n\n\n","title":"SMLMAnalysis.DriftConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.RenderConfig","page":"Step Configs & Info","text":"RenderConfig <: AbstractSMLMConfig\n\nFlat rendering configuration. Fields correspond 1:1 with render() keyword arguments.\n\nFields\n\nstrategy::RenderingStrategy: Rendering algorithm (default: GaussianRender())\npixel_size::Union{Float64, Nothing}: Pixel size in nm (data bounds mode)\nzoom::Union{Float64, Nothing}: Zoom factor (camera FOV mode)\nroi::Union{Tuple, Nothing}: Camera pixel ROI as (x_range, y_range)\ntarget::Union{Image2DTarget, Nothing}: Explicit target specification\ncolormap::Union{Symbol, Nothing}: Intensity-based colormap (:inferno, :viridis, etc.)\ncolor_by::Union{Symbol, Nothing}: Field for field-based coloring (:z, :photons, etc.)\ncolor::Union{RGB, Symbol, Nothing}: Manual color\ncategorical::Bool: Use categorical palette for integer fields (default: false)\nclip_percentile::Union{Float64, Nothing}: Intensity clipping percentile (default: 0.99). Use nothing for saturate mode (no normalization, values can exceed 1.0).\nfield_range::Union{Tuple{Float64,Float64}, Symbol}: Value range or :auto\nfield_clip_percentiles::Union{Tuple{Float64,Float64}, Nothing}: Percentile clipping\nbackend::Symbol: Computation backend (default: :cpu)\nfilename::Union{String, Nothing}: Save to file if provided\nscalebar::Bool: Enable scale bar overlay (default: false)\nscalebar_length::Union{Float64, Nothing}: Physical length in μm (nothing = auto)\nscalebar_position::Symbol: Corner position (:br, :bl, :tr, :tl)\nscalebar_color::Symbol: Bar color (:white or :black)\n\n\n\n\n\n","title":"SMLMAnalysis.RenderConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.BaGoLConfig","page":"Step Configs & Info","text":"BaGoLConfig <: SMLMData.AbstractSMLMConfig\n\nConfiguration for BaGoL analysis. All fields correspond 1:1 to run_bagol kwargs.\n\nCount Model\n\nμ=10.0: Mean localizations per emitter\nshape=2.0: Gamma shape (1=exponential/dSTORM, >1=peaked/DNA-PAINT)\nlearn_distribution=true: Learn count params. true=both, false=fix both, :mu=learn μ only, :shape=learn shape only\ngamma=nothing: DM concentration. nothing=use shape (default), Float64=fixed\nlearn_rho=true: Learn Poisson-K emitter density ρ independently of μ/shape. For fully fixed hyperparameters, set both learn_distribution=false and learn_rho=false.\nrho=nothing: Initial ρ in emitters per unit area. If provided with learn_rho=false, hold this fixed for Fazel-style fixed-ρ-over-bounding-box flat Poisson-K runs.\n\nMCMC\n\nn_iterations=4000: Total MCMC iterations\nburn_in=2000: Burn-in before accumulating\nsync_interval=100: Iterations between global hierarchical updates\nallocation_model=:dm: :dm (Dirichlet-Multinomial), :decoupled (no partition prior), or :categorical (labeled K^(-N) — Fazel-equivalent when paired with spatial_model=:flat + k_prior=:none)\nspatial_model=:locmix: :locmix (localization mixture) or :flat\nk_prior=:auto: K-prior gating. :auto uses spatial-model default (Poisson(ρA) under :flat, none under :locmix); :poisson always include (only valid with :flat); :none always disable\nn_restricted_scans=5: Jain-Neal restricted Gibbs scans for split/merge\nn_bd_substeps=5: Birth/death substeps per BD selection\n\nPartitioning\n\npartition_sigma=3.0: DBSCAN threshold in sigma units (Inf=no partitioning)\nmin_partition_size=0: Minimum locs per partition\nmax_partition_size=1000: METIS-split partitions larger than this\nskip_partition_size=typemax(Int): Skip partitions larger than this\noverlap=:auto: Overlap width for bisected partitions (:auto or Float64 in μm)\nbridge_ratio=0.0: Optional bridge refinement strength for DBSCAN clusters\nmin_split_size=3: Minimum core component size for bridge refinement\n\nUncertainty Correction (standalone; 0 in the integrated pipeline)\n\nse_adjust=:auto: :auto runs the finder (estimate_se_adjust) and uses τ̂ (skipped if the SMLD is already σ-corrected); 0.0 = no correction; a numeric value = manual τ (μm) added in quadrature to per-loc σ (σ²+τ²) — scalar, (τx,τy) tuple, length-N vector, or (τx_vec,τy_vec).\nforce_se_adjust=false: Apply even if the SMLD is already σ-corrected.\n\nOutput\n\nposterior_pixel_size=0.001: Rao-Blackwellized posterior image pixel size in μm (1 nm; 0.0=disable)\narchive_path=nothing: Mmap chain archive path (nothing=disable)\nprogress_file=nothing: Write progress to file (nothing=disable)\nverbose=true: Print progress to stdout\n\n\n\n\n\n","title":"SMLMAnalysis.BaGoLConfig"},{"category":"type","location":"api/steps/#SMLMAnalysis.DetectFitInfo","page":"Step Configs & Info","text":"DetectFitInfo <: AbstractSMLMInfo\n\nInfo from combined detection and fitting step.\n\nselected_source_indices records the original source slot indices when DetectFitConfig.datasets selected a subset (e.g. [1,2,3,5,7] when a corrupted block was skipped). nothing means no selection was applied and the output datasets correspond 1:1 to the resolved source slots.\n\n\n\n\n\n","title":"SMLMAnalysis.DetectFitInfo"},{"category":"type","location":"api/steps/#SMLMAnalysis.FilterInfo","page":"Step Configs & Info","text":"FilterInfo <: AbstractSMLMInfo\n\nInfo from quality filtering step.\n\n\n\n\n\n","title":"SMLMAnalysis.FilterInfo"},{"category":"type","location":"api/steps/#SMLMAnalysis.IntensityFilterInfo","page":"Step Configs & Info","text":"IntensityFilterInfo <: AbstractSMLMInfo\n\nInfo from intensity-based multi-emitter rejection step.\n\nFields\n\nn_before::Int: Emitter count before filtering\nn_after::Int: Emitter count after filtering\nfield_mode::Symbol: Actual field mode used (:uniform or :gaussian, may fallback)\nlambda_max_global::Float64: Global mode photon count\nfield_params::Union{NamedTuple, Nothing}: Gaussian field params (A, x0, y0, w, bg) or nothing\nfield_fit_r2::Float64: R² of field fit (0.0 for uniform)\nelapsed_s::Float64: Elapsed time (s)\np2_estimate::Union{Float64, Nothing}: Estimated double-emitter fraction\np2_tail_threshold::Float64: Normalized threshold used for p₂ estimation\np2_tail_obs::Union{Float64, Nothing}: Observed tail mass fraction above threshold\np2_tail_f2::Union{Float64, Nothing}: Double-distribution tail mass fraction above threshold\n\n\n\n\n\n","title":"SMLMAnalysis.IntensityFilterInfo"},{"category":"type","location":"api/steps/#SMLMAnalysis.DensityFilterInfo","page":"Step Configs & Info","text":"DensityFilterInfo <: AbstractSMLMInfo\n\nInfo from density-based filtering step.\n\n\n\n\n\n","title":"SMLMAnalysis.DensityFilterInfo"},{"category":"type","location":"api/steps/#SMLMAnalysis.BaGoLInfo","page":"Step Configs & Info","text":"BaGoLInfo <: AbstractSMLMInfo\n\nInfo from BaGoL grouping step.\n\nFields\n\nn_locs_in::Int: Input localization count\nn_emitters::Int: Output emitter count\ncompression::Float64: Compression ratio (nlocsin / n_emitters)\nfinal_μ::Float64: Final mean localizations per emitter\nfinal_shape::Float64: Final NegBin shape parameter\nn_partitions::Int: Number of spatial partitions used\ndiagnostics::SMLMBaGoL.BaGoLDiagnostics: Full diagnostics for advanced use\n\n\n\n\n\n","title":"SMLMAnalysis.BaGoLInfo"},{"category":"section","location":"workflows/troubleshooting/#Troubleshooting","page":"Troubleshooting","text":"A practical guide to the failure modes you are most likely to hit. Each entry is symptom → likely cause → fix, with a link to the step page that covers it in depth. Most problems are either a step-ordering mistake (caught by Julia's dispatch as a MethodError) or a parameter mismatched to the data or optics.","title":"Troubleshooting"},{"category":"section","location":"workflows/troubleshooting/#MethodError-from-analyze-(wrong-step-order)","page":"Troubleshooting","text":"Symptom. An error like MethodError: no method matching analyze(::Vector{...}, ::FilterConfig).\n\nCause. A step that consumes a BasicSMLD was placed before any DetectFitConfig, or the steps are otherwise in an impossible order. Only DetectFitConfig turns raw image data into an smld; every other step expects an existing smld, so dispatching one on a raw image stack finds no method.\n\nFix. Make DetectFitConfig the first step:\n\nsteps = [\n    DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130)),\n    FilterConfig(photons = (500.0, Inf)),   # now receives a BasicSMLD\n    RenderConfig(zoom = 20),\n]\n\nThis is dispatch working as designed — a wrong order is a clear error, not a silent wrong answer. See The Pipeline Model.","title":"MethodError from analyze (wrong step order)"},{"category":"section","location":"workflows/troubleshooting/#Pipeline-produced-no-SMLD","page":"Troubleshooting","text":"Symptom. Pipeline produced no SMLD. Did you include a DetectFitConfig step?\n\nCause. The steps vector contains no DetectFitConfig, so the orchestrator never produced any localizations to thread through the pipeline.\n\nFix. Add a DetectFitConfig as the first step (see Detection & Fitting). If you are resuming from a saved smld (smld = load_smld(\"after_detectfit.h5\")), run the later steps directly on it rather than through a pipeline that starts from images.","title":"Pipeline produced no SMLD"},{"category":"section","location":"workflows/troubleshooting/#Too-many-or-too-few-detections","page":"Troubleshooting","text":"Symptom. The detection/fit overlay boxes noise instead of real spots, or misses obvious molecules; DetectFitInfo.n_fits is far below n_rois.\n\nCause. BoxerConfig.boxsize and psf_sigma are mismatched to your optics: a wrong psf_sigma (expected PSF width, in microns) mis-thresholds detection, and a boxsize too small for the PSF (or too large, merging neighbors) makes the fit reject candidates. A large n_rois–n_fits gap is the tell.\n\nFix. Tune boxsize and psf_sigma to your pixel size and PSF, and choose a fitter PSF model that matches (GaussianXYNBS for free width, AstigmaticXYZNB for 3D); inspect the overlay figure after each adjustment. See Detection & Fitting.","title":"Too many or too few detections"},{"category":"section","location":"workflows/troubleshooting/#Large-inter-dataset-shift-warnings-/-bad-drift","page":"Troubleshooting","text":"Symptom. A PROGRESS-level warning that inter-dataset shifts exceed ~500 nm, a jagged drift_trajectory.png, or a smeared reconstruction after drift correction.\n\nCause. The wrong DriftConfig.dataset_mode. Correcting one continuous movie as :registered (or independent overlapping acquisitions as :continuous) produces large spurious inter-dataset shifts.\n\nFix. Match the mode to how the data was acquired — :continuous for one long movie (e.g. DriftConfig(degree = 3, dataset_mode = :continuous, chunk_frames = 4000) for long runs), :registered for multiple overlapping acquisitions of the same field. See Drift Correction. Note that per-dataset frame numbering is by design (required by the Legendre normalization), not a bug.","title":"Large inter-dataset shift warnings / bad drift"},{"category":"section","location":"workflows/troubleshooting/#Crowded-data-/-suspiciously-bright-localizations","page":"Troubleshooting","text":"Symptom. A few localizations with abnormally high photon counts, or a reconstruction that looks denser/blurrier than expected.\n\nCause. When two fluorophores are active inside one diffraction-limited spot, the fit returns a single localization with a corrupted position and an inflated photon count. Separately, frame connection can over- or under-link in dense data.\n\nFix. Add an Intensity Filter (e.g. IntensityFilterConfig(cutoff = 0.01, field_mode = :gaussian)) to reject multi-emitter events with its Poisson upper-tail test; check its p2_estimate to gauge how crowded the data is. For Frame Connection, tune max_frame_gap: too large over-links distinct emitters into one track, too small fragments one emitter into many. A compression near 1 in the summary means almost nothing connected.","title":"Crowded data / suspiciously bright localizations"},{"category":"section","location":"workflows/troubleshooting/#Out-of-memory-on-large-acquisitions","page":"Troubleshooting","text":"Symptom. Julia runs out of memory while loading or detecting on a big acquisition.\n\nCause. Holding the entire image stack in memory at once.\n\nFix. Process the acquisition as multiple datasets — detection and fitting loop over datasets individually, so memory use stays bounded. Use file-based detection (a single file split into blocks, or one file per dataset) rather than passing all images in memory:\n\n# MIC blocks auto-detected as datasets, loaded block by block\nDetectFitConfig(path = \"data.h5\", h5_format = :mic,\n                boxer = BoxerConfig(boxsize = 9))\n\nSee Multi-Dataset.","title":"Out-of-memory on large acquisitions"},{"category":"section","location":"workflows/troubleshooting/#GPU-/-CUDA-errors-during-fitting","page":"Troubleshooting","text":"Symptom. A CUDA-related error raised inside the Detection & Fitting step.\n\nCause. Fitting is GPU-accelerated through GaussMLE, which needs a working CUDA setup. The error surfaces when CUDA is unavailable or misconfigured.\n\nFix. Confirm your CUDA installation (driver, CUDA.jl functional); see Installation & Setup for requirements and the GaussMLE docs for its GPU/CPU options.","title":"GPU / CUDA errors during fitting"},{"category":"section","location":"workflows/troubleshooting/#Empty-output-/-no-figures-written","page":"Troubleshooting","text":"Symptom. The pipeline runs cleanly but no diagnostic figures or stats.md appear in the step output folders.\n\nCause. Figures are gated on two things: an outdir must be set, and verbose must be high enough. At Verbosity.SILENT/PROGRESS only counts and timing are emitted — basic figures (overlays, fit-quality, drift plots) start at Verbosity.STANDARD, diagnostics at DETAILED, movies at DEBUG.\n\nFix. Set an output directory and raise the verbosity:\n\nconfig = AnalysisConfig(camera = cam, steps = [...],\n                        outdir = \"output/\", verbose = Verbosity.DETAILED)\n\nSee Running a Pipeline for the verbosity levels and what each one writes.","title":"Empty output / no figures written"},{"category":"section","location":"steps/filter/#Quality-Filter","page":"Quality Filter","text":"Single-molecule localization produces a population of fits of wildly varying quality — bright clean blinks alongside dim, mis-fit, or multi-emitter events. The quality-filter step keeps only the localizations that pass per-property threshold ranges, discarding the rest. It is selected by a FilterConfig and is a native SMLMAnalysis step (no upstream package): the thresholding logic lives in this package.\n\nanalyze(smld, FilterConfig(photons = (500.0, Inf)))   # → (filtered_smld, StepInfo)","title":"Quality Filter"},{"category":"section","location":"steps/filter/#When-to-use-/-prerequisites","page":"Quality Filter","text":"Run on a BasicSMLD of localizations — typically right after detection/fitting to cut weak and spurious fits before they reach later steps.\nThe step is repeatable (see The Pipeline Model): a common pattern is a coarse filter early (e.g. a loose photon floor) and a tighter filter after frame connection, once linked blinks have sharper precision and goodness-of-fit estimates.\nFiltering on precision, psf_sigma, z, or sigma_z requires emitters that actually carry those fields; the z / sigma_z filters are no-ops on 2D SMLDs (emitters without z / σ_z).","title":"When to use / prerequisites"},{"category":"section","location":"steps/filter/#Inputs,-returns-and-artifacts","page":"Quality Filter","text":"Input: the current smld.\nReturns: (filtered_smld, StepInfo). The filter's own FilterInfo (n_before, n_after, elapsed_s) is on StepInfo.info.\nArtifacts (when outdir is set, at STANDARD verbosity): stats.md (counts, acceptance %, criteria applied), fit_quality.png (photon, background, precision, p-value, PSF-width — and for 3D fits z and σz — distributions with the threshold/rejected regions drawn), a `fitoverlay.pngof sampled ROIs colored by pass/fail (drawn only when a detect-fit sample cache is present; gracefully skipped for a standalone filter), and a post-filterlocalizationsperframe.png. AtDETAILEDverbosity adetailedstats.mdgives per-criterion pass/fail counts. A filtered SMLD is checkpointed tosmldfiltered.jld2atCheckpoint.ALL`.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/filter/#Concept","page":"Quality Filter","text":"Each criterion is a closed interval. An emitter is kept only if every enabled criterion passes — the filters combine as a logical AND over the population. All criteria are (min, max) tuples (use -Inf / Inf for an open bound), and every filter defaults to nothing, i.e. disabled, so you pay only for the criteria you set.\n\nphotons — total fitted photons; a floor removes dim, poorly-localized blinks.\nprecision — lateral localization precision in microns, tested as max(σ_x, σ_y). These σ are the exact CRLB fit uncertainties from the MLE Fisher information (Smith et al. 2010; Huang et al. 2013 for sCMOS), not an analytical approximation; a precision ceiling is the most direct way to bound reconstruction resolution.\npvalue — goodness-of-fit p-value from the fit's log-likelihood-ratio statistic (Huang et al. 2011); a floor rejects fits whose residuals are inconsistent with the PSF model (often multi-emitter or contaminated ROIs).\npsf_sigma — fitted PSF width. Real single molecules cluster tightly around one width; out-of-band widths flag defocus or blends. Pass an explicit (min, max) in microns, or :auto to bound the population mode ± 10 % (computed per axis for anisotropic σx/σy models).\nz, sigma_z — axial position and axial precision (microns) for 3D fits; the axial analogs of photons-window and precision. The z window is handy to drop localizations railed to the z-grid edge.","title":"Concept"},{"category":"section","location":"steps/filter/#Configuration","page":"Quality Filter","text":"All fields are (min, max) tuples (microns where a length), nothing to disable; psf_sigma also accepts the :auto symbol.\n\nfield typical meaning\nphotons (500.0, Inf) photon-count window; floor removes dim fits\nprecision (0.0, 0.007) lateral precision max(σ_x, σ_y) window, µm (≤7 nm here)\npvalue (1e-3, 1.0) goodness-of-fit p-value window; floor rejects bad fits\npsf_sigma :auto PSF-width window, µm — or :auto for mode ± 10 %\nz (-0.4, 0.4) axial-position window, µm (3D only; no-op on 2D)\nsigma_z (0.0, 0.030) axial-precision window, µm (3D only; no-op on 2D)\n\nSee the API Reference for the complete field list.\n\n# Coarse cut after detection…\n(smld, _) = analyze(smld, FilterConfig(photons = (500.0, Inf)))\n\n# …then a tighter pass after frame connection\n(smld, info) = analyze(smld, FilterConfig(\n    photons   = (1000.0, Inf),\n    precision = (0.0, 0.007),     # ≤ 7 nm lateral\n    pvalue    = (1e-3, 1.0),\n    psf_sigma = :auto,            # keep widths within mode ± 10 %\n))\n\ninfo.info.n_after / info.info.n_before    # acceptance fraction","title":"Configuration"},{"category":"section","location":"steps/filter/#Output-and-interpretation","page":"Quality Filter","text":"StepInfo.summary reports the headline numbers:\n\nfield meaning\nn_before localizations entering the filter\nn_after localizations kept\nacceptance n_after / n_before, rounded to 3 digits\n\nSanity checks: inspect fit_quality.png and confirm the threshold lines sit on the shoulders of each distribution, not through its peak — a filter that removes most of the data is usually mis-set (e.g. a precision ceiling tighter than the fits can achieve, or a photon floor above the median). For 3D data a spike at the edge of the z panel signals grid-railed degenerate fits that the z window should remove. The per-criterion detailed_stats.md (DETAILED verbosity) shows which single criterion is responsible for most of the loss.","title":"Output & interpretation"},{"category":"section","location":"steps/filter/#Notes-and-caveats","page":"Quality Filter","text":"Order matters for downstream precision. Filtering on precision / psf_sigma before frame connection uses single-blink CRLB values; the same filter after connection acts on the combined, higher-precision tracks. Decide which population you mean to threshold.\n:auto follows the data. The mode ± 10 % band adapts to each dataset's fitted-width distribution, so the same FilterConfig(psf_sigma = :auto) yields different bounds on different acquisitions — a feature for batch runs, but check the bounds drawn in fit_quality.png if a dataset is unusual.\n3D filters silently no-op on 2D. z / sigma_z are skipped when emitters lack those fields, so leaving them set in a shared config is harmless.\nDisabled by default. An empty FilterConfig() passes everything through unchanged.","title":"Notes & caveats"},{"category":"section","location":"steps/filter/#References","page":"Quality Filter","text":"C. S. Smith, N. Joseph, B. Rieger, K. A. Lidke. \"Fast, single-molecule localization that achieves theoretically minimum uncertainty.\" Nature Methods 7, 373–375 (2010). doi:10.1038/nmeth.1449 — the exact CRLB (from the MLE Fisher information) behind the σ values the precision criterion bounds.\nF. Huang, T. M. P. Hartwich, F. E. Rivera-Molina, et al. \"Video-rate nanoscopy using sCMOS camera-specific single-molecule localization algorithms.\" Nature Methods 10, 653–658 (2013). doi:10.1038/nmeth.2488 — extends the exact CRLB to per-pixel sCMOS noise.\nF. Huang, S. L. Schwartz, J. M. Byars, K. A. Lidke. \"Simultaneous multiple-emitter fitting for single molecule super-resolution imaging.\" Biomedical Optics Express 2, 1377–1393 (2011). doi:10.1364/BOE.2.001377 — the log-likelihood-ratio goodness-of-fit test behind the pvalue criterion.\n\nThis is a native SMLMAnalysis step; see the API Reference for the full FilterConfig field list.","title":"References"},{"category":"section","location":"guide/#Guide","page":"User Guide","text":"This page is an orientation map for the rest of the documentation. Each topic now has its own dedicated page under Concepts, Workflows, and Pipeline Steps — use the task-oriented links below to jump straight to what you need.\n\nNew to the package? Start with the Tutorial, which runs a complete pipeline on simulated data and shows the output of every step.","title":"User Guide"},{"category":"section","location":"guide/#Find-what-you-need","page":"User Guide","text":"I want to… Go to\nUnderstand how analyze() routes steps by dispatch The Pipeline Model\nSee where SMLMAnalysis sits in the JuliaSMLM ecosystem The JuliaSMLM Ecosystem\nKnow what an SMLD stores and how provenance is tracked Data Model & Provenance\nInstall the package and its unregistered dependencies Installation & Setup\nRun a full pipeline from an AnalysisConfig Running a Pipeline\nSet the output detail level (verbosity) Running a Pipeline\nProcess multiple files, or chunk one long movie Multi-Dataset Acquisitions\nAnalyze multiple color channels and overlay them Multi-Channel Analysis\nSave intermediate state and resume a session later I/O & Resume\nImport SMART / MIC microscope .h5 data I/O & Resume\nAdd a custom step to the pipeline Extending the Pipeline\nDiagnose a failure or an unexpected result Troubleshooting","title":"Find what you need"},{"category":"section","location":"guide/#Configure-a-step","page":"User Guide","text":"Every pipeline step has a reference page documenting its config fields, the algorithm it dispatches to, and the diagnostic outputs it writes:\n\nDetection & Fitting · Quality Filter · Intensity Filter · Frame Connection · Drift Correction · Density Filter · Rendering · Bayesian Grouping (BaGoL) · Clustering\n\nMulti-channel steps: Composite Render · Cross-Alignment · Cross-Correlation\n\nnote: Uncertainty calibration is not a separate step\nCRLB-vs-observed uncertainty calibration is configured inside frame connection via FrameConnectConfig(calibration = CalibrationConfig(...)), so it is documented on the Frame Connection page rather than as its own step.\n\nSee Pipeline Steps: Overview for the full list and the ordering rules (DetectFitConfig must come first; most other steps are freely orderable, repeatable, or optional), and the API Reference for every exported config, info, and function.","title":"Configure a step"},{"category":"section","location":"concepts/multitarget/#The-Multi-Target-Model","page":"The Multi-Target Model","text":"A multi-color experiment images two or more targets — say an IgG variant and complement C1q — each in its own channel. SMLMAnalysis handles these by generalizing the single-channel pipeline, not by bolting on a separate system. If you understand The Pipeline Model, you already understand most of this.","title":"The Multi-Target Model"},{"category":"section","location":"concepts/multitarget/#Two-axes-of-generalization","page":"The Multi-Target Model","text":"The single-channel pipeline threads one BasicSMLD through a list of step configs, dispatching analyze(state, config) on their types. The multi-target pipeline keeps exactly that idea and changes only two things:\n\n Single-channel Multi-target\nState one BasicSMLD a Vector{BasicSMLD} — one entry per channel\nSteps <: AbstractSMLMConfig <: AbstractMultiTargetStep\n\nEverything else — the analyze(state, step) dispatch, the (result, info) tuple per step, the threaded state — is unchanged. A cross-channel step is simply an analyze(::Vector{BasicSMLD}, ::AbstractMultiTargetStep) method.","title":"Two axes of generalization"},{"category":"section","location":"concepts/multitarget/#Two-phases","page":"The Multi-Target Model","text":"One MultiTargetConfig and one analyze call describe a run with two phases:\n\nPer-channel pipelines. Each channel is analyzed independently by a full single-channel pipeline — the orchestrator literally calls analyze(data, AnalysisConfig) once per channel, reusing that channel's own camera, steps, ROI, and verbosity. The resulting per-channel SMLDs become the channel vector.\nCross-channel steps. The ordered AbstractMultiTargetSteps then run over that Vector{BasicSMLD}, threading it from one step to the next.\n\nPhase 1 shares no state between channels — each is exactly the analysis you would run on a single color. All channel interaction happens in phase 2.","title":"Two phases"},{"category":"section","location":"concepts/multitarget/#Pass-through-vs.-state-modifying-steps","page":"The Multi-Target Model","text":"As with single-channel steps, a cross-channel step either transforms the state or just reads it for its outputs:\n\nPass-through steps return the channel vector unchanged — Composite Render (the multi-color overlay image) and Cross-Correlation (the co-localization g(r)).\nState-modifying steps replace the vector — Cross-Alignment returns aligned SMLDs. Because the state threads in order, a composite render placed after an alignment step shows the corrected overlay, while one placed before shows the raw registration. Step order is meaningful.","title":"Pass-through vs. state-modifying steps"},{"category":"section","location":"concepts/multitarget/#No-provenance-is-lost","page":"The Multi-Target Model","text":"analyze returns a (MultiTargetResult, MultiTargetInfo) tuple that exposes both the merged channel vector and each channel's complete single-channel records:\n\nresult.smlds         # Vector{BasicSMLD} (aligned if Cross-Alignment ran)\nresult[:IgG]         # the channel's full AnalysisResult\ninfo.channels[:IgG]  # the channel's full AnalysisInfo\n\nBecause every channel keeps its own AnalysisResult and AnalysisInfo, you can reach any per-channel step's info exactly as in a single-channel run — see Data Model & Provenance.\n\nFor the concrete configuration, the analyze call shape, the on-disk output layout, and each cross-channel step in detail, see Multi-Channel Analysis.","title":"No provenance is lost"},{"category":"section","location":"steps/detectfit/#Detection-and-Fitting","page":"Detection & Fitting","text":"This is the first step of every pipeline: it turns raw camera frames into localizations. It detects candidate molecules and cuts a small region of interest (ROI) around each (SMLMBoxer), then fits a point-spread-function model to every ROI to recover a sub-pixel position, photon count, background, and per-localization uncertainty (GaussMLE). It is selected by a DetectFitConfig.\n\nanalyze(images, DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130),\n                                fitter = GaussMLEConfig(psf_model = GaussianXYNBS())))\n# → (smld, StepInfo)","title":"Detection & Fitting"},{"category":"section","location":"steps/detectfit/#When-to-use-/-prerequisites","page":"Detection & Fitting","text":"Always first. Detection & fitting is the only step that produces a BasicSMLD from raw image data; every other step consumes and returns an existing smld. Placing another step before it is a MethodError (see The Pipeline Model).\nNeeds a camera. In a full pipeline the camera is injected automatically from AnalysisConfig.camera; for a standalone analyze(images, cfg) call set camera = on the config (or let it be built from a MIC H5 file, below).","title":"When to use / prerequisites"},{"category":"section","location":"steps/detectfit/#Inputs,-returns-and-artifacts","page":"Detection & Fitting","text":"Input — the raw image stack(s), supplied one of three ways:\nIn-memory: pass the images to analyze(images, cfg) — a single 3D array (one dataset) or a Vector of 3D arrays (multiple datasets).\nOne file, split into datasets: DetectFitConfig(path = \"data.h5\", dataset_frames = [...]).\nMultiple files, one per dataset: DetectFitConfig(paths = [\"d1.h5\", \"d2.h5\"]).\nReturns — (smld, StepInfo); the StepInfo.info is a DetectFitInfo (n_rois, n_fits, n_datasets, n_frames_per_dataset, …).\nArtifacts (when outdir is set) — a detection/fit overlay on sample frames and fit-quality diagnostics; at DEBUG verbosity, per-frame detection movies.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/detectfit/#Concept","page":"Detection & Fitting","text":"Two operations run back to back:\n\nDetection (SMLMBoxer). The frames are filtered to enhance diffraction-limited spots and thresholded; local maxima become candidate molecules, and a boxsize×boxsize ROI is cut around each.\nFitting (GaussMLE). Each ROI is fit by maximum likelihood to a PSF model, returning the sub-pixel position, photons, background, and the Cramér–Rao-bound (CRLB) uncertainty of each — the σ_x/σ_y that the rest of the pipeline relies on (see Data Model & Provenance).\n\nThe detailed detection and fitting algorithms live in the SMLMBoxer and GaussMLE documentation; this step composes them and handles datasets, files, and cameras.","title":"Concept"},{"category":"section","location":"steps/detectfit/#Configuration","page":"Detection & Fitting","text":"DetectFitConfig embeds the two upstream configs and adds the data-source and camera options:\n\nfield default meaning\nboxer BoxerConfig(boxsize=11, psf_sigma=0.135) detection config (ROI size, expected PSF σ in µm)\nfitter GaussMLEConfig(psf_model=GaussianXYNBS(), iterations=20) PSF-fit config (model, iterations)\ncamera nothing the camera; injected by AnalysisConfig, or set for standalone use\npath / paths nothing file-based sources (single file / one file per dataset)\ndataset_frames nothing frame ranges that split a single file into datasets\ndatasets nothing subset of source slots to include\nh5_format :auto :auto, :smart, or :mic (see I/O & Resume)\npixel_size, qe nothing, 1.0 build an SCMOS camera from MIC H5 calibration when camera is unset\n\nPSF models (from GaussMLE) include GaussianXYNB, GaussianXYNBS (free width), GaussianXYNBSXSY (elliptical), and AstigmaticXYZNB (3D).\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(\n            boxer  = BoxerConfig(boxsize = 9, psf_sigma = 0.130),\n            fitter = GaussMLEConfig(psf_model = GaussianXYNBS(), iterations = 20)),\n        # … subsequent steps …\n    ],\n)\n(result, info) = analyze(image_stacks, config)\nstepinfo(info, :detectfit).info    # the DetectFitInfo","title":"Configuration"},{"category":"section","location":"steps/detectfit/#Output-and-interpretation","page":"Detection & Fitting","text":"DetectFitInfo reports how detection and fitting went:\n\nfield meaning\nn_rois candidate ROIs found by detection\nn_fits localizations surviving the fit\nn_datasets, n_frames_per_dataset dataset/frame structure of the result\n\nSanity checks: n_fits close to n_rois means most detections fit cleanly; a large gap suggests boxsize/psf_sigma are mismatched to the data or the detection threshold is too low. The overlay figure should show boxes centered on real spots, not noise.","title":"Output & interpretation"},{"category":"section","location":"steps/detectfit/#Notes-and-caveats","page":"Detection & Fitting","text":"Per-dataset processing. Detection and fitting loop over datasets individually, so arbitrarily large acquisitions can be processed memory-efficiently. Frame numbers are per dataset — see Multi-Dataset.\nGPU. GaussMLE fits on the GPU (CUDA) when available; see its docs for GPU/CPU behavior and the per-model parameter sets.\nTune boxsize and psf_sigma to your optics — they are the parameters that most affect detection quality.","title":"Notes & caveats"},{"category":"section","location":"steps/detectfit/#References","page":"Detection & Fitting","text":"Detection. F. Huang, et al. \"Video-rate nanoscopy using sCMOS camera-specific single-molecule localization algorithms.\" Nature Methods 10, 653–658 (2013). doi:10.1038/nmeth.2488\nMLE fitting. C. S. Smith, N. Joseph, B. Rieger, K. A. Lidke. \"Fast, single-molecule localization that achieves theoretically minimum uncertainty.\" Nature Methods 7, 373–375 (2010). doi:10.1038/nmeth.1449\n\nSee the SMLMBoxer and GaussMLE documentation for the detection and fitting algorithms and their full options.","title":"References"},{"category":"section","location":"steps/driftcorrect/#Drift-Correction","page":"Drift Correction","text":"Sample drift — the slow wander of the stage during a long acquisition — blurs a super-resolution reconstruction just as motion blur smears a photograph. The drift-correction step estimates the drift trajectory and subtracts it from every localization. It is selected by a DriftConfig and backed by SMLMDriftCorrection, which corrects drift fiducial-free — no beads required.\n\nanalyze(smld, DriftConfig(degree = 2))   # → (corrected_smld, StepInfo)","title":"Drift Correction"},{"category":"section","location":"steps/driftcorrect/#When-to-use-/-prerequisites","page":"Drift Correction","text":"Run on a BasicSMLD of localizations (after detection/fitting, and usually after a quality filter and frame connection).\nDrift is estimated from the data's own redundancy — the same fixed structures reappear across frames — so it needs enough localizations spread over the acquisition to register against. Very sparse data drifts-correct poorly.","title":"When to use / prerequisites"},{"category":"section","location":"steps/driftcorrect/#Inputs,-returns-and-artifacts","page":"Drift Correction","text":"Input: the current smld.\nReturns: (corrected_smld, StepInfo). The fitted drift model is on StepInfo.info.model, and the pipeline also surfaces it on AnalysisResult.drift_model.\nArtifacts (when outdir is set): drift_trajectory.png (X/Y drift vs. frame and the XY path), stats.md (max drift, inter-dataset shifts, entropy, convergence), and at DETAILED verbosity a per_dataset.md breakdown. A corrected SMLD is checkpointed to smld_corrected.jld2 at Checkpoint.EXPENSIVE.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/driftcorrect/#Concept","page":"Drift Correction","text":"The algorithm models the reconstruction as a Gaussian mixture and searches for the drift trajectory that makes the localizations of fixed structures pile up as tightly as possible — i.e. the trajectory that minimizes the entropy of the rendered cloud (Cnossen et al. 2021). The trajectory itself is expanded in a Legendre-polynomial basis over normalized time, so degree sets how wiggly a drift path the fit can follow. This refines the fiducial-free, redundancy-based approach of Wester et al. 2021. For the full derivation see the SMLMDriftCorrection documentation.","title":"Concept"},{"category":"section","location":"steps/driftcorrect/#Two-modes","page":"Drift Correction","text":"dataset_mode chooses how the datasets within an SMLD relate (see Multi-Dataset):\n\n:continuous — one long, uninterrupted movie. For long acquisitions (≳4000 frames) split the fit into chunks (chunk_frames) or use a single higher-degree polynomial for shorter ones.\nDriftConfig(degree = 3, dataset_mode = :continuous, chunk_frames = 4000)\n:registered — multiple independent acquisitions of the same field of view with stage registration between them (e.g. SeqSRM). Each dataset is corrected independently and then aligned to the others; this requires spatial overlap between datasets.\nDriftConfig(degree = 2, dataset_mode = :registered)","title":"Two modes"},{"category":"section","location":"steps/driftcorrect/#Configuration","page":"Drift Correction","text":"DriftConfig is re-exported from SMLMDriftCorrection (the \"upstream owns the config\" idiom). The fields you set most often:\n\nfield typical meaning\ndegree 2–5 polynomial degree of the drift trajectory; higher = more flexible\ndataset_mode :registered :continuous (one movie) or :registered (independent, overlapping acquisitions)\nquality :singlepass :singlepass (fast) or :iterative (refines to convergence)\nchunk_frames 0 for continuous mode, frames per chunk (≈4000 is reasonable); 0 = single polynomial\nn_chunks 0 alternative to chunk_frames: fixed number of chunks\nauto_roi false estimate drift from a dense ROI subset (faster, stronger signal)\n\nSee the SMLMDriftCorrection documentation for the complete field list and defaults.\n\n# Continuous 8000-frame acquisition, chunked\n(corrected, info) = analyze(smld,\n    DriftConfig(degree = 3, dataset_mode = :continuous,\n                chunk_frames = 4000, auto_roi = true))\n\ndrift = info.info.model                       # the fitted drift model\nSMLMDriftCorrection.drift_trajectory(drift)   # sampled trajectory (µm)","title":"Configuration"},{"category":"section","location":"steps/driftcorrect/#Output-and-interpretation","page":"Drift Correction","text":"The step's StepInfo.summary reports the headline numbers:\n\nfield meaning\nmax_drift_nm largest intra-dataset drift excursion (nm)\nmax_intershift_nm largest inter-dataset shift (nm), multi-dataset only\ndataset_mode, quality the mode/quality actually used\nconverged, iterations for :iterative quality\nentropy, backend final entropy and compute backend\n\nSanity checks: a sensible max_drift_nm is tens to a few hundred nm over a long acquisition; the drift_trajectory.png should look like a smooth wander, not noise. The step warns (at PROGRESS) when inter-dataset shifts exceed ~500 nm — usually a sign the wrong dataset_mode was chosen (e.g. :registered on data acquired as one continuous movie).","title":"Output & interpretation"},{"category":"section","location":"steps/driftcorrect/#Notes-and-caveats","page":"Drift Correction","text":"Mode mismatch is the most common mistake. Continuous data corrected as :registered (or vice versa) produces large spurious inter-dataset shifts; heed the warning and switch modes.\nFrame numbering is per-dataset, which is what lets the Legendre basis normalize each dataset's time to -1 1 — see Multi-Dataset.\nThreads help. Per-dataset correction is parallelized; start Julia with -t auto.","title":"Notes & caveats"},{"category":"section","location":"steps/driftcorrect/#References","page":"Drift Correction","text":"J. Cnossen, T. J. Cui, C. Joo, C. Smith. \"Drift correction in localization microscopy using entropy minimization.\" Optics Express 29, 27961 (2021). doi:10.1364/OE.426620\nM. J. Wester, et al. \"Robust, fiducial-free drift correction for super-resolution imaging.\" Scientific Reports 11, 23672 (2021). doi:10.1038/s41598-021-02850-7\n\nSee the SMLMDriftCorrection documentation for the algorithm in full and all configuration options.","title":"References"},{"category":"section","location":"workflows/multidataset/#Multi-Dataset-Acquisitions","page":"Multi-Dataset","text":"Most real acquisitions are not one continuous movie. A long experiment is split into several blocks — multiple cells, several ROIs on one coverslip, or a single long movie broken into segments. SMLMAnalysis treats these as datasets that live inside one BasicSMLD, processed together but tracked separately. This page covers what a dataset is, why the split matters, and the three ways to feed multi-dataset data into Detection & Fitting.","title":"Multi-Dataset Acquisitions"},{"category":"section","location":"workflows/multidataset/#What-a-\"dataset\"-is","page":"Multi-Dataset","text":"A dataset is an independent acquisition block within a single localization set — a cell, an ROI, or a registered segment of one long movie. Every emitter records which block it came from in its dataset field (see Data Model & Provenance), and the BasicSMLD knows how many blocks it holds. Datasets are not user-numbered: their boundaries come from the shape of the data you supply (one image stack, one file, or one frame range per dataset).","title":"What a \"dataset\" is"},{"category":"section","location":"workflows/multidataset/#Why-split-into-datasets","page":"Multi-Dataset","text":"Memory efficiency. Detection & Fitting loops over datasets one at a time — detect, fit, append, free. With the file-based modes below, only one dataset's images are resident at once, so arbitrarily large acquisitions fit in memory.\nPer-dataset frame numbering. Each emitter's frame is relative to its own dataset (1 to n_frames_per_dataset), not a global frame index. This is required by Drift Correction: the Legendre-polynomial drift basis normalizes each dataset's time axis to [-1, 1], so frame counting must restart at each dataset boundary.\nDataset tracking. Because emitter.dataset is carried through every step, downstream operations (notably drift correction's per-dataset trajectories and inter-dataset alignment) know exactly which block each localization belongs to.","title":"Why split into datasets"},{"category":"section","location":"workflows/multidataset/#SMLD-structure","page":"Multi-Dataset","text":"After detection and fitting, the combined BasicSMLD encodes the split in two fields:\n\nField Meaning\nsmld.n_frames frames per dataset (not the global total)\nsmld.n_datasets number of datasets in the set\nemitter.dataset which dataset (1-based) an emitter belongs to\nemitter.frame frame index within that dataset (1-based)\n\nSo an acquisition of 4 datasets × 2000 frames has n_frames == 2000 and n_datasets == 4; the total of 8000 frames is n_frames * n_datasets.\n\nnote: n_frames is per-dataset\nIf you expect n_frames to be the grand total and see a smaller number, that is correct — it is frames per dataset. Multiply by n_datasets for the total.","title":"SMLD structure"},{"category":"section","location":"workflows/multidataset/#Supplying-multi-dataset-data","page":"Multi-Dataset","text":"DetectFitConfig resolves datasets from whichever data source you provide. There are three modes:\n\nMode How datasets are defined\nIn-memory one 3D image stack per element of a Vector\nSingle file MIC blocks (auto), or explicit dataset_frames ranges\nMultiple files one file per element of paths","title":"Supplying multi-dataset data"},{"category":"section","location":"workflows/multidataset/#In-memory:-a-vector-of-image-stacks","page":"Multi-Dataset","text":"Pass a Vector of H × W × F arrays — each element is one dataset:\n\nimage_stacks = [stack1, stack2, stack3, stack4]   # 4 datasets\n(result, info) = analyze(image_stacks, config)\n\n# Step-by-step is the same input:\n(smld, info) = analyze(image_stacks,\n    DetectFitConfig(camera = cam, boxer = BoxerConfig(boxsize = 9)))\n\nA single 3D array is wrapped automatically into a one-dataset set, so single-acquisition code needs no special handling.","title":"In-memory: a vector of image stacks"},{"category":"section","location":"workflows/multidataset/#Single-file-split-by-frame-range-or-block","page":"Multi-Dataset","text":"Point DetectFitConfig at one H5 file and let it carve the datasets. MIC-format files expose acquisition blocks that auto-detect as datasets; for other formats, give explicit dataset_frames ranges:\n\n# MIC blocks become datasets automatically\nconfig = AnalysisConfig(camera = cam, steps = [\n    DetectFitConfig(path = \"data.h5\", h5_format = :mic,\n                    boxer = BoxerConfig(boxsize = 9)),\n    RenderConfig(zoom = 20),\n])\n(result, info) = analyze(config)          # no data argument — loaded from file\n\n# Or split one stack into explicit per-dataset frame ranges\nDetectFitConfig(path = \"movie.h5\",\n                dataset_frames = [1:2000, 2001:4000, 4001:6000])\n\nh5_format accepts :auto (default), :smart, or :mic. Each source is loaded, processed, and freed before the next — the memory-efficient path for large files.","title":"Single file split by frame range or block"},{"category":"section","location":"workflows/multidataset/#Multiple-files,-one-per-dataset","page":"Multi-Dataset","text":"Give a paths vector to treat each file as its own dataset:\n\nDetectFitConfig(paths = [\"d1.h5\", \"d2.h5\", \"d3.h5\", \"d4.h5\"],\n                boxer = BoxerConfig(boxsize = 9))","title":"Multiple files, one per dataset"},{"category":"section","location":"workflows/multidataset/#Selecting-a-subset-of-datasets","page":"Multi-Dataset","text":"datasets picks a subset of the resolved sources — useful for re-running a few blocks. It applies uniformly across all three modes, and the selected blocks are reindexed to a contiguous 1:length(datasets) in the output:\n\nDetectFitConfig(path = \"data.h5\", h5_format = :mic, datasets = [1, 2, 5])\n\nThe original source indices are preserved in DetectFitInfo.selected_source_indices.","title":"Selecting a subset of datasets"},{"category":"section","location":"workflows/multidataset/#Connecting-to-drift-correction","page":"Multi-Dataset","text":"Multi-dataset structure exists largely to serve Drift Correction, whose two modes map directly onto how the datasets relate:\n\ndataset_mode = :registered (default) — for spatially registered blocks (the stage returns to roughly the same position between datasets). Each dataset gets its own intra-dataset drift polynomial, then inter-dataset alignment via entropy optimization over the spatial overlap.\ndataset_mode = :continuous — for one long acquisition split into segments, where drift accumulates continuously across the dataset boundaries.\n\nSee Drift Correction for the full mode reference, chunking guidance, and quality settings. For how datasets thread through the rest of the pipeline, see The Pipeline Model and Running a Pipeline.","title":"Connecting to drift correction"},{"category":"section","location":"api/io/#Multi-Target-and-I/O","page":"Multi-Target & I/O","text":"","title":"Multi-Target & I/O"},{"category":"section","location":"api/io/#Multi-Target","page":"Multi-Target & I/O","text":"Multi-channel (multi-color) analysis types. The orchestrator config MultiTargetConfig is documented on the Multi-Channel Analysis page; its result, info, and step types are below. See Composite Render / Cross-Alignment / Cross-Correlation for the individual steps.","title":"Multi-Target"},{"category":"section","location":"api/io/#I/O","page":"Multi-Target & I/O","text":"Save and load localization results and pipeline state, and import microscope .h5 data. See I/O & Resume for the workflow.","title":"I/O"},{"category":"section","location":"api/io/#Utilities","page":"Multi-Target & I/O","text":"","title":"Utilities"},{"category":"type","location":"api/io/#SMLMAnalysis.MultiTargetResult","page":"Multi-Target & I/O","text":"MultiTargetResult\n\nResult of a multi-target analysis. Holds per-channel AnalysisResult objects and the final SMLD vectors for composite rendering.\n\nFields\n\nlabels::Vector{Symbol}: Channel labels in order\nsmlds::Vector{SMLMData.BasicSMLD}: Per-channel SMLD results (may be aligned)\nchannels::Dict{Symbol, AnalysisResult}: Per-channel results\nstep_infos::Vector{StepInfo}: Multi-target step history\noutdir::String: Output directory\n\nIndexing\n\nresult[:IgG]         # Access per-channel AnalysisResult\nkeys(result)         # Channel labels\nresult.smlds         # Vector of all SMLDs\n\n\n\n\n\n","title":"SMLMAnalysis.MultiTargetResult"},{"category":"type","location":"api/io/#SMLMAnalysis.MultiTargetInfo","page":"Multi-Target & I/O","text":"MultiTargetInfo <: AbstractSMLMInfo\n\nAggregated metadata from a multi-target analysis.\n\nFields\n\nelapsed_s::Float64: Total elapsed time in seconds\nchannels::Dict{Symbol, AnalysisInfo}: Per-channel analysis info\nstep_infos::Vector{StepInfo}: Multi-target step history (composite renders, alignment, etc.)\n\nLook up a cross-channel step by name with the stepinfo / stepinfos accessors (these search step_infos, not the per-channel channels).\n\n\n\n\n\n","title":"SMLMAnalysis.MultiTargetInfo"},{"category":"type","location":"api/io/#SMLMAnalysis.AbstractMultiTargetStep","page":"Multi-Target & I/O","text":"AbstractMultiTargetStep <: AbstractSMLMConfig\n\nAbstract supertype for steps that operate on Vector{BasicSMLD} in the multi-target pipeline (composite rendering, cross-channel alignment, etc.).\n\n\n\n\n\n","title":"SMLMAnalysis.AbstractMultiTargetStep"},{"category":"type","location":"api/io/#SMLMAnalysis.CompositeRenderConfig","page":"Multi-Target & I/O","text":"CompositeRenderConfig <: AbstractMultiTargetStep\n\nConfiguration for a composite multi-channel render in the multi-target pipeline.\n\nFields\n\nstrategy: Rendering strategy (default: GaussianRender())\nzoom: Zoom factor (default: 20.0)\ncolors: Per-channel colors (nothing = inherit from MultiTargetConfig)\nclip_percentile: Intensity clipping. :auto (default) picks per strategy — saturate (no clip) for histogram, 0.99 for others. A Float64 clips at that percentile; nothing forces saturate mode.\nnormalize_each: Per-channel normalization (nothing = auto: false for histogram, true for others)\nscalebar: Enable scale bar (default: true)\nscalebar_length: Scale bar length in μm (nothing = auto)\nscalebar_position: Scale bar corner (default: :br)\nscalebar_color: Scale bar color (default: :white)\n\n\n\n\n\n","title":"SMLMAnalysis.CompositeRenderConfig"},{"category":"type","location":"api/io/#SMLMAnalysis.CrossAlignConfig","page":"Multi-Target & I/O","text":"CrossAlignConfig <: AbstractMultiTargetStep\n\nConfiguration for cross-channel alignment in the multi-target pipeline.\n\nWraps SMLMDriftCorrection.align_smld which uses entropy-based or FFT cross-correlation alignment.\n\nFields\n\nmethod: Alignment method — :entropy (CC + entropy refinement) or :fft (CC only)\nmaxn: Maximum neighbors for entropy calculation (default: 100)\nhistbinsize: Histogram bin size in μm for cross-correlation (default: 0.05)\n\n\n\n\n\n","title":"SMLMAnalysis.CrossAlignConfig"},{"category":"type","location":"api/io/#SMLMAnalysis.CrossCorrConfig","page":"Multi-Target & I/O","text":"CrossCorrConfig <: AbstractMultiTargetStep\n\nConfiguration for pair cross-correlation g(r) between two channels.\n\nFields\n\nr_max: Maximum correlation distance in μm (default: 1.0)\ndr: Radial bin width in μm (default: 0.01)\nedge_correction: Apply Ripley's isotropic edge correction (default: true)\nchannels: Which channels to correlate, as 1-based indices (default: (1, 2))\n\n\n\n\n\n","title":"SMLMAnalysis.CrossCorrConfig"},{"category":"type","location":"api/io/#SMLMAnalysis.CompositeRenderInfo","page":"Multi-Target & I/O","text":"CompositeRenderInfo <: AbstractSMLMInfo\n\nInfo from a multi-channel composite render step.\n\n\n\n\n\n","title":"SMLMAnalysis.CompositeRenderInfo"},{"category":"type","location":"api/io/#SMLMAnalysis.CrossAlignInfo","page":"Multi-Target & I/O","text":"CrossAlignInfo <: AbstractSMLMInfo\n\nInfo from cross-channel alignment step.\n\n\n\n\n\n","title":"SMLMAnalysis.CrossAlignInfo"},{"category":"type","location":"api/io/#SMLMAnalysis.CrossCorrInfo","page":"Multi-Target & I/O","text":"CrossCorrInfo <: AbstractSMLMInfo\n\nInfo from pair cross-correlation g(r) step.\n\nFields\n\nr::Vector{Float64}: Bin centers (μm)\ng::Vector{Float64}: g(r) values\nn_a::Int: Emitter count in channel A\nn_b::Int: Emitter count in channel B\narea::Float64: FOV area (μm²)\nr_max::Float64: Maximum correlation distance (μm)\ndr::Float64: Bin width (μm)\nchannel_a::Symbol: Label for channel A\nchannel_b::Symbol: Label for channel B\nelapsed_s::Float64: Elapsed time (s)\n\n\n\n\n\n","title":"SMLMAnalysis.CrossCorrInfo"},{"category":"function","location":"api/io/#SMLMAnalysis.save_smld","page":"Multi-Target & I/O","text":"save_smld(filepath::String, smld::BasicSMLD;\n          source_file::Union{String,Nothing}=nothing,\n          drift_model=nothing,\n          compression::Int=3)\n\nSave BasicSMLD to HDF5 file with full metadata for reproducibility.\n\nSupports all emitter types including GaussMLE types with fitted PSF widths:\n\nEmitter2DFitSigma: saves fitted σ (isotropic PSF width)\nEmitter2DFitSigmaXY: saves fitted σx, σy (anisotropic PSF widths)\n\nArguments\n\nfilepath: Output .h5 file path\nsmld: BasicSMLD object to save\nsource_file: Original data file path (for provenance tracking)\ndrift_model: Optional drift correction model (LegendrePolynomial, etc.)\ncompression: HDF5 compression level 0-9 (default: 3)\n\nFile Structure\n\n/metadata           - Format version, package info, timestamps\n/emitters           - Columnar emitter data (x, y, z, photons, psf_sigma_x, etc.)\n/camera             - Camera type and calibration\n/drift_correction   - Drift model coefficients (if provided)\n/provenance         - Source file info\n\nExample\n\nsave_smld(\"results.h5\", smld; source_file=\"/data/experiment.h5\", drift_model=dm)\n\n\n\n\n\n","title":"SMLMAnalysis.save_smld"},{"category":"function","location":"api/io/#SMLMAnalysis.load_smld","page":"Multi-Target & I/O","text":"load_smld(filepath::String) -> BasicSMLD\n\nLoad BasicSMLD from HDF5 file.\n\nReturns a BasicSMLD with the saved emitters, camera, and metadata. Automatically reconstructs the correct emitter type (including GaussMLE types with PSF width fields).\n\nDrift correction info is stored in metadata[\"drift_correction\"] if present.\n\nExample\n\nsmld = load_smld(\"results.h5\")\n\n\n\n\n\n","title":"SMLMAnalysis.load_smld"},{"category":"function","location":"api/io/#SMLMAnalysis.smld_info","page":"Multi-Target & I/O","text":"smld_info(filepath::String)\n\nPrint summary info about an SMLD HDF5 file without loading all data.\n\n\n\n\n\n","title":"SMLMAnalysis.smld_info"},{"category":"function","location":"api/io/#SMLMAnalysis.save_pipeline_state","page":"Multi-Target & I/O","text":"save_pipeline_state(path::String, result::AnalysisResult;\n                    smld_raw=nothing, step_infos=StepInfo[], camera=nothing)\n\nSave pipeline state to JLD2 for cross-session resume.\n\nExample\n\n(result, info) = analyze(image_stacks, config)\nsave_pipeline_state(\"output/checkpoint.jld2\", result;\n    step_infos=info.step_infos, camera=config.camera)\n\n\n\n\n\n","title":"SMLMAnalysis.save_pipeline_state"},{"category":"function","location":"api/io/#SMLMAnalysis.load_pipeline_state","page":"Multi-Target & I/O","text":"load_pipeline_state(path::String) -> NamedTuple\n\nLoad pipeline state from JLD2.\n\nReturns a NamedTuple with fields: smld, smldraw, smldconnected, driftmodel, stepinfos, camera.\n\nExample\n\nstate = load_pipeline_state(\"output/checkpoint.jld2\")\nsmld = state.smld\n# Continue pipeline from here\n(smld, info) = analyze(smld, FilterConfig(photons=(500.0, Inf)))\n\n\n\n\n\n","title":"SMLMAnalysis.load_pipeline_state"},{"category":"function","location":"api/io/#SMLMAnalysis.load_smart_h5","page":"Multi-Target & I/O","text":"load_smart_h5(filepath::String; frame_range=nothing)\n\nLoad image data from a SMART microscope HDF5 file.\n\nArguments\n\nfilepath::String: Path to the HDF5 file\nframe_range: Optional range of frames to load (e.g., 1:1000). If nothing, loads all frames.\n\nReturns\n\ndata::Array{UInt16, 3}: Image data (width, height, frames)\n\nExample\n\n# Load all frames\ndata = load_smart_h5(\"data/experiment.h5\")\n\n# Load first 1000 frames\ndata = load_smart_h5(\"data/experiment.h5\", frame_range=1:1000)\n\n\n\n\n\n","title":"SMLMAnalysis.load_smart_h5"},{"category":"function","location":"api/io/#SMLMAnalysis.load_smart_h5_info","page":"Multi-Target & I/O","text":"load_smart_h5_info(filepath::String)\n\nLoad metadata about a SMART microscope HDF5 file without reading the full dataset.\n\nReturns a NamedTuple with:\n\nfilepath: Full path to the file\nwidth, height, nframes: Image dimensions\ndtype: Data type of the images\nfilesizegb: Approximate file size in GB\n\nExample\n\ninfo = load_smart_h5_info(\"path/to/smart_data.h5\")\nprintln(\"File contains \", info.nframes, \" frames of size \", info.width, \"x\", info.height)\n\n\n\n\n\n","title":"SMLMAnalysis.load_smart_h5_info"},{"category":"function","location":"api/io/#SMLMAnalysis.load_smart_h5_frame","page":"Multi-Target & I/O","text":"load_smart_h5_frame(filepath::String, frame::Int)\n\nLoad a single frame from a SMART microscope HDF5 file.\n\nArguments\n\nfilepath::String: Path to the HDF5 file\nframe::Int: Frame number to load (1-indexed)\n\nReturns\n\nframe_data::Matrix{UInt16}: Single frame image\n\nExample\n\nframe1 = load_smart_h5_frame(\"data/experiment.h5\", 1)\n\n\n\n\n\n","title":"SMLMAnalysis.load_smart_h5_frame"},{"category":"function","location":"api/io/#SMLMAnalysis.smart_h5_to_array","page":"Multi-Target & I/O","text":"smart_h5_to_array(filepath::String; max_frames=nothing)\n\nLoad SMART microscope data as a properly formatted array for SMLM processing.\n\nThis function transposes the data from (width, height, frames) to (height, width, frames) to match standard image conventions where the first dimension is rows (y) and second is columns (x).\n\nArguments\n\nfilepath::String: Path to the HDF5 file\nmax_frames: Optional maximum number of frames to load\n\nReturns\n\ndata::Array{UInt16, 3}: Image data in (rows, cols, frames) format\ninfo::NamedTuple: File metadata\n\nExample\n\ndata, info = smart_h5_to_array(\"data/experiment.h5\", max_frames=1000)\nprintln(\"Loaded \", size(data, 3), \" frames of size \", size(data, 1), \"x\", size(data, 2))\n\n\n\n\n\n","title":"SMLMAnalysis.smart_h5_to_array"},{"category":"function","location":"api/io/#SMLMAnalysis.load_mic_h5","page":"Multi-Target & I/O","text":"load_mic_h5(filepath; max_frames=nothing, max_blocks=nothing) -> images, dataset_indices\n\nLoad MIC dSTORM H5 file. Each block becomes a \"dataset\".\n\nReturns:\n\nimages: 3D array (height × width × n_frames)\ndataset_indices: Vector{Int} mapping each frame to its block (1-indexed)\n\n\n\n\n\n","title":"SMLMAnalysis.load_mic_h5"},{"category":"function","location":"api/io/#SMLMAnalysis.load_mic_h5_info","page":"Multi-Target & I/O","text":"load_mic_h5_info(filepath) -> NamedTuple\n\nGet info about MIC H5 file without loading all data.\n\nReturns NamedTuple with fields:\n\nheight, width: image dimensions\nn_frames: total frames across all blocks\nn_blocks: number of data blocks (datasets)\nframesperblock: Vector of frame counts per block\nhas_calibration: whether calibration data exists\n\n\n\n\n\n","title":"SMLMAnalysis.load_mic_h5_info"},{"category":"function","location":"api/io/#SMLMAnalysis.load_mic_h5_block","page":"Multi-Target & I/O","text":"load_mic_h5_block(filepath, block_num::Int) -> Array{Float32,3}\n\nLoad a single data block from MIC H5 file. block_num is 1-indexed.\n\n\n\n\n\n","title":"SMLMAnalysis.load_mic_h5_block"},{"category":"function","location":"api/io/#SMLMAnalysis.load_mic_h5_calibration","page":"Multi-Target & I/O","text":"load_mic_h5_calibration(filepath) -> NamedTuple\n\nLoad calibration data from MIC H5 file.\n\nReturns NamedTuple with (offset, variance, gain) as 2D arrays.\n\nNOTE: The gain in MIC H5 files is INVERTED compared to our convention. Our convention: ADU = photons * gain, so gain ≈ 0.24 e-/ADU MIC H5: gain stored as ~4, which is 1/gain in our convention Use loadmich5calibrationfor_scmos() to get corrected values.\n\n\n\n\n\n","title":"SMLMAnalysis.load_mic_h5_calibration"},{"category":"function","location":"api/io/#SMLMAnalysis.load_mic_h5_calibration_for_scmos","page":"Multi-Target & I/O","text":"load_mic_h5_calibration_for_scmos(filepath) -> NamedTuple\n\nLoad calibration data and convert to SCMOSCamera convention.\n\nReturns NamedTuple with:\n\noffset: per-pixel offset (unchanged)\nreadnoise: per-pixel readnoise (sqrt of variance)\ngain: per-pixel gain (INVERTED from stored value)\n\n\n\n\n\n","title":"SMLMAnalysis.load_mic_h5_calibration_for_scmos"},{"category":"function","location":"api/io/#SMLMAnalysis.build_camera_from_mic_h5","page":"Multi-Target & I/O","text":"build_camera_from_mic_h5(filepath; pixel_size, qe=1.0) -> SCMOSCamera\n\nBuild an SCMOSCamera from MIC H5 per-pixel calibration data (offset, readnoise, gain).\n\nPixel size and QE are not stored in MIC H5 files and must be provided.\n\nArguments\n\nfilepath: Path to MIC H5 file with Calibration/ group\npixel_size: Pixel size in μm (required)\nqe: Quantum efficiency 0-1 (default: 1.0)\n\n\n\n\n\n","title":"SMLMAnalysis.build_camera_from_mic_h5"},{"category":"function","location":"api/io/#SMLMAnalysis.crop_camera","page":"Multi-Target & I/O","text":"crop_camera(camera::AbstractCamera, roi_x::UnitRange, roi_y::UnitRange)\n\nCrop camera to specified pixel ROI while preserving physical coordinates.\n\nReturns a new camera with:\n\npixeledgesx/y sliced to cover ROI (preserves original μm positions)\nFor SCMOSCamera: offset/gain/readnoise/qe matrices cropped accordingly\n\nArguments\n\ncamera: Original camera\nroi_x: Pixel range in x (columns), e.g., 100:300\nroi_y: Pixel range in y (rows), e.g., 50:200\n\nExample\n\ncam = IdealCamera(512, 512, 0.1)\ncam_cropped = crop_camera(cam, 100:300, 50:200)\n# cam_cropped.pixel_edges_x[1] = 10.0 μm (not 0!)\n\n\n\n\n\n","title":"SMLMAnalysis.crop_camera"},{"category":"function","location":"api/io/#SMLMAnalysis.crop_images","page":"Multi-Target & I/O","text":"crop_images(images::AbstractArray{T,3}, roi_x::UnitRange, roi_y::UnitRange) where T\n\nCrop image stack to specified pixel ROI.\n\nArguments\n\nimages: 3D array (y, x, frames) - Julia convention: [row, col]\nroi_x: Pixel range in x (columns)\nroi_y: Pixel range in y (rows)\n\n\n\n\n\n","title":"SMLMAnalysis.crop_images"},{"category":"function","location":"api/io/#SMLMAnalysis.step_name","page":"Multi-Target & I/O","text":"step_name(cfg::AbstractSMLMConfig) -> String\n\nDerive step name from config type (e.g., FilterConfig → \"filter\", DriftConfig → \"drift\").\n\n\n\n\n\n","title":"SMLMAnalysis.step_name"},{"category":"function","location":"api/io/#SMLMAnalysis.step_outdir","page":"Multi-Target & I/O","text":"step_outdir(outdir, step_number, cfg) -> Union{String, Nothing}\n\nCompute output directory for a step: outdir/02_filter/. Returns nothing if outdir is nothing.\n\n\n\n\n\n","title":"SMLMAnalysis.step_outdir"},{"category":"function","location":"api/io/#SMLMAnalysis.n_datasets","page":"Multi-Target & I/O","text":"n_datasets(ds::DataSource) -> Int\n\nNumber of datasets in this data source.\n\n\n\n\n\n","title":"SMLMAnalysis.n_datasets"},{"category":"function","location":"api/io/#SMLMAnalysis.n_frames_per_dataset","page":"Multi-Target & I/O","text":"n_frames_per_dataset(ds::DataSource) -> Int\n\nFrames per dataset. For Vector{Array}, uses first element.\n\n\n\n\n\n","title":"SMLMAnalysis.n_frames_per_dataset"},{"category":"section","location":"steps/render/#Rendering","page":"Rendering","text":"A localization list is not an image — the rendering step turns the BasicSMLD into a super-resolution picture by accumulating every emitter onto a finely subdivided pixel grid. It is selected by a RenderConfig and backed by SMLMRender, which offers several rendering strategies, intensity- or field-based coloring, and PNG export.\n\nanalyze(smld, RenderConfig(zoom = 20, colormap = :inferno))   # → (smld, StepInfo)\n\nRendering is a pass-through: it writes the image to disk and returns the input smld unchanged. That is deliberate — a render can sit anywhere in the pipeline and be repeated at different zooms or colormaps without altering the data flowing to later steps. The pipeline treats it as a non-SMLD-producing step.","title":"Rendering"},{"category":"section","location":"steps/render/#When-to-use-/-prerequisites","page":"Rendering","text":"Run on a BasicSMLD of localizations — typically as the final step, after detection/fitting, a quality filter, frame connection, and drift correction.\nBecause it does not consume or transform the data, it is freely repeatable: drop in several RenderConfig steps (different strategies, zoom levels, colormaps, or color_by fields) and each writes its own image.\nTo capture the image array in code (rather than only on disk), call SMLMRender.render(smld, cfg) or the step helper render_step(smld, cfg) directly — both return (image, RenderInfo).","title":"When to use / prerequisites"},{"category":"section","location":"steps/render/#Inputs,-returns-and-artifacts","page":"Rendering","text":"Input: the current smld.\nReturns: (smld, StepInfo) — the same smld passes through. The RenderInfo (output size, pixel size, strategy, color mode) is on StepInfo.info.\nArtifacts (when outdir is set): a PNG written into the step folder with a descriptive name built from the run — <strategy>_<colormap>_<color_by>_<zoom>x.png (e.g. gaussianrender_inferno_20x.png); config.toml and info.toml; and at STANDARD verbosity a stats.md. No checkpoint is written (nothing changes). If you set RenderConfig(filename = ...) yourself, that path is used instead of the auto-generated name.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/render/#Concept","page":"Rendering","text":"Each localization is placed on an output grid finer than the camera pixels. The strategy decides what gets drawn at each position; the coloring decides what value maps to color. See the SMLMRender documentation for the details of each.\n\nStrategies (strategy): HistogramRender() (fast binning), GaussianRender() (smooth blobs, the default), CircleRender() (anti-aliased circles at localization precision), and EllipseRender().\nColoring: intensity-based — accumulate counts and apply colormap (:inferno, :hot, :magma); or field-based — set color_by to an emitter field (:z, :photons, :frame, :σ_x) and color by that value with a perceptual map like :turbo or :viridis. CircleRender/EllipseRender require color_by or a manual color.\nResolution: zoom renders the exact camera FOV with subdivided pixels (zoom=20 on a 128×128 camera → 2560×2560), giving predictable, reproducible sizes; pixel_size (in nm) instead uses the data bounds with a margin. roi (zoom mode) renders a subset of the FOV as camera pixel ranges.","title":"Concept"},{"category":"section","location":"steps/render/#Configuration","page":"Rendering","text":"RenderConfig is re-exported from SMLMRender (the \"upstream owns the config\" idiom). Its fields map 1:1 to render() keyword arguments. The ones you set most:\n\nfield typical/default meaning\nstrategy GaussianRender() rendering algorithm: HistogramRender, GaussianRender, CircleRender, EllipseRender\nzoom 20 (nothing) subdivide camera pixels by this factor; renders exact FOV\npixel_size nothing output pixel size in nm (data-bounds mode); alternative to zoom\nroi nothing render a camera-pixel subregion, (x_range, y_range) (zoom mode)\ncolormap :inferno (nothing) intensity colormap\ncolor_by nothing emitter field for field-based coloring (:z, :photons, :frame)\nclip_percentile 0.99 intensity clip before normalizing; nothing = saturate mode\ncategorical false categorical palette for integer fields\nscalebar false overlay a scale bar\nscalebar_length nothing scale-bar length in µm (nothing = auto)\nscalebar_position :br corner: :br, :bl, :tr, :tl\nbackend :cpu compute backend (:cpu, :cuda, :metal)\n\nSee the SMLMRender documentation for the full field list (target, color, field_range, field_clip_percentiles, scalebar_color, …) and defaults.\n\n# Two renders of the same SMLD — both pass smld through unchanged\n(smld, _) = analyze(smld, RenderConfig(zoom = 20, colormap = :inferno, scalebar = true))\n(smld, _) = analyze(smld, RenderConfig(zoom = 10, color_by = :photons, colormap = :viridis))\n\n# Get the image array directly\n(img, rinfo) = SMLMRender.render(smld, RenderConfig(zoom = 20))","title":"Configuration"},{"category":"section","location":"steps/render/#Output-and-interpretation","page":"Rendering","text":"The step's StepInfo.summary reports:\n\nfield meaning\nn_locs emitters actually rendered (RenderInfo.n_emitters_rendered)\nstrategy strategy used (:gaussian, :histogram, :circle, :ellipse)\noutput_size (height, width) of the output image in pixels\n\nstats.md additionally records the output pixel size (nm), color mode, and render time. Sanity checks: output_size should equal zoom × the camera dimensions (within the roi) in zoom mode; n_locs should match the input localization count unless some fell outside the rendered FOV. If a histogram render looks washed out, lower clip_percentile (or set it to nothing for saturate mode); for field coloring, confirm the chosen color_by field exists on the emitters.","title":"Output & interpretation"},{"category":"section","location":"steps/render/#Notes-and-caveats","page":"Rendering","text":"Pass-through, not a transform. The returned smld is the input; later steps see the data exactly as before. Position the render anywhere and repeat it freely.\nNo checkpoint. Because nothing changes, no SMLD checkpoint is written for this step.\nCoordinates are in microns; zoom/roi are expressed in camera pixels, pixel_size/scalebar_length in nm/µm respectively.\nGPU rendering is available via backend = :cuda / :metal for large outputs.","title":"Notes & caveats"},{"category":"section","location":"steps/render/#References","page":"Rendering","text":"No external citation — super-resolution histogram/Gaussian rendering is a standard SMLM visualization. See the SMLMRender documentation for the rendering strategies, coloring modes, and all configuration options.","title":"References"},{"category":"section","location":"steps/edgeclassify/#edgeclassify-step","page":"Edge Classification","text":"Cellular SMLM data has a boundary: localizations belong to a cell interior, sit on its membrane, or fall outside it in the surrounding background. The edge classification step carves one or more cell masks directly from the localization cloud and labels every localization :interior / :membrane / :outside. It is backed by SMLMClustering and, like clustering, is selected entirely by the type of its config:\n\nKdeValleyConfig — the dSTORM-tuned default. Finds the cell boundary from a KDE valley between the dense interior and the sparse background.\nOuterPolygonConfig — carves the outer ring of the dominant cell as a polygon.\n\nBoth are <: AbstractEdgeClassifyConfig, re-exported from SMLMClustering as const aliases, so they are available unqualified after using SMLMAnalysis.\n\n(smld, info) = analyze(smld, KdeValleyConfig())     # → (smld, StepInfo)\n(smld, info) = analyze(smld, OuterPolygonConfig())","title":"Edge Classification"},{"category":"section","location":"steps/edgeclassify/#Non-destructive-by-design","page":"Edge Classification","text":"The step is state-modifying but emitter-preserving: the returned SMLD wraps the same emitters — nothing is filtered out. The authoritative per-emitter class is not written back onto the emitters (a per-emitter side-list would desync the moment a downstream step subsets emitters); it lives in the step's EdgeClassifyInfo, reached at the classify point:\n\n(smld, step_info) = analyze(smld, KdeValleyConfig())\ninfo = step_info.info                 # ::EdgeClassifyInfo\ninfo.class                            # per-emitter Vector{Symbol}\ninterior_mask(info)                   # BitVector of the :interior emitters\nin_cell(info)                         # :interior ∪ :membrane\ninterior_fraction(info)               # scalar summary\n\nOnly the cell-mask geometry travels downstream, mirrored into metadata:\n\nmetadata[\"edge_cells\"] — the MultiCellMask (Vector{CellPolygon}, largest-first)\nmetadata[\"edge_outer_polygon\"] — the dominant cell's outer ring (back-compat)\n\nA later spatial-statistics step reads that geometry as its observation region — e.g. HopkinsConfig uses edge_cells / edge_outer_polygon to restrict its clustering tendency estimate to inside the cell.","title":"Non-destructive by design"},{"category":"section","location":"steps/edgeclassify/#When-to-use","page":"Edge Classification","text":"Run late in the pipeline, on a cleaned BasicSMLD, when downstream analysis must distinguish cell interior from membrane or background — e.g. restricting a Hopkins clustering-tendency estimate to the cell, or reporting an interior fraction.\nBecause it removes nothing, it is safe to run before clustering or rendering; the emitters and their ordering are unchanged.","title":"When to use"},{"category":"section","location":"steps/edgeclassify/#Outputs","page":"Edge Classification","text":"At verbose >= STANDARD with an outdir, the step writes config.toml, info.toml, and an edge report (diagnostics plus figures) under {step}_edge_classify/. The StepInfo summary records the gate :method, emitter counts, and the number of cells.\n\nSee the SMLMClustering documentation for the algorithm details and the full per-config parameter list.","title":"Outputs"},{"category":"section","location":"steps/edgeclassify/#References","page":"Edge Classification","text":"Edge classification is a method native to SMLMClustering (KdeValleyConfig / OuterPolygonConfig). The outer-cell boundary is built from an alpha-shape / concave-hull of the localization cloud; the Voronoi-area density that the KDE-valley gate builds on is that of SR-Tesseler:\n\nF. Levet, et al. \"SR-Tesseler: a method to segment and quantify localization-based super-resolution microscopy data.\" Nature Methods 12, 1065–1071 (2015). doi:10.1038/nmeth.3579\n\nSee the SMLMClustering edge-classification docs for the boundary-extraction algorithms and their references.","title":"References"},{"category":"section","location":"steps/intensityfilter/#Intensity-Filter","page":"Intensity Filter","text":"When two fluorophores are active at once inside a single diffraction-limited spot, the fitter sees one blob and returns a single localization with a corrupted position and an abnormally high photon count. The intensity filter rejects these multi-emitter events with a statistical test on brightness. It is a step native to SMLMAnalysis, selected by an IntensityFilterConfig.\n\nanalyze(smld, IntensityFilterConfig(cutoff = 0.01))   # → (filtered_smld, StepInfo)","title":"Intensity Filter"},{"category":"section","location":"steps/intensityfilter/#When-to-use-/-prerequisites","page":"Intensity Filter","text":"Run on a fitted BasicSMLD whose emitters carry photons (i.e. after Detection & Fitting).\nMost valuable on denser data, where simultaneous activations within one PSF are common (e.g. high-density dSTORM/PAINT). On very sparse data it has little to reject.\nIt needs enough localizations to estimate the excitation field — see min_bin_count below.","title":"When to use / prerequisites"},{"category":"section","location":"steps/intensityfilter/#Inputs,-returns-and-artifacts","page":"Intensity Filter","text":"Input — the current smld.\nReturns — (filtered_smld, StepInfo); the StepInfo.info is an IntensityFilterInfo.\nArtifacts (when outdir is set) — diagnostic figures of the fitted excitation field and the per-localization p-value distribution, plus the usual config.toml / info.toml.","title":"Inputs, returns & artifacts"},{"category":"section","location":"steps/intensityfilter/#Concept","page":"Intensity Filter","text":"A single emitter does not emit a fixed number of photons — the count is Poisson, and its mean varies across the field of view with the excitation intensity. The filter accounts for both:\n\nEstimate the expected single-emitter rate, spatially. The field of view is binned (n_bins per axis); in each bin the rate_percentile (default 95th) of the photon distribution is taken as the upper bound of single-emitter emission there — high enough to sit above typical single-emitter counts but below true doubles. A smooth field model is then fit across bins: a spatially-varying Gaussian beam (field_mode = :gaussian) or a single global rate (:uniform).\nTest each localization. Given its position's expected rate λ, a Poisson upper-tail test asks how improbable the observed photon count is under single-emitter emission. Localizations with p-value below cutoff are rejected as multi-emitter events.\n\nOptionally (estimate_p2), the filter also estimates p₂, the fraction of double-emitter events, by decomposing the bright tail of the photon distribution — a useful diagnostic of how crowded the data is.","title":"Concept"},{"category":"section","location":"steps/intensityfilter/#Configuration","page":"Intensity Filter","text":"field default meaning\ncutoff 0.01 p-value cutoff; localizations with p < cutoff are rejected\nfield_mode :gaussian excitation-field model: :gaussian (spatially-varying beam) or :uniform (one global rate)\nn_bins 10 spatial grid bins per axis for field estimation\nmin_bin_count 30 minimum localizations per bin to estimate its rate\nrate_percentile 0.95 per-bin percentile taken as the single-emitter rate; higher = more permissive\nestimate_p2 true also estimate the double-emitter fraction p₂\np2_tail_threshold, p2_n_bins 1.0, 200 p₂ tail-decomposition parameters\n\n(filtered, info) = analyze(smld,\n    IntensityFilterConfig(cutoff = 0.01, field_mode = :gaussian, rate_percentile = 0.95))\n\ninfo.info.p2_estimate    # estimated double-emitter fraction (or nothing)","title":"Configuration"},{"category":"section","location":"steps/intensityfilter/#Output-and-interpretation","page":"Intensity Filter","text":"IntensityFilterInfo reports:\n\nfield meaning\nn_before, n_after localizations in and surviving\nfield_mode the field model actually used\nlambda_max_global peak expected single-emitter rate (photons)\nfield_fit_r2 goodness of the field fit\np2_estimate estimated fraction of double-emitter events (or nothing)\np2_tail_obs, p2_tail_f2 observed vs. modeled bright-tail mass\n\nSanity checks: field_fit_r2 should be high for a clean :gaussian fit — if it is poor, the beam assumption may not hold and :uniform is the safer choice. A large p2_estimate signals crowded data where multi-emitter rejection matters most; if it is near zero, the filter is mostly a no-op and can be dropped.","title":"Output & interpretation"},{"category":"section","location":"steps/intensityfilter/#Notes-and-caveats","page":"Intensity Filter","text":"Native, in-house method — the per-bin-percentile + smooth-field + Poisson-tail approach is implemented in SMLMAnalysis; there is no separate upstream package for it.\nField estimation needs data. With too few localizations per bin (below min_bin_count) the field estimate is unreliable; reduce n_bins or fall back to field_mode = :uniform.\nRepeatable. Like the other filters, it can appear more than once in a pipeline, though once is typical.","title":"Notes & caveats"},{"category":"section","location":"steps/intensityfilter/#References","page":"Intensity Filter","text":"This is a method native to SMLMAnalysis; there is no separate primary publication. See the API Reference for the full IntensityFilterConfig and IntensityFilterInfo field lists.","title":"References"},{"category":"section","location":"workflows/io_resume/#I/O-and-Resume","page":"I/O & Resume","text":"This page covers getting data in and results out: importing raw microscope acquisitions, saving and reloading localizations, persisting full pipeline state for cross-session resume, and the per-step checkpoints that let you iterate on later steps without re-running expensive ones.","title":"I/O & Resume"},{"category":"section","location":"workflows/io_resume/#Saving-and-loading-localizations-(HDF5)","page":"I/O & Resume","text":"A finished (or intermediate) BasicSMLD is saved to a self-describing HDF5 file with save_smld and read back with load_smld:\n\nsave_smld(\"results.h5\", result.smld;\n          source_file = \"/data/experiment.h5\",   # recorded in /provenance\n          drift_model = result.drift_model)       # optional, embedded in the file\nsmld = load_smld(\"results.h5\")\n\nThe file stores emitter columns (positions and uncertainties in microns, photons, background, frame/dataset/track ids), the camera and its calibration, and optional provenance. save_smld also takes compression (HDF5 deflate level 0–9, default 3). load_smld reconstructs the correct emitter type automatically, including the GaussMLE σ-fitting types (Emitter2DFitSigma, Emitter2DFitSigmaXY) and 3D (Emitter3DFit).\n\nTo inspect a file without loading all emitters, use smld_info, which prints the format version, emitter type, counts, PSF model, save timestamp, and available emitter fields:\n\nsmld_info(\"results.h5\")","title":"Saving and loading localizations (HDF5)"},{"category":"section","location":"workflows/io_resume/#Saving-and-resuming-full-pipeline-state-(JLD2)","page":"I/O & Resume","text":"HDF5 holds one BasicSMLD. To snapshot the whole pipeline — final SMLD, the raw SMLD, the frame-connected tracks, the drift model, and step provenance — use the JLD2-backed save_pipeline_state / load_pipeline_state:\n\n(result, info) = analyze(image_stacks, config)\nsave_pipeline_state(\"output/pipeline.jld2\", result;\n                    step_infos = info.step_infos,\n                    camera     = config.camera)\n\nload_pipeline_state returns a NamedTuple you can read fields from or feed back into analyze to continue from where you left off:\n\nstate = load_pipeline_state(\"output/pipeline.jld2\")\nstate.smld             # final BasicSMLD\nstate.smld_connected   # frame-connected tracks (or nothing)\nstate.drift_model      # fitted drift model (or nothing)\nstate.step_infos       # Vector{StepInfo}\n\n(smld, _) = analyze(state.smld, FilterConfig(photons = (300.0, Inf)))","title":"Saving and resuming full pipeline state (JLD2)"},{"category":"section","location":"workflows/io_resume/#Step-checkpoints-during-a-run","page":"I/O & Resume","text":"When AnalysisConfig.outdir is set, the pipeline can drop each step's output BasicSMLD as a JLD2 file inside that step's subdirectory, so you can resume from any step or sweep a later step's parameters without re-running the upstream work. What gets written is controlled by the checkpoint level (see Running a Pipeline for the level semantics and how to set it):\n\nLevel Constant Writes\n0 Checkpoint.NONE nothing\n1 Checkpoint.END only the final SMLD-producing step\n2 Checkpoint.EXPENSIVE (default) DetectFit, FrameConnect, Drift, BaGoL\n3 Checkpoint.ALL every SMLD-producing step (filters included)\n\nEach step uses a stable filename, so you always know where to look:\n\nStep Checkpoint file Written at\nDetection & Fitting smld_raw.jld2 EXPENSIVE\nFrame Connection smld_combined.jld2 EXPENSIVE\nDrift Correction smld_corrected.jld2 (drift model embedded) EXPENSIVE\nBaGoL smld_bagol.jld2 EXPENSIVE\nFilter smld_filtered.jld2 ALL\nIntensity Filter smld_intensity.jld2 ALL\nDensity Filter smld_density.jld2 ALL\nClustering smld_clustered.jld2 ALL\n\nThe SMLD is stored under the smld key, so reload it with JLD2 and pick up the pipeline from there:\n\nusing JLD2\nsmld = JLD2.load(\"output/01_detectfit/smld_raw.jld2\")[\"smld\"]   # full BasicSMLD\n(smld, _) = analyze(smld, FilterConfig(photons = (500.0, Inf)))\n\nThe default EXPENSIVE level guarantees no costly step ever runs producing only figures — its SMLD is always on disk for downstream iteration.","title":"Step checkpoints during a run"},{"category":"section","location":"workflows/io_resume/#Importing-microscope-data","page":"I/O & Resume","text":"","title":"Importing microscope data"},{"category":"section","location":"workflows/io_resume/#SMART-microscope-H5","page":"I/O & Resume","text":"The SMART format keeps the image stack under /Main/data. Load the full stack (or a frame range), peek at metadata only, or load a properly oriented array:\n\nimages = load_smart_h5(\"acquisition.h5\")                 # (width, height, frames)\nimages = load_smart_h5(\"acquisition.h5\"; frame_range = 1:1000)\ninfo   = load_smart_h5_info(\"acquisition.h5\")            # width, height, nframes, dtype, file_size_gb\ndata, info = smart_h5_to_array(\"acquisition.h5\"; max_frames = 1000)  # transposed to (rows, cols, frames)","title":"SMART microscope H5"},{"category":"section","location":"workflows/io_resume/#MIC-H5","page":"I/O & Resume","text":"The MIC (MATLAB Instrument Control) format stores one or more data blocks, each treated as a dataset, plus optional per-pixel sCMOS calibration. Pixel size and QE are not stored and must be supplied when building a camera:\n\nimages, dataset_indices = load_mic_h5(\"experiment.h5\")   # images (h×w×frames), block index per frame\ninfo  = load_mic_h5_info(\"experiment.h5\")                # n_frames, n_blocks, frames_per_block, has_calibration\nblock = load_mic_h5_block(\"experiment.h5\", 1)            # one block, memory-efficient\ncam   = build_camera_from_mic_h5(\"experiment.h5\"; pixel_size = 0.1, qe = 0.9)  # SCMOSCamera from calibration","title":"MIC H5"},{"category":"section","location":"workflows/io_resume/#Loading-directly-in-DetectFitConfig","page":"I/O & Resume","text":"For large acquisitions you usually do not load images into memory yourself. DetectFitConfig reads files directly via path (one file) or paths (one file per dataset), with h5_format = :auto (default), :smart, or :mic; MIC blocks are auto-detected as datasets. With h5_format = :mic, camera = nothing, and pixel_size/qe set, the camera is built from the MIC calibration automatically.\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps  = [DetectFitConfig(path = \"experiment.h5\", h5_format = :mic,\n                              boxer = BoxerConfig(boxsize = 9)), RenderConfig(zoom = 20)],\n)\n(result, info) = analyze(config)   # no in-memory data argument needed","title":"Loading directly in DetectFitConfig"},{"category":"section","location":"workflows/io_resume/#Output-directory-layout","page":"I/O & Resume","text":"With outdir set, every step writes a numbered subdirectory — outdir/01_detectfit/, outdir/02_filter/, outdir/05_driftcorrect/, … — each containing config.toml (the exact config used), info.toml (scalar fields of the upstream info struct), stats.md (a human-readable summary), any verbosity-gated figures, and the checkpoint SMLD described above. A top-level outdir/config.toml records the camera, ROI, verbosity, checkpoint level, and the full ordered step list. See Data Model & Provenance for what each holds.","title":"Output directory layout"},{"category":"section","location":"steps/#Pipeline-Steps:-Overview","page":"Overview","text":"This section documents every analysis step — one page per analyze() step. Each step is selected by the type of its config (see The Pipeline Model), runs in the order you list it in AnalysisConfig.steps, and returns an updated SMLD plus a StepInfo. This page is the map: the catalog, the typical order, and where each method comes from in the literature.","title":"Pipeline Steps: Overview"},{"category":"section","location":"steps/#Catalog","page":"Overview","text":"Step Config Backed by What it does\nDetection & Fitting DetectFitConfig SMLMBoxer + GaussMLE Detect ROIs in raw frames and fit a PSF to each → the initial localizations\nQuality Filter FilterConfig SMLMAnalysis Threshold on photons, precision, p-value, PSF width, z\nIntensity Filter IntensityFilterConfig SMLMAnalysis Reject multi-emitter events via a Poisson upper-tail test\nFrame Connection FrameConnectConfig SMLMFrameConnection Link repeated blinks into single, higher-precision localizations\nDrift Correction DriftConfig SMLMDriftCorrection Estimate and remove sample drift (fiducial-free)\nDensity Filter DensityFilterConfig SMLMAnalysis Drop isolated localizations by neighbor count\nRendering RenderConfig SMLMRender Render localizations to a super-resolution image\nBayesian Grouping BaGoLConfig SMLMBaGoL Group localizations into true emitters (RJMCMC)\nClustering DBSCANConfig, VoronoiConfig, HopkinsConfig, … SMLMClustering Label clusters / compute spatial statistics\nEdge Classification KdeValleyConfig, OuterPolygonConfig SMLMClustering Label localizations interior / membrane / outside a cell mask\nComposite Render CompositeRenderConfig SMLMAnalysis + SMLMRender Multi-channel RGB overlay\nCross-Alignment CrossAlignConfig SMLMAnalysis + SMLMDriftCorrection Register channels to a common frame\nCross-Correlation CrossCorrConfig SMLMAnalysis + NearestNeighbors Pair cross-correlation g(r) between channels\n\nThe last three operate on multiple channels (Vector{BasicSMLD}) and are usually driven by MultiTargetConfig — see Multi-Channel.","title":"Catalog"},{"category":"section","location":"steps/#A-typical-pipeline","page":"Overview","text":"A standard single-color dSTORM / DNA-PAINT analysis runs roughly in this order:\n\nsteps = [\n    DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130),\n                    fitter = GaussMLEConfig(psf_model = GaussianXYNBS())),\n    FilterConfig(photons = (500.0, Inf)),     # cut weak / spurious fits\n    FrameConnectConfig(max_frame_gap = 5),     # link repeated blinks\n    DriftConfig(degree = 2),                   # remove drift\n    RenderConfig(zoom = 20, colormap = :inferno),\n]\n\nCommon variations:\n\nAdd an Intensity Filter early to reject multi-emitter events.\nAdd a Density Filter after drift correction to clean isolated noise.\nReplace/augment the final render with Bayesian Grouping for counting and maximum precision, then render the grouped emitters.\nAdd a Clustering step to label structures, then render colored by label.\n\nSteps are composable (see The Pipeline Model): reorder, repeat, or omit them as the experiment requires.","title":"A typical pipeline"},{"category":"section","location":"steps/#Primary-literature","page":"Overview","text":"Each method's primary reference, as published by the backing package. Full citations are on the References page; each step page repeats its own reference and links to the upstream documentation for algorithm details.\n\nStep Method Primary reference\nDetection Gaussian-filter local-max + sCMOS-aware thresholding Huang et al., Nat. Methods 10, 653 (2013)\nFitting MLE Gaussian/astigmatic PSF; CRLB precision Smith et al., Nat. Methods 7, 373 (2010)\nQuality filter χ²/LLR goodness-of-fit p-value Huang et al., Biomed. Opt. Express 2, 1377 (2011)\nFrame connection Spatiotemporal LAP clustering of repeated blinks Schodt & Lidke, Front. Bioinform. (2021)\nDrift correction Fiducial-free entropy minimization (Legendre basis) Cnossen et al., Opt. Express 29, 27961 (2021); Wester et al., Sci. Rep. 11, 23672 (2021)\nBayesian grouping Collapsed reversible-jump MCMC grouping Fazel et al., Nat. Commun. 13, 7152 (2022)\nClustering DBSCAN; HDBSCAN; Voronoi (SR-Tesseler); Hopkins tendency Ester et al., KDD-96 (1996); Campello et al., PAKDD (2013); Levet et al., Nat. Methods 12, 1065 (2015); Hopkins & Skellam, Ann. Bot. 18, 213 (1954)\nCross-correlation Pair-correlation g(r) between channels Sengupta et al., Nat. Methods 8, 969 (2011); Veatch et al., PLoS ONE 7, e31457 (2012)\n\nThe native SMLMAnalysis steps — Quality Filter, Intensity Filter, Density Filter, and the multi-channel steps — are described in full on their own pages, including the method and any reference.","title":"Primary literature"},{"category":"section","location":"#SMLMAnalysis.jl","page":"Home","text":"(Image: Left: a diffraction-limited image. Right: the super-resolution reconstruction, resolving 8-mer rings.)\n\nThe same single-molecule localizations rendered at the microscope's diffraction-limited resolution (left) and at the localization precision SMLMAnalysis achieves (right) — simulated 8-mer ring structures.\n\nSMLMAnalysis is the entry point to the JuliaSMLM ecosystem. It turns raw single-molecule localization microscopy (SMLM) movies into super-resolution results by orchestrating the ecosystem's specialized packages — detection, fitting, filtering, frame connection, drift correction, rendering, Bayesian grouping, and clustering — behind one consistent interface:\n\n(result, info) = analyze(data, config)\n\nYou describe what analysis you want as a list of typed step configs; the package runs them in order, threads the data through, writes diagnostics and provenance to disk, and hands back the final localizations plus a record of everything that happened. Every step is backed by a dedicated, separately documented JuliaSMLM package — SMLMAnalysis is the layer that composes them into a reproducible pipeline.","title":"SMLMAnalysis.jl"},{"category":"section","location":"#How-it-works","page":"Home","text":"(Image: The analyze() pipeline: one call runs an ordered list of typed step configs; each step returns (smld, info), and optional steps drop in by adding their config.)\n\nAn SMLM pipeline is a sequence of transformations on a localization set. In SMLMAnalysis each transformation is a step, configured by a typed struct (DetectFitConfig, FilterConfig, DriftConfig, …). A pipeline is just an ordered vector of those configs:\n\nusing SMLMAnalysis\n\nconfig = AnalysisConfig(\n    camera = cam,\n    steps = [\n        DetectFitConfig(boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130),\n                        fitter = GaussMLEConfig(psf_model = GaussianXYNBS())),\n        FilterConfig(photons = (500.0, Inf)),\n        FrameConnectConfig(max_frame_gap = 5),\n        DriftConfig(degree = 2),\n        RenderConfig(zoom = 20, colormap = :inferno),\n    ],\n    outdir = \"output/\",\n)\n\n(result, info) = analyze(image_stacks, config)\nresult.smld          # final localizations (a SMLMData.BasicSMLD)\ninfo.step_infos      # per-step history; stepinfo(info, :detectfit) looks one up by name\n\nThe same steps can be run one at a time for interactive exploration — every analyze(smld, cfg) call returns a (smld, info) tuple you can inspect before deciding the next step:\n\n(smld, _) = analyze(image_stacks, DetectFitConfig(camera = cam,\n                        boxer = BoxerConfig(boxsize = 9, psf_sigma = 0.130)))\n(smld, _) = analyze(smld, FilterConfig(photons = (500.0, Inf)))\n(smld, _) = analyze(smld, RenderConfig(zoom = 20, colormap = :inferno))  # render step is a pass-through\n(img,  _) = render(smld, RenderConfig(zoom = 20, colormap = :inferno))   # get the image array directly\n\nSee The Pipeline Model for why this dispatch design makes steps freely composable, and Pipeline Steps for the catalog of everything analyze() can do.","title":"How it works"},{"category":"section","location":"#Installation","page":"Home","text":"Once SMLMAnalysis is registered in the Julia General registry:\n\nusing Pkg\nPkg.add(\"SMLMAnalysis\")\n\nUntil then (and for development against the local ecosystem), see Installation & Setup for the current install path.","title":"Installation"},{"category":"section","location":"#The-ecosystem","page":"Home","text":"SMLMData (core types)\n    |\n    +-- SMLMBoxer ............ ROI detection\n    +-- GaussMLE ............. GPU-accelerated MLE fitting\n    +-- SMLMFrameConnection .. linking blinks + uncertainty calibration\n    +-- SMLMDriftCorrection .. fiducial-free drift correction\n    +-- SMLMRender ........... super-resolution rendering\n    +-- SMLMBaGoL ............ Bayesian grouping of localizations\n    +-- SMLMClustering ....... DBSCAN / Hierarchical / Voronoi / Hopkins\n    +-- SMLMSim .............. simulation + image generation\n    +-- MicroscopePSFs ....... PSF models\n    |\n    +-- SMLMAnalysis ......... integrates all of the above\n\nSMLMAnalysis depends on each of these and exposes their key types directly (so using SMLMAnalysis is usually all you need). It also adds several analysis steps of its own — quality filtering, intensity-based multi-emitter rejection, density filtering, and multi-channel composite rendering / cross-alignment / cross-correlation — that do not live in any single upstream package. See The JuliaSMLM Ecosystem for who does what and when to reach for a package directly.","title":"The ecosystem"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Run it now → Getting Started — a full pipeline with figures at every step.\nUnderstand the design → Concepts — the pipeline model, the ecosystem map, and the data model.\nDay-to-day tasks → Workflows — installing, running, multi-dataset and multi-channel acquisitions, I/O and resume, and adding your own step.\nPer-step reference → Pipeline Steps — one page per analyze() step, each with its primary literature.\n\n","title":"Where to go next"},{"category":"section","location":"tutorial/#Tutorial","page":"Getting Started","text":"This walkthrough runs a complete SMLM analysis pipeline on simulated data, showing the output at every step. All figures are generated from the scripts in examples/ — the core walkthrough uses analysis_example.jl and stepwise_example.jl; the Bayesian Grouping (BaGoL) and Multi-Target (Multi-Color) sections below use bagol_example.jl and multicolor_example.jl.","title":"Tutorial"},{"category":"section","location":"tutorial/#Setup","page":"Getting Started","text":"using SMLMAnalysis\nusing MicroscopePSFs\n\n# Camera: 256x128 pixels @ 100nm\ncamera = IdealCamera(256, 128, 0.1)\n\n# Simulate: 4 datasets x 2000 frames, 8-mer pattern\nsim_params = StaticSMLMConfig(density=2.0, σ_psf=0.13, nframes=2000, ndatasets=4)\npattern = Nmer2D(n=8, d=0.05)\nfluor = GenericFluor(photons=50000.0, k_off=20.0, k_on=0.02)\n\n(_, sim_info) = simulate(sim_params; pattern=pattern, molecule=fluor, camera=camera)\n\nImage generation uses gen_images from SMLMSim with a Gaussian PSF model. See the example scripts for the full setup including the per-dataset image generation workaround.","title":"Setup"},{"category":"section","location":"tutorial/#AnalysisConfig-Approach","page":"Getting Started","text":"The primary interface uses AnalysisConfig to define a complete pipeline:\n\nconfig = AnalysisConfig(\n    camera = camera,\n    steps = [\n        DetectFitConfig(\n            boxer=BoxerConfig(boxsize=7, psf_sigma=0.13),\n            fitter=GaussMLEConfig(psf_model=GaussianXYNBS(), iterations=20)),\n        FilterConfig(photons=(500.0, Inf), precision=(0.0, 0.007), pvalue=(1e-3, 1.0)),\n        FrameConnectConfig(max_frame_gap=5, calibration=CalibrationConfig(clamp_k_to_one=true)),\n        DriftConfig(degree=2),\n        DensityFilterConfig(n_sigma=2.0),\n        RenderConfig(zoom=20, colormap=:inferno),\n    ],\n    outdir = \"output/\",\n    verbose = Verbosity.STANDARD\n)\n\n# image_stacks is Vector{Array}: 4 datasets, each (height, width, 2000)\n(result, info) = analyze(image_stacks, config)\n\nThe analyze function returns (AnalysisResult, AnalysisInfo) following the JuliaSMLM tuple-pattern. Each step produces diagnostic outputs in the output directory.","title":"AnalysisConfig Approach"},{"category":"section","location":"tutorial/#Step-1:-Detection-Fitting","page":"Getting Started","text":"DetectFitConfig combines ROI detection (SMLMBoxer) and MLE fitting (GaussMLE) into a single step. For multi-dataset data (Vector{Array}), each dataset is processed independently.\n\nDetectFitConfig(\n    boxer = BoxerConfig(boxsize=7, min_photons=500.0, psf_sigma=0.13),\n    fitter = GaussMLEConfig(psf_model=GaussianXYNBS(), iterations=20),\n)\n\nDataset boundaries come from the data structure: Vector{Array} of length 4 = 4 datasets.","title":"Step 1: Detection + Fitting"},{"category":"section","location":"tutorial/#Detection-overlay","page":"Getting Started","text":"Shows detected ROIs overlaid on raw camera frames. Yellow boxes mark each candidate molecule:\n\n(Image: Detection overlay)","title":"Detection overlay"},{"category":"section","location":"tutorial/#Fit-quality-distributions","page":"Getting Started","text":"Generated by the filter step: histograms of photons, background, localization precision, p-value, and PSF width from the raw (pre-filter) data. Gray regions show what will be rejected by the filter thresholds:\n\n(Image: Fit quality)","title":"Fit quality distributions"},{"category":"section","location":"tutorial/#Step-2:-Filtering","page":"Getting Started","text":"FilterConfig applies quality-based filtering. All criteria use (min, max) tuples:\n\nFilterConfig(\n    photons = (500.0, Inf),      # Minimum 500 photons\n    precision = (0.0, 0.007),    # Maximum 7nm precision\n    pvalue = (1e-3, 1.0),       # Goodness-of-fit threshold\n    psf_sigma = :auto            # Optional: mode +/- 10%\n)\n\nThe acceptance rate (typically 60-90% for well-optimized detection) is reported in the step summary.","title":"Step 2: Filtering"},{"category":"section","location":"tutorial/#Step-3:-Frame-Connection-(with-uncertainty-calibration)","page":"Getting Started","text":"FrameConnectConfig links localizations of the same emitter across consecutive frames. Uncertainty calibration is integrated as a sub-config via the calibration field — when set, the linked tracks are used to fit the CRLB-vs-observed-variance model before the per-track combine step (link → calibrate → combine).\n\nFrameConnectConfig(\n    max_frame_gap = 5,                                  # Allow gaps of up to 5 dark frames\n    max_sigma_dist = 5.0,                               # Spatial matching threshold\n    calibration = CalibrationConfig(clamp_k_to_one=true),  # k >= 1 (CRLB is theoretical lower bound)\n)\n\nThe calibration fits observed_variance = A + B * CRLB_variance, giving sigma_motion = sqrt(A) (extra motion/jitter variance) and k = sqrt(B) (CRLB scale factor). After calibration, each emitter's uncertainty is corrected to sqrt(sigma_motion^2 + k^2 * sigma_CRLB^2) and tracks are recombined with weighted averaging using corrected uncertainties.\n\nCalibration results are available on the returned info:\n\n(smld, fc_info) = analyze(smld, FrameConnectConfig(max_frame_gap=5,\n    calibration=CalibrationConfig(clamp_k_to_one=true)))\n\ncal = fc_info.info.calibration   # FrameConnectInfo.calibration::CalibrationResult\ncal.k_scale                       # k\ncal.sigma_motion_nm               # sqrt(A) in nm\ncal.mean_chi2                     # ~2.0 means well-calibrated","title":"Step 3: Frame Connection (with uncertainty calibration)"},{"category":"section","location":"tutorial/#Track-size-distribution","page":"Getting Started","text":"Shows the number of localizations per track. The mean track length reflects blinking kinetics (k_off):\n\n(Image: Track histogram)","title":"Track size distribution"},{"category":"section","location":"tutorial/#Drift-jitter","page":"Getting Started","text":"Frame-to-frame position shifts estimated from linked emitters. Shows both instantaneous jitter and cumulative drift across all datasets:\n\n(Image: Drift jitter)","title":"Drift jitter"},{"category":"section","location":"tutorial/#Uncertainty-calibration","page":"Getting Started","text":"Compares reported variance (CRLB) against observed variance from frame-to-frame scatter. The fit gives A (motion variance in nm^2) and B (CRLB scale factor):\n\n(Image: Uncertainty calibration)","title":"Uncertainty calibration"},{"category":"section","location":"tutorial/#Step-4:-Drift-Correction","page":"Getting Started","text":"DriftConfig corrects sample drift using entropy-based optimization from SMLMDriftCorrection.\n\nDriftConfig(\n    degree = 2,                  # Polynomial degree\n    dataset_mode = :registered,  # Registered mode (multi-dataset)\n    quality = :singlepass        # :singlepass or :iterative\n)","title":"Step 4: Drift Correction"},{"category":"section","location":"tutorial/#Drift-trajectory","page":"Getting Started","text":"Shows the estimated X and Y drift over time and the XY path. For multi-dataset data, each dataset's trajectory is shown in a different color:\n\n(Image: Drift trajectory)\n\nFor details on continuous vs registered modes and chunking strategies, see Drift Correction.","title":"Drift trajectory"},{"category":"section","location":"tutorial/#Step-5:-Density-Filter","page":"Getting Started","text":"DensityFilterConfig removes isolated localizations that lack neighbors, which are likely false detections.\n\nDensityFilterConfig(\n    n_sigma = 2.0,           # Search radius in sigma units\n    min_neighbors = :auto    # Auto-detect threshold via valley method\n)","title":"Step 5: Density Filter"},{"category":"section","location":"tutorial/#Neighbor-histogram","page":"Getting Started","text":"The valley between the isolated peak (low neighbors) and the clustered peak (high neighbors) determines the threshold:\n\n(Image: Neighbor histogram)","title":"Neighbor histogram"},{"category":"section","location":"tutorial/#Step-6:-Render","page":"Getting Started","text":"RenderConfig generates super-resolution images using SMLMRender. Multiple renders can be added as separate pipeline steps.\n\n# Gaussian blur rendering (publication quality)\nRenderConfig(zoom=20, colormap=:inferno)\n\n# Histogram binning colored by time\nRenderConfig(strategy=HistogramRender(), zoom=10, colormap=:turbo, color_by=:absolute_frame, clip_percentile=nothing)\n\n# Circle rendering colored by time\nRenderConfig(strategy=CircleRender(), zoom=50, colormap=:turbo, color_by=:absolute_frame)","title":"Step 6: Render"},{"category":"section","location":"tutorial/#Gaussian-render","page":"Getting Started","text":"(Image: Gaussian render)","title":"Gaussian render"},{"category":"section","location":"tutorial/#Histogram-render","page":"Getting Started","text":"(Image: Histogram render)","title":"Histogram render"},{"category":"section","location":"tutorial/#Circle-render","page":"Getting Started","text":"(Image: Circle render)","title":"Circle render"},{"category":"section","location":"tutorial/#Step-by-Step-Workflow","page":"Getting Started","text":"The analyze() dispatch enables iterative parameter tuning:\n\n# Run detection + fitting (expensive step)\n(smld, df_info) = analyze(image_stacks, DetectFitConfig(\n    camera=camera, boxer=BoxerConfig(boxsize=7, psf_sigma=0.13));\n    outdir=\"output/\", step_number=1, verbose=Verbosity.STANDARD)\n\n# Save for later resume\nsave_smld(\"output/after_detectfit.h5\", smld)\n\n# Try filter parameters\n(smld_filtered, _) = analyze(smld, FilterConfig(photons=(500.0, Inf));\n    outdir=\"output/\", step_number=2, verbose=Verbosity.STANDARD)\n\n# Not happy? Try looser filter on same smld (no re-detection needed)\n(smld_filtered, _) = analyze(smld, FilterConfig(photons=(300.0, Inf)))\n\n# Continue pipeline (calibration is a sub-config of FrameConnectConfig)\n(smld_fc, fc_info) = analyze(smld_filtered, FrameConnectConfig(max_frame_gap=5,\n    calibration=CalibrationConfig(clamp_k_to_one=true)))\ncal = fc_info.info.calibration   # CalibrationResult\n(smld_dc, dc_info) = analyze(smld_fc, DriftConfig(degree=2))\n(smld_dc, _) = analyze(smld_dc, RenderConfig(zoom=20, colormap=:inferno))  # pass-through; writes image to outdir\n(img, _) = render(smld_dc, RenderConfig(zoom=20, colormap=:inferno))       # in-memory image array\n\n# Resume from saved checkpoint in new session\nsmld = load_smld(\"output/after_detectfit.h5\")\n(smld, _) = analyze(smld, FilterConfig(photons=(400.0, Inf)))","title":"Step-by-Step Workflow"},{"category":"section","location":"tutorial/#Output-Directory-Structure","page":"Getting Started","text":"When outdir is set, each step writes to a numbered subdirectory:\n\noutput/\n├── 01_detectfit/\n│   ├── config.toml\n│   ├── info.toml\n│   ├── stats.md\n│   └── detection_overlay.png\n├── 02_filter/\n│   ├── config.toml\n│   ├── stats.md\n│   ├── fit_quality.png\n│   └── fit_overlay.png\n├── 03_frameconnect/\n│   ├── config.toml\n│   ├── info.toml\n│   ├── stats.md\n│   ├── track_histogram.png\n│   ├── drift_jitter.png\n│   ├── shift_histogram.png\n│   └── uncertainty_calibration.png   # when calibration=CalibrationConfig()\n├── 04_driftcorrect/\n│   ├── config.toml\n│   ├── info.toml\n│   ├── stats.md\n│   └── drift_trajectory.png\n├── 05_densityfilter/\n│   ├── config.toml\n│   ├── info.toml\n│   ├── stats.md\n│   └── neighbor_histogram.png\n├── 06_render/\n│   ├── config.toml\n│   ├── info.toml\n│   └── gaussianrender_inferno_20x.png\n└── summary.md","title":"Output Directory Structure"},{"category":"section","location":"tutorial/#tutorial-bagol","page":"Getting Started","text":"When each emitter blinks or rebinds many times — as in DNA-PAINT, or dSTORM with long acquisitions — every binding site produces a cloud of localizations. BaGoL (Bayesian Grouping of Localizations) groups that cloud into a single high-precision emitter using reversible-jump MCMC. It runs after frame connection (and any filtering), before the final render.\n\nThe examples/bagol_example.jl script simulates a DNA-PAINT hexamer — 6 binding sites on a 40 nm circle — producing ~10 localizations per site over 4000 frames. The BaGoL step is a single analyze() call whose fields pass straight through to SMLMBaGoL.run_bagol:\n\n(smld_bagol, step_info) = analyze(smld, BaGoLConfig(\n    μ = 10.0,                     # Expected localizations per binding site\n    shape = 2.0,                  # NegBin shape (1 = dSTORM, >1 = DNA-PAINT)\n    learn_distribution = true,    # Let the MCMC refine μ and shape\n    n_iterations = 10000,\n    burn_in = 2000,\n    partition_sigma = 3.0,        # DBSCAN pre-partition threshold (σ units)\n    posterior_pixel_size = 0.002, # 2 nm posterior image (0.0 to disable)\n))\n\nbagol = step_info.info\nbagol.n_emitters    # number of grouped emitters (MAP-N)\nbagol.compression   # localizations-to-emitters ratio\nbagol.final_μ       # refined mean localizations per emitter\n\nBaGoL is state-modifying: smld_bagol replaces smld for any downstream render. Its diagnostics are written to the step folder (08_bagol/) — the 2 nm posterior image, partition circles, the localization-vs-MAP-N overlay, and MCMC acceptance rates — using SMLMBaGoL's own report system rather than inline rendering.","title":"Bayesian Grouping (BaGoL)"},{"category":"section","location":"tutorial/#Count-distribution","page":"Getting Started","text":"The grouping fits a negative-binomial model to the number of localizations per emitter. The recovered μ and shape α summarize the blinking/rebinding kinetics:\n\n(Image: Count distribution)","title":"Count distribution"},{"category":"section","location":"tutorial/#Nearest-neighbor-distances","page":"Getting Started","text":"After grouping, the distances between neighboring MAP-N emitters recover the underlying geometry — here the ~19 nm edge spacing of the simulated hexamer:\n\n(Image: Nearest-neighbor distances)\n\nSuper-resolution rendering of the grouped emitters is done with a subsequent RenderConfig step (precision-weighted Gaussian). For the full field reference — partitioning, the count model, learned vs. fixed distributions — see Bayesian Grouping (BaGoL).","title":"Nearest-neighbor distances"},{"category":"section","location":"tutorial/#tutorial-multitarget","page":"Getting Started","text":"To analyze multiple color channels and overlay them, run each channel through its own AnalysisConfig, then tie them together with a MultiTargetConfig. The multi-target steps (composite render, cross-alignment, cross-correlation) dispatch on the resulting Vector{BasicSMLD}.\n\nexamples/multicolor_example.jl analyzes two channels — Nmer2D clusters and Line2D filaments — each with a full DetectFit → Filter → FrameConnect → Drift → Render pipeline, then composites them:\n\nmt = MultiTargetConfig(\n    labels = [:clusters, :lines],\n    colors = [:cyan, :magenta],   # default for 2 channels\n    steps = [\n        CompositeRenderConfig(zoom=20.0, strategy=GaussianRender()),\n        CrossCorrConfig(r_max=0.5, dr=0.005),\n    ],\n    outdir = \"output/multicolor/\",\n)\n\n(result, info) = analyze([\n    (image_stacks_clusters, config_clusters),\n    (image_stacks_lines,    config_lines),\n], mt)\n\nresult[:clusters].smld     # per-channel SMLD\nresult.smlds               # Vector{BasicSMLD} (all channels, possibly aligned)\ninfo.channels[:clusters]   # per-channel AnalysisInfo","title":"Multi-Target (Multi-Color)"},{"category":"section","location":"tutorial/#Composite-render","page":"Getting Started","text":"CompositeRenderConfig renders each channel in its assigned color and overlays them — cyan clusters over magenta filaments:\n\n(Image: Composite render)","title":"Composite render"},{"category":"section","location":"tutorial/#Cross-correlation","page":"Getting Started","text":"CrossCorrConfig computes the pair cross-correlation g(r) between two channels. A peak above the complete-spatial-randomness baseline (g = 1) at short range indicates co-localization:\n\n(Image: Cross-correlation g(r))\n\nFor per-channel output layout, cross-alignment, and the full step reference, see Multi-Channel Analysis.","title":"Cross-correlation"},{"category":"section","location":"api/internals/#Internals","page":"Internals","text":"Non-exported helpers. These are implementation details and may change between releases — they are documented here for contributors. See Extending the Pipeline for the public extension points.","title":"Internals"},{"category":"type","location":"api/internals/#SMLMAnalysis.TeeLogger","page":"Internals","text":"TeeLogger(loggers)\n\nLogger that forwards messages to multiple loggers. Used internally to tee @info output to both console and log file.\n\n\n\n\n\n","title":"SMLMAnalysis.TeeLogger"},{"category":"method","location":"api/internals/#SMLMAnalysis._apply_roi-Tuple{Vector{<:AbstractArray{<:Real, 3}}, Any}","page":"Internals","text":"Apply ROI cropping to data.\n\n\n\n\n\n","title":"SMLMAnalysis._apply_roi"},{"category":"method","location":"api/internals/#SMLMAnalysis._auto_camera-Tuple{DetectFitConfig}","page":"Internals","text":"_auto_camera(cfg::DetectFitConfig) -> AbstractCamera\n\nResolve camera from DetectFitConfig. Uses cfg.camera if set, otherwise auto-builds from MIC H5 calibration when pixel_size is provided.\n\n\n\n\n\n","title":"SMLMAnalysis._auto_camera"},{"category":"method","location":"api/internals/#SMLMAnalysis._calc_inter_shifts-Tuple{Any}","page":"Internals","text":"Calculate inter-dataset shift magnitudes in nm (Euclidean distance)\n\n\n\n\n\n","title":"SMLMAnalysis._calc_inter_shifts"},{"category":"method","location":"api/internals/#SMLMAnalysis._calc_max_drift-Tuple{Any, Any}","page":"Internals","text":"Calculate max intra-dataset drift using drift_trajectory\n\n\n\n\n\n","title":"SMLMAnalysis._calc_max_drift"},{"category":"method","location":"api/internals/#SMLMAnalysis._calculate_mode-Union{Tuple{Vector{T}}, Tuple{T}} where T<:Real","page":"Internals","text":"_calculate_mode(values; n_bins=100)\n\nCalculate the mode of a distribution using histogram binning. Returns the center of the most populated bin.\n\nUses a median-centered range (median ± 3×MAD) to avoid outlier peaks at large fitted PSF sigma pulling the mode away from the true peak.\n\n\n\n\n\n","title":"SMLMAnalysis._calculate_mode"},{"category":"method","location":"api/internals/#SMLMAnalysis._columnar_to_smld-Tuple{Any}","page":"Internals","text":"Reconstruct SMLD from columnar format.\n\n\n\n\n\n","title":"SMLMAnalysis._columnar_to_smld"},{"category":"method","location":"api/internals/#SMLMAnalysis._compute_bin_stat-Tuple{Any, Any, Any, IntensityFilterConfig, Any}","page":"Internals","text":"_compute_bin_stat(xs, ys, values, cfg, stat_fn) -> Matrix{Float64}\n\nBin emitters spatially and compute stat_fn(bin_values) per bin. Returns n_bins × n_bins grid in Makie convention: grid[ix, iy] maps to (x_centers[ix], y_centers[iy]) when used with heatmap!(ax, x_centers, y_centers, grid).\n\nThis is the fundamental convention: first array index = x-axis (horizontal), second = y-axis (vertical). Julia arrays are (row, col) = (y, x) in image convention, but Makie's heatmap maps first index to x. So we store as [ix, iy].\n\n\n\n\n\n","title":"SMLMAnalysis._compute_bin_stat"},{"category":"method","location":"api/internals/#SMLMAnalysis._compute_crosscorr-Tuple{BasicSMLD, BasicSMLD, CrossCorrConfig}","page":"Internals","text":"_compute_crosscorr(smld_a, smld_b, cfg) -> (r, g, area)\n\nCompute pair cross-correlation g(r) between two SMLDs using KDTree + Ripley's edge correction.\n\n\n\n\n\n","title":"SMLMAnalysis._compute_crosscorr"},{"category":"method","location":"api/internals/#SMLMAnalysis._compute_intensity_pvalues-NTuple{4, Any}","page":"Internals","text":"_compute_intensity_pvalues(xs, ys, photons, field_model)\n\nCompute Poisson upper-tail p-values for each emitter. p = P(X ≥ N) where X ~ Poisson(λ) and λ = field_model(x, y).\n\nλ is the per-bin percentile (upper bound of single-emitter emission at this position), so emitters near or below λ get p ≈ 1 (kept), while emitters far above λ get small p.\n\n\n\n\n\n","title":"SMLMAnalysis._compute_intensity_pvalues"},{"category":"method","location":"api/internals/#SMLMAnalysis._construct_emitters-Tuple{String, Type, Int64, Bool, Vararg{Any, 24}}","page":"Internals","text":"_construct_emitters(...)\n\nInternal: Construct emitters of the appropriate type based on emittertypestr.\n\n\n\n\n\n","title":"SMLMAnalysis._construct_emitters"},{"category":"method","location":"api/internals/#SMLMAnalysis._convolve_1d-Tuple{Vector{Float64}}","page":"Internals","text":"_convolve_1d(h::Vector{Float64}) -> Vector{Float64}\n\nSelf-convolution of histogram h. Returns vector of length 2n-1 where n = length(h). The result represents the distribution of the sum of two independent draws from h.\n\n\n\n\n\n","title":"SMLMAnalysis._convolve_1d"},{"category":"method","location":"api/internals/#SMLMAnalysis._default_colors-Tuple{Int64}","page":"Internals","text":"_default_colors(n::Int) -> Vector{Symbol}\n\nDefault color palette for multi-target rendering. 2 channels: cyan/magenta. 3 channels: cyan/magenta/yellow. Supports up to 6 channels; provide explicit colors for more.\n\n\n\n\n\n","title":"SMLMAnalysis._default_colors"},{"category":"method","location":"api/internals/#SMLMAnalysis._detect_h5_format-Tuple{String}","page":"Internals","text":"Detect H5 file format by checking internal structure\n\n\n\n\n\n","title":"SMLMAnalysis._detect_h5_format"},{"category":"method","location":"api/internals/#SMLMAnalysis._empty_emitters-Tuple{String, Type, Bool}","page":"Internals","text":"_empty_emitters(emitter_type_str, T, is_3d)\n\nInternal: Create empty emitter vector of appropriate type.\n\n\n\n\n\n","title":"SMLMAnalysis._empty_emitters"},{"category":"method","location":"api/internals/#SMLMAnalysis._estimate_bleaching_rate-Tuple{Vector{Int64}}","page":"Internals","text":"_estimate_bleaching_rate(frame_counts) -> NamedTuple or nothing\n\nFit N(t) = a + b*exp(-k*t) to a per-frame localization count vector via linearized initial guess + Nelder-Mead refinement.\n\nThis is the OBSERVED decay rate, not the true kbleach for GenericFluor: bleaching only occurs from the On state, so `kobserved = kbleach * Pon, wherePon = kon/(kon+koff)is the duty cycle. To get true k_bleach:kbleach = kobserved / P_on`.\n\nReturns (k_bleach, N_0, offset, half_life, r_squared, valid_frames, smoothed) or nothing if the fit fails.\n\n\n\n\n\n","title":"SMLMAnalysis._estimate_bleaching_rate"},{"category":"method","location":"api/internals/#SMLMAnalysis._estimate_excitation_field-Tuple{Any, Any, Any, Any, IntensityFilterConfig, Any}","page":"Internals","text":"_estimate_excitation_field(xs, ys, photons, lambda_global, cfg, verbose)\n\nEstimate excitation field from per-bin percentiles of photon counts.\n\nEach spatial bin computes quantile(photons, rate_percentile) independently, capturing the local upper bound of single-emitter emission. A Gaussian field model is then fit to these per-bin rates to produce a smooth field_model(x, y).\n\nReturns (actual_mode, field_params, r2, field_model).\n\n\n\n\n\n","title":"SMLMAnalysis._estimate_excitation_field"},{"category":"method","location":"api/internals/#SMLMAnalysis._estimate_koff-Tuple{Vector{Int64}}","page":"Internals","text":"Estimate koff from track length distribution. For geometric distribution: mean = 1/koff, so k_off = 1/mean\n\n\n\n\n\n","title":"SMLMAnalysis._estimate_koff"},{"category":"method","location":"api/internals/#SMLMAnalysis._estimate_p2-Tuple{Any, Any, Any, Any, IntensityFilterConfig}","page":"Internals","text":"_estimate_p2(xs, ys, photons, field_model, cfg) -> (p2, tail_obs, tail_f2)\n\nEstimate the double-emitter fraction p₂ (info-only) from field-normalized photons.\n\np2 is produced by cfg.p2_method:\n\n:mixture (default) — threshold-free mixture fit (_estimate_p2_mixture); unbiased.\n:tail — legacy tail-ratio tail_obs / tail_f2 at cfg.p2_tail_threshold; biased high.\n\ntail_obs / tail_f2 are always returned as tail-mass diagnostics regardless of method. Returns (nothing, nothing, nothing) if there are too few localizations to estimate.\n\n\n\n\n\n","title":"SMLMAnalysis._estimate_p2"},{"category":"method","location":"api/internals/#SMLMAnalysis._estimate_p2_mixture-Tuple{Any, IntensityFilterConfig}","page":"Internals","text":"_estimate_p2_mixture(normalized, cfg) -> Union{Float64, Nothing}\n\nThreshold-free double-emitter fraction via a two-component mixture fit of the field-normalized photon distribution:\n\nh(x) = (1 - p)·f_single(x) + p·f_double(x),   f_double = f_single ⊛ f_single\n\nf_single is modelled as a Gamma density (2 shape/scale parameters); f_double is its numerical self-convolution (_convolve_1d). The three parameters (k, θ, p) are fit by maximum likelihood (multinomial NLL of the binned counts) via Nelder-Mead, and p — the mixing weight — is returned directly as the double fraction, clamped to [0, 1].\n\nUnlike the legacy tail-ratio estimate this uses the whole distribution: the Gamma tail is pinned by the bulk, so any excess near x ≈ 2 (where singles are scarce) is attributed to doubles rather than assumed away. It therefore removes the ~1 - rate_percentile floor and returns ≈0 when there are no doubles.\n\nNote: identifiability requires a peaked single-emitter distribution (mode away from 0). For a near-exponential single (Gamma shape ≈ 1) a small double fraction is partly absorbable by the Gamma shape and may be under-estimated. Returns nothing if the fit fails.\n\n\n\n\n\n","title":"SMLMAnalysis._estimate_p2_mixture"},{"category":"method","location":"api/internals/#SMLMAnalysis._field_normalized_photons-NTuple{4, Any}","page":"Internals","text":"_field_normalized_photons(xs, ys, photons, field_model) -> Vector{Float64}\n\nField-normalized photon counts n_i = photons_i / max(1, λ(x_i, y_i)). Under a well-fit field model, single emitters cluster near 1 and genuine doubles near 2.\n\n\n\n\n\n","title":"SMLMAnalysis._field_normalized_photons"},{"category":"method","location":"api/internals/#SMLMAnalysis._fit_box_color-Tuple{Any, FilterConfig, Any}","page":"Internals","text":"Determine box color based on FilterConfig thresholds. All-nothing filters = all green.\n\n\n\n\n\n","title":"SMLMAnalysis._fit_box_color"},{"category":"method","location":"api/internals/#SMLMAnalysis._fit_gaussian_field-NTuple{5, Any}","page":"Internals","text":"_fit_gaussian_field(cx, cy, rates, xs, ys)\n\nFit N(x,y) = A·exp(-2·((x-x₀)² + (y-y₀)²)/w²) + bg to per-bin rates.\n\nReturns (params::NamedTuple, r2::Float64) or (nothing, 0.0) on failure.\n\n\n\n\n\n","title":"SMLMAnalysis._fit_gaussian_field"},{"category":"method","location":"api/internals/#SMLMAnalysis._fit_overlay_title-Tuple{FilterConfig}","page":"Internals","text":"Build title string from actual FilterConfig thresholds.\n\n\n\n\n\n","title":"SMLMAnalysis._fit_overlay_title"},{"category":"method","location":"api/internals/#SMLMAnalysis._fmt_corr-Tuple{Any}","page":"Internals","text":"Format a possibly-nothing numeric value for stats output.\n\n\n\n\n\n","title":"SMLMAnalysis._fmt_corr"},{"category":"method","location":"api/internals/#SMLMAnalysis._get_gaussmle_emitter_types-Tuple{}","page":"Internals","text":"_get_gaussmle_emitter_types()\n\nInternal: Try to get GaussMLE emitter types if the package is loaded. Returns nothing if GaussMLE is not available.\n\n\n\n\n\n","title":"SMLMAnalysis._get_gaussmle_emitter_types"},{"category":"method","location":"api/internals/#SMLMAnalysis._get_psf_sigma_bounds-Tuple{Any, Vector}","page":"Internals","text":"_get_psf_sigma_bounds(range_spec, values) -> (lo, hi)\n\nCalculate PSF sigma filter bounds.\n\n:auto → mode ± 10%\n(min, max) → explicit bounds in microns\n\n\n\n\n\n","title":"SMLMAnalysis._get_psf_sigma_bounds"},{"category":"method","location":"api/internals/#SMLMAnalysis._grid_figure_size-Tuple{Any}","page":"Internals","text":"_grid_figure_size(data; n_cols=4, n_rows=3, panel_height=200)\n\nCalculate figure size for grid overlay plots based on data aspect ratio.\n\n\n\n\n\n","title":"SMLMAnalysis._grid_figure_size"},{"category":"method","location":"api/internals/#SMLMAnalysis._inject_camera-Tuple{DetectFitConfig, SMLMData.AbstractCamera}","page":"Internals","text":"_inject_camera(cfg::DetectFitConfig, camera::AbstractCamera) -> DetectFitConfig\n\nInject camera into DetectFitConfig if not already set. Used by AnalysisConfig pipeline.\n\n\n\n\n\n","title":"SMLMAnalysis._inject_camera"},{"category":"method","location":"api/internals/#SMLMAnalysis._is_config_struct-Tuple{Any}","page":"Internals","text":"Check if a value is a config-like struct (has fields, not a primitive/collection).\n\n\n\n\n\n","title":"SMLMAnalysis._is_config_struct"},{"category":"method","location":"api/internals/#SMLMAnalysis._load_source-Tuple{Any, Any}","page":"Internals","text":"Load images from a data source\n\n\n\n\n\n","title":"SMLMAnalysis._load_source"},{"category":"method","location":"api/internals/#SMLMAnalysis._normalize_data-Tuple{Vector{<:AbstractArray{<:Real, 3}}}","page":"Internals","text":"Normalize input data to Vector{AbstractArray{<:Real,3}} for uniform processing.\n\n\n\n\n\n","title":"SMLMAnalysis._normalize_data"},{"category":"method","location":"api/internals/#SMLMAnalysis._p2_tail_masses-Tuple{Any, IntensityFilterConfig}","page":"Internals","text":"_p2_tail_masses(normalized, cfg) -> (tail_obs, tail_f2)\n\nDiagnostic tail masses at τ = cfg.p2_tail_threshold: the observed mass above τ and the self-convolution (double-emitter) mass above τ, using a [0, p99.5] histogram with cfg.p2_n_bins bins. These populate IntensityFilterInfo and back the legacy :tail estimator. Returns (nothing, nothing) on failure.\n\n\n\n\n\n","title":"SMLMAnalysis._p2_tail_masses"},{"category":"function","location":"api/internals/#SMLMAnalysis._plan_sample_frames","page":"Internals","text":"_plan_sample_frames(ds_frame_counts::Vector{Int}, n_samples::Int=12)\n\nPrecompute which frames to sample across all datasets for overlay plots. Always includes first and last absolute frames, with equal spacing between.\n\nReturns (plan, abs_frames):\n\nplan::Dict{Int, Vector{Int}}: dataset index → local frame indices to capture\nabs_frames::Vector{Int}: absolute frame numbers for display labels (in order)\n\n\n\n\n\n","title":"SMLMAnalysis._plan_sample_frames"},{"category":"method","location":"api/internals/#SMLMAnalysis._prepare_step-Tuple{AbstractSMLMConfig, SMLMData.AbstractCamera}","page":"Internals","text":"Pre-dispatch hook: inject pipeline-level config into step configs.\n\n\n\n\n\n","title":"SMLMAnalysis._prepare_step"},{"category":"method","location":"api/internals/#SMLMAnalysis._produces_smld-Tuple{AbstractSMLMConfig}","page":"Internals","text":"_find_final_smld_step(steps) -> Int\n\nWalk steps in reverse and return the 1-based index of the last step that produces a BasicSMLD (i.e., not a terminal render). Returns 0 if no SMLD-producing step is found.\n\nUsed by _run_pipeline to translate Checkpoint.END into a per-step \"this is the final, save it\" decision.\n\n\n\n\n\n","title":"SMLMAnalysis._produces_smld"},{"category":"method","location":"api/internals/#SMLMAnalysis._resolve_colors-Tuple{CompositeRenderConfig, Vector{Symbol}}","page":"Internals","text":"_resolve_colors(cfg::CompositeRenderConfig, defaults::Vector{Symbol}) -> Vector{Symbol}\n\nUse per-step colors if specified, otherwise fall back to MultiTargetConfig defaults.\n\n\n\n\n\n","title":"SMLMAnalysis._resolve_colors"},{"category":"method","location":"api/internals/#SMLMAnalysis._resolve_file_sources-Tuple{DetectFitConfig}","page":"Internals","text":"Resolve file-based sources from config options.\n\nReturns a vector of NamedTuples describing each dataset to load. Dataset count is determined from the data structure, not from a config field.\n\n\n\n\n\n","title":"SMLMAnalysis._resolve_file_sources"},{"category":"method","location":"api/internals/#SMLMAnalysis._resolve_psf_bounds-Tuple{Any, FilterConfig}","page":"Internals","text":"_resolve_psf_bounds(emitters, cfg) -> Union{NamedTuple, Nothing}\n\nPre-resolve PSF sigma filter bounds from the emitter population. Returns named tuple with :iso or :aniso bounds, or nothing if psf_sigma filter is off.\n\n\n\n\n\n","title":"SMLMAnalysis._resolve_psf_bounds"},{"category":"method","location":"api/internals/#SMLMAnalysis._rightmost_significant_peak-Tuple{AbstractVector{<:Real}, Real}","page":"Internals","text":"_rightmost_significant_peak(smoothed, peak_threshold) -> Int\n\nIndex of the rightmost local maximum of smoothed (a value ≥ both neighbours, with a missing right edge treated as 0) whose height is at least peak_threshold. Returns 1 if none qualifies. Shared by the valley-threshold search and the diagnostic figure so both mark the same peak.\n\n\n\n\n\n","title":"SMLMAnalysis._rightmost_significant_peak"},{"category":"method","location":"api/internals/#SMLMAnalysis._ripley_edge_weight-Tuple{Real, Real, Real, NTuple{4, var\"#s41\"} where var\"#s41\"<:Real}","page":"Internals","text":"_ripley_edge_weight(x, y, r, bounds) -> Float64\n\nRipley's isotropic edge correction: fraction of circle circumference at (x,y) with radius r that falls inside the rectangular FOV defined by bounds.\n\nReturns a weight in (0, 1]. Points near boundaries get lower weights, which are inverted to up-weight their contributions.\n\n\n\n\n\n","title":"SMLMAnalysis._ripley_edge_weight"},{"category":"function","location":"api/internals/#SMLMAnalysis._run_pipeline","page":"Internals","text":"_run_pipeline(initial_state, steps, camera, outdir, verbose)\n\nInternal pipeline executor. Folds analyze() dispatch over the steps vector, threading state through each step. Step routing is determined entirely by Julia's method dispatch on (state_type, config_type).\n\nWrong step ordering produces a MethodError — e.g., FilterConfig before DetectFitConfig gives no method matching analyze(::Vector{...}, ::FilterConfig).\n\n\n\n\n\n","title":"SMLMAnalysis._run_pipeline"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_box_overlay-NTuple{5, Any}","page":"Internals","text":"_save_box_overlay(dir, filename, images, roi_batch, box_colors; ...)\n\nROIBatch convenience (detectfit/filter): unpack x_corners/y_corners/frame_indices/roi_size and delegate to the core renderer above. Behavior unchanged for existing callers.\n\n\n\n\n\n","title":"SMLMAnalysis._save_box_overlay"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_box_overlay-NTuple{8, Any}","page":"Internals","text":"_save_box_overlay(dir, filename, images, x_corners, y_corners, frame_indices, box_size, box_colors; ...)\n\nCore box-overlay renderer: a grid of sample frames (contrast-stretched grayscale) with a colored box_size rectangle drawn at each (x_corner, y_corner) on its frame. This is the shared \"boxes on the raw data\" diagnostic style — detectfit (detection_overlay.png) and filter (fit_overlay.png) render through it so the whole pipeline's overlays are one visual family.\n\nThe box-geometry is passed as plain arrays so non-ROIBatch callers use the identical renderer; see the roi_batch convenience method below.\n\n\n\n\n\n","title":"SMLMAnalysis._save_box_overlay"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_calibration_figures-Tuple{Any, CalibrationResult, BasicSMLD}","page":"Internals","text":"Save calibration diagnostic plots from FrameConnectInfo.calibration. Called when calibration was enabled and outdir is set.\n\n\n\n\n\n","title":"SMLMAnalysis._save_calibration_figures"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_calibration_plot-Tuple{Any, CalibrationResult}","page":"Internals","text":"Plot uncertainty calibration: binned observed vs CRLB variance with WLS fit line. FC stores bin data in μm²; convert to nm² for readable axes.\n\n\n\n\n\n","title":"SMLMAnalysis._save_calibration_plot"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_config!-Tuple{String, AbstractSMLMConfig}","page":"Internals","text":"Save step config to config.toml in the step output directory.\n\n\n\n\n\n","title":"SMLMAnalysis._save_config!"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_continuous_drift_figure-Tuple{Any, Any, Any}","page":"Internals","text":"Plot drift trajectory with global frame view for multi-dataset acquisitions. Uses drift_trajectory default mode (inter + intra per chunk) so boundary discontinuities between chunks are visible as gaps.\n\n\n\n\n\n","title":"SMLMAnalysis._save_continuous_drift_figure"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_detectfit_sample_cache-NTuple{5, Any}","page":"Internals","text":"Save sample frame data to pipeline cache for the filter step to generate fit_overlay.png.\n\nDecomposes ROIBatch into plain arrays (avoids camera serialization issues with JLD2).\n\n\n\n\n\n","title":"SMLMAnalysis._save_detectfit_sample_cache"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_drift_jitter_plot-Tuple{Any, BasicSMLD}","page":"Internals","text":"Plot frame-to-frame drift jitter and cumulative drift from linked emitters. Three panels: jitter X, jitter Y (top), cumulative drift (bottom). Uses global frame numbering (absolute_frame across datasets).\n\n\n\n\n\n","title":"SMLMAnalysis._save_drift_jitter_plot"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_drift_model!-Tuple{Any, Any, Int64}","page":"Internals","text":"_save_drift_model!(group, dm::LegendrePolynomial, compression)\n\nSave LegendrePolynomial drift model to HDF5 group.\n\n\n\n\n\n","title":"SMLMAnalysis._save_drift_model!"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_filter_quality_figures-Tuple{Any, Any, FilterConfig}","page":"Internals","text":"_save_filter_quality_figures(dir, smld_raw, cfg)\n\nGenerate fit quality distribution plots with filter thresholds highlighted. Shows photons, background, precision, p-value, and PSF sigma distributions from the raw (unfiltered) SMLD, with rejected regions marked per FilterConfig.\n\n\n\n\n\n","title":"SMLMAnalysis._save_filter_quality_figures"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_fit_overlay-Tuple{Any, Any, Any, Any, Any, Any, Any, FilterConfig}","page":"Internals","text":"Generate fit overlay: boxes colored by fit quality using FilterConfig thresholds.\n\nColors: green=pass, red=photons fail, orange=precision fail, purple=pvalue fail, gray=no match.\n\n\n\n\n\n","title":"SMLMAnalysis._save_fit_overlay"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_fit_overlay_from_cache-Tuple{Any, Any, Any, FilterConfig}","page":"Internals","text":"Load detectfit sample cache and generate fit_overlay.png with FilterConfig thresholds. Gracefully skips if cache is missing (e.g., standalone filter without prior detectfit).\n\n\n\n\n\n","title":"SMLMAnalysis._save_fit_overlay_from_cache"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_info!-Tuple{String, Any}","page":"Internals","text":"_save_info!(dir::String, info; section::String=\"\")\n\nWrite upstream Info struct fields to info.toml in TOML format.\n\nWrites scalar fields (numbers, bools, strings, symbols, tuples of scalars). Skips complex fields (arrays, dicts, structs like BasicSMLD, models).\n\nWhen section is empty, writes a fresh file with type header. When section is provided, appends a [section] block.\n\n\n\n\n\n","title":"SMLMAnalysis._save_info!"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_loc_per_frame-Tuple{String, BasicSMLD}","page":"Internals","text":"_save_loc_per_frame(dir, smld; filename, title) -> bleach_result or nothing\n\nPlot localizations per absolute frame across all datasets, with photobleaching exponential decay fit overlay (a + b*exp(-k*t)). Marks dataset boundaries for multi-dataset SMLDs.\n\nUsed by detectfit (raw fits) and filter (post-filter fits) steps.\n\n\n\n\n\n","title":"SMLMAnalysis._save_loc_per_frame"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_multitarget_config!-Tuple{MultiTargetConfig}","page":"Internals","text":"_save_multitarget_config!(config::MultiTargetConfig)\n\nSerialize MultiTargetConfig to TOML file in the output directory.\n\n\n\n\n\n","title":"SMLMAnalysis._save_multitarget_config!"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_pipeline_config!-Tuple{String, Any, SMLMData.AbstractCamera, Any, Int64, Int64}","page":"Internals","text":"_save_pipeline_config!(dir, steps, camera, roi, verbose, checkpoint)\n\nWrite the orchestration-level AnalysisConfig to dir/config.toml.\n\nMirrors the per-step _save_config! (which writes each step's config into its own subdir) but captures what those don't: the camera, the global ROI crop, verbosity, checkpoint level, and a manifest of the step sequence.\n\n\n\n\n\n","title":"SMLMAnalysis._save_pipeline_config!"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_shift_histogram-Tuple{Any, CalibrationResult}","page":"Internals","text":"Plot histogram of frame-to-frame shifts from calibration analysis.\n\n\n\n\n\n","title":"SMLMAnalysis._save_shift_histogram"},{"category":"method","location":"api/internals/#SMLMAnalysis._save_step_smld-Tuple{Union{Nothing, String}, BasicSMLD}","page":"Internals","text":"_save_step_smld(dir, smld; filename, kwargs...)\n\nPersist a step's output SMLD via JLD2 so downstream iteration (e.g., diagnostic plots, parameter sweeps, BaGoL re-runs) can resume without re-running the upstream pipeline. The SMLD is stored under the smld key:\n\ndata = JLD2.load(\"path/smld_corrected.jld2\")\nsmld = data[\"smld\"]   # full BasicSMLD with camera, n_frames, n_datasets\n\nExtra named values are stored as additional top-level keys (e.g., pass drift_model=drift_model to embed the drift model alongside the SMLD).\n\nNo-op if dir is nothing.\n\n\n\n\n\n","title":"SMLMAnalysis._save_step_smld"},{"category":"method","location":"api/internals/#SMLMAnalysis._select_sources-Tuple{AbstractVector, Union{Nothing, AbstractVector{Int64}}}","page":"Internals","text":"Apply cfg.datasets selection to a resolved source list. Bounds-checks and returns the selected subset in the order given. Pass-through when cfg.datasets is nothing.\n\n\n\n\n\n","title":"SMLMAnalysis._select_sources"},{"category":"method","location":"api/internals/#SMLMAnalysis._smld_to_columnar-Tuple{Union{Nothing, BasicSMLD}}","page":"Internals","text":"Convert SMLD emitters to columnar format for fast JLD2 serialization. Returns a NamedTuple with arrays for each field.\n\n\n\n\n\n","title":"SMLMAnalysis._smld_to_columnar"},{"category":"method","location":"api/internals/#SMLMAnalysis._spatial_bin_rates-Tuple{Any, Any, Any, IntensityFilterConfig}","page":"Internals","text":"_spatial_bin_rates(xs, ys, photons, cfg)\n\nBin emitters spatially and compute rate_percentile of photons per bin. Returns named tuple with centers_x, centers_y, rates, counts, and grid info.\n\n\n\n\n\n","title":"SMLMAnalysis._spatial_bin_rates"},{"category":"method","location":"api/internals/#SMLMAnalysis._toml_value-Tuple{Any}","page":"Internals","text":"_toml_value(v) -> String\n\nRender a scalar config/info value as a syntactically valid TOML value string.\n\nThe pipeline writes config.toml / info.toml provenance records with a hand-rolled serializer; without this, common config value types emit invalid TOML: tuples render as (500.0, Inf) (parens aren't TOML, Inf must be inf), ranges as 1:19, symbol vectors as [:red, :blue], and strings interpolate unescaped \"/\\. Routing every scalar emit through _toml_value guarantees the .toml files parse back.\n\nMapping:\n\nAbstractString → escaped TOML basic string (quotes \", escapes \\ \" \\n \\t \\r).\nSymbol → quoted string.\nBool → \"true\"/\"false\" (checked before Integer, since Bool <: Integer).\nInteger → decimal.\nAbstractFloat → decimal, with Inf/NaN mapped to TOML inf/-inf/nan.\nAbstractRange → quoted string form (e.g. \"1:19\"; not materialized).\nTuple/AbstractVector → TOML array, elements recursed (e.g. (500.0, Inf) → [500.0, inf]).\nanything else → its string(...) form, quoted, so nothing is ever emitted raw/invalid.\n\n\n\n\n\n","title":"SMLMAnalysis._toml_value"},{"category":"method","location":"api/internals/#SMLMAnalysis._valley_threshold-Tuple{Vector{Int64}}","page":"Internals","text":"Find threshold using valley detection between isolated (low neighbors) and clustered (high neighbors).\n\nAlgorithm:\n\nBuild and smooth histogram\nFind rightmost significant peak (clustered population)\nFind local minimum (valley) between origin and that peak\nIf no clear valley (unimodal), use heuristics based on peak position\n\n\n\n\n\n","title":"SMLMAnalysis._valley_threshold"},{"category":"method","location":"api/internals/#SMLMAnalysis._with_dataset-Union{Tuple{T}, Tuple{Emitter2DFit{T}, Int64}} where T","page":"Internals","text":"Update emitter's dataset field using struct reconstruction\n\n\n\n\n\n","title":"SMLMAnalysis._with_dataset"},{"category":"method","location":"api/internals/#SMLMAnalysis._with_log_file-Tuple{Any, Any}","page":"Internals","text":"_with_log_file(f, outdir)\n\nRun f() with @info output teed to outdir/log.txt. If outdir is nothing, just runs f() directly.\n\n\n\n\n\n","title":"SMLMAnalysis._with_log_file"},{"category":"method","location":"api/internals/#SMLMAnalysis._write_camera!-Tuple{IO, SMLMData.AbstractCamera}","page":"Internals","text":"_write_camera!(io, cam)\n\nWrite a compact [camera] provenance block: type, pixel grid dimensions, and pixel size derived from the edge vectors (rather than dumping the full edges). sCMOS calibration fields are written as scalars, or summarized by shape if maps.\n\n\n\n\n\n","title":"SMLMAnalysis._write_camera!"},{"category":"method","location":"api/internals/#SMLMAnalysis._write_composite_readme!-Tuple{String, MultiTargetConfig, Vector{<:BasicSMLD}, Vector{StepInfo}}","page":"Internals","text":"_write_composite_readme!(composite_dir, config, smlds, step_infos)\n\nWrite a README.md in the composite directory documenting the color scheme, channel labels, multi-target steps, and per-channel localization counts.\n\n\n\n\n\n","title":"SMLMAnalysis._write_composite_readme!"},{"category":"method","location":"api/internals/#SMLMAnalysis._write_config_fields!-Tuple{IO, Any}","page":"Internals","text":"Write config fields to TOML. Nested structs become [section] blocks.\n\n\n\n\n\n","title":"SMLMAnalysis._write_config_fields!"},{"category":"method","location":"api/internals/#SMLMAnalysis._write_detection_frame_movies-NTuple{6, Any}","page":"Internals","text":"_write_detection_frame_movies(dir, stacks, abs_frames, fps, v)\n\nDEBUG-verbosity output. For each detection-overlay gallery frame, write a short realtime MP4 of the raw camera frames. stacks[k] is a (H, W, ~100)-frame window containing gallery frame abs_frames[k] (same order as the overlay panel). Contrast is ONE global linear stretch over the whole 100-frame window: black point = 0.1st percentile (0.001), white point = 99.9th percentile (0.999) of all pixels in the window. Played at fps (realtime = 1/exposure). A label (frame / fps / stretch percentiles / black-white values) is burned into each frame. Files: dir/frame_movies/detection_frame_<gallery_frame>.mp4. Requires ffmpeg on PATH.\n\n\n\n\n\n","title":"SMLMAnalysis._write_detection_frame_movies"},{"category":"method","location":"api/internals/#SMLMAnalysis._write_info_field!-Tuple{IO, Symbol, Number}","page":"Internals","text":"Write a single field to info.toml, skipping complex types.\n\n\n\n\n\n","title":"SMLMAnalysis._write_info_field!"},{"category":"method","location":"api/internals/#SMLMAnalysis._write_residual_correlation!-Tuple{IO, Any, Int64}","page":"Internals","text":"Write residual correlation section to stats.md.\n\n\n\n\n\n","title":"SMLMAnalysis._write_residual_correlation!"},{"category":"method","location":"api/internals/#SMLMAnalysis.bagol_step-Tuple{BasicSMLD, BaGoLConfig}","page":"Internals","text":"bagol_step(smld, cfg; outdir=nothing, step_number=0, verbose=Verbosity.STANDARD)\n\nGroup localizations into emitters via BaGoL. Returns (bagol_smld, BaGoLInfo).\n\n\n\n\n\n","title":"SMLMAnalysis.bagol_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.cache_dir-Tuple{Union{Nothing, String}}","page":"Internals","text":"cache_dir(outdir) -> Union{String, Nothing}\n\nReturns joinpath(outdir, \".cache\") or nothing if outdir is nothing.\n\n\n\n\n\n","title":"SMLMAnalysis.cache_dir"},{"category":"method","location":"api/internals/#SMLMAnalysis.composite_render_step-Tuple{Vector{<:BasicSMLD}, CompositeRenderConfig}","page":"Internals","text":"composite_render_step(smlds, cfg; outdir, step_number, verbose, colors) -> (smlds, CompositeRenderInfo)\n\nRender a multi-channel composite image. SMLDs pass through unmodified.\n\n\n\n\n\n","title":"SMLMAnalysis.composite_render_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.crossalign_step-Tuple{Vector{<:BasicSMLD}, CrossAlignConfig}","page":"Internals","text":"crossalign_step(smlds, cfg; outdir, step_number, verbose) -> (aligned_smlds, CrossAlignInfo)\n\nAlign multiple SMLDs to a common reference using entropy-based alignment.\n\n\n\n\n\n","title":"SMLMAnalysis.crossalign_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.crosscorr_step-Tuple{Vector{<:BasicSMLD}, CrossCorrConfig}","page":"Internals","text":"crosscorr_step(smlds, cfg; outdir, step_number, verbose, labels) -> (smlds, CrossCorrInfo)\n\nCompute pair cross-correlation g(r) between two channels. SMLDs pass through unmodified.\n\n\n\n\n\n","title":"SMLMAnalysis.crosscorr_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.densityfilter_step-Tuple{BasicSMLD, DensityFilterConfig}","page":"Internals","text":"densityfilter_step(smld, cfg; outdir=nothing, step_number=0, verbose=Verbosity.STANDARD)\n\nFilter localizations by neighbor density. Returns (filtered_smld, DensityFilterInfo).\n\nArguments\n\nsmld::BasicSMLD: Input localizations\ncfg::DensityFilterConfig: Density filter parameters\n\nKeyword Arguments\n\noutdir: Output directory (nothing to skip file output)\nstep_number: Step number for output directory naming\nverbose: Verbosity level\n\nReturns\n\n(filtered_smld, DensityFilterInfo)\n\n\n\n\n\n","title":"SMLMAnalysis.densityfilter_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.detectfit-Tuple{AbstractArray{<:Real, 3}, SMLMData.AbstractCamera, DetectFitConfig}","page":"Internals","text":"detectfit(images::AbstractArray{<:Real,3}, camera, cfg; kwargs...) -> (smld, info)\n\nConvenience method for a single image stack. Wraps into a 1-element vector.\n\n\n\n\n\n","title":"SMLMAnalysis.detectfit"},{"category":"method","location":"api/internals/#SMLMAnalysis.detectfit-Tuple{SMLMData.AbstractCamera, DetectFitConfig}","page":"Internals","text":"detectfit(camera, cfg; kwargs...) -> (smld, info)\n\nFile-based detectfit. Loads image data from cfg.path or cfg.paths. Each data source is loaded one at a time for memory efficiency.\n\n\n\n\n\n","title":"SMLMAnalysis.detectfit"},{"category":"method","location":"api/internals/#SMLMAnalysis.detectfit-Tuple{Vector{<:AbstractArray{<:Real, 3}}, SMLMData.AbstractCamera, DetectFitConfig}","page":"Internals","text":"detectfit(data, camera, cfg; kwargs...) -> (smld, DetectFitInfo)\n\nRun combined detection and fitting on image data.\n\nArguments\n\ndata::Vector{<:AbstractArray{<:Real,3}}: Vector of image stacks (one per dataset)\ncamera::AbstractCamera: Camera model\ncfg::DetectFitConfig: Detection and fitting configuration\n\nKeywords\n\noutdir::Union{String,Nothing}=nothing: Output directory for results\nstep_number::Int=1: Step number in the pipeline\nverbose::Int=Verbosity.STANDARD: Verbosity level\n\nReturns\n\n(smld::BasicSMLD, info::DetectFitInfo)\n\n\n\n\n\n","title":"SMLMAnalysis.detectfit"},{"category":"method","location":"api/internals/#SMLMAnalysis.driftcorrect_step-Tuple{BasicSMLD, DriftConfig}","page":"Internals","text":"driftcorrect_step(smld, cfg; outdir, step_number, verbose) -> (corrected_smld, DriftInfo)\n\nRun drift correction on smld, returning the corrected SMLD as the primary result, plus the upstream DriftInfo.\n\nReturns\n\n(corrected_smld, DriftInfo)\n\n\n\n\n\n","title":"SMLMAnalysis.driftcorrect_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.filter_step-Tuple{BasicSMLD, FilterConfig}","page":"Internals","text":"filter_step(smld, cfg; outdir=nothing, step_number=0, verbose=Verbosity.STANDARD)\n\nFilter localizations by quality criteria. Returns (filtered_smld, FilterInfo).\n\nDiagnostic plots use smld (the input) for pre-filter distributions.\n\nArguments\n\nsmld::BasicSMLD: Input localizations\ncfg::FilterConfig: Filter criteria\n\nKeyword Arguments\n\noutdir: Output directory (nothing to skip file output)\nstep_number: Step number for output directory naming\nverbose: Verbosity level\n\nReturns\n\n(filtered_smld, FilterInfo)\n\n\n\n\n\n","title":"SMLMAnalysis.filter_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.frameconnect_step-Tuple{BasicSMLD, FrameConnectConfig}","page":"Internals","text":"frameconnect_step(smld, cfg; outdir, step_number, verbose) -> (combined_smld, FrameConnectInfo)\n\nRun frame connection on smld, returning the combined (recombined) SMLD as the primary result, plus the upstream FrameConnectInfo.\n\nWhen cfg.calibration is set, uncertainty calibration is applied before combination: link -> calibrate -> combine (single pass with correct weights).\n\nReturns\n\n(combined_smld, FrameConnectInfo)\n\n\n\n\n\n","title":"SMLMAnalysis.frameconnect_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.intensityfilter_step-Tuple{BasicSMLD, IntensityFilterConfig}","page":"Internals","text":"intensityfilter_step(smld, cfg; outdir=nothing, step_number=0, verbose=Verbosity.STANDARD)\n\nFilter localizations by intensity-based multi-emitter rejection. Returns (filtered_smld, IntensityFilterInfo).\n\n\n\n\n\n","title":"SMLMAnalysis.intensityfilter_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.load_cache-Tuple{Union{Nothing, String}, String}","page":"Internals","text":"load_cache(outdir, filename) -> Union{Dict, Nothing}\n\nLoad data from outdir/.cache/filename via JLD2. Returns nothing if missing or outdir is nothing.\n\n\n\n\n\n","title":"SMLMAnalysis.load_cache"},{"category":"method","location":"api/internals/#SMLMAnalysis.render_step-Tuple{BasicSMLD, RenderConfig}","page":"Internals","text":"render_step(smld, cfg; outdir=nothing, step_number=0, verbose=Verbosity.STANDARD)\n\nRender localizations to a super-resolution image. Returns (render_image, RenderInfo).\n\nArguments\n\nsmld::BasicSMLD: Input localizations\ncfg::SMLMRender.RenderConfig: Render configuration\n\nKeyword Arguments\n\noutdir: Output directory (nothing to skip file output)\nstep_number: Step number for output directory naming\nverbose: Verbosity level\n\nReturns\n\n(render_image, RenderInfo)\n\n\n\n\n\n","title":"SMLMAnalysis.render_step"},{"category":"method","location":"api/internals/#SMLMAnalysis.save_cache-Tuple{Union{Nothing, String}, String}","page":"Internals","text":"save_cache(outdir, filename; kwargs...)\n\nSave data to outdir/.cache/filename via JLD2. No-op if outdir is nothing.\n\n\n\n\n\n","title":"SMLMAnalysis.save_cache"}]
}
