# Internals

```@meta
CurrentModule = SMLMAnalysis
```

Non-exported helpers. These are implementation details and may change between
releases — they are documented here for contributors. See
[Extending the Pipeline](@ref) for the public extension points.

```@autodocs
Modules = [SMLMAnalysis]
Public = false
```
